import add from './math'

##
  Description: project entrypoint calling imported add function
##
entry main = f(args: string[]): void =>
    let result = add(3, 4)
    print('{result}')
    return void
