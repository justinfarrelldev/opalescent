# _fs_read_text_invalid_utf8

A minimal filesystem fixture project reserved for rerunnability coverage of the invalid-UTF-8 `read_text_sync` project directory contract.

## How to compile and run

```bash
opal run src/main.op
```
