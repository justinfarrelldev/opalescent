# _fs_path_file_name

A minimal filesystem fixture project reserved for rerunnability coverage of the `path_file_name` project directory contract.

## How to compile and run

```bash
opal run src/main.op
```
