# import-types-basic

Demonstrates importing a single type from a `.types.op` file and using it in `main.op`.

This is a success fixture — it should compile and run successfully.
Expected output: `Alice is 30 years old`
