##
  Description: starting point of the application — tests string interpolation with multiple values
##
entry main = f(args: string[]): void =>
    let name = 'world'
    let count: int32 = 42
    let result = 'Hello {name}, count is {count}'
    print(result)
    return void
