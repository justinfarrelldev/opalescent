# fs-path-manipulation

An end-to-end Opalescent fixture that showcases path algebra only: lexical normalization, component joins, path queries, and absolute resolution without mutating the filesystem.

## How to run

```bash
opal run src/main.op
```

Expected stdout:

```text
passed 40/40
```
