# import-types-multiple

Demonstrates importing multiple types from a single `.types.op` file.

This is a success fixture — it should compile and run successfully.
Expected output: `Rect 10x20 at (0,0)`
