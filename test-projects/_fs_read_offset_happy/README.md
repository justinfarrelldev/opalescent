# _fs_read_offset_happy

A minimal filesystem fixture project reserved for rerunnability coverage of the happy-path offset-read project directory contract.

## How to compile and run

```bash
opal run src/main.op
```
