##
  Description: Test project with deliberate errors from multiple compiler phases
##
entry main = f(args: string[]): void =>
    # Type error 1: assigning string to int32
    let x: int32 = 'hello'
    
    # Type error 2: assigning string to int32
    let y: int32 = 'world'
    
    print('This should not compile')
    return void
