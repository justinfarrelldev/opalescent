##
    Description: starting point of the application
##
entry main = f(args: string[]): void =>
    print('Hello world!')
    return void

##
    Description: second entry point, which should not be allowed in a single file
##
entry second = f(): void =>
    print('This should cause a DuplicateEntryPoint error')
    return void