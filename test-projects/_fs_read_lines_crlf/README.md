# _fs_read_lines_crlf

A minimal filesystem fixture project reserved for rerunnability coverage of the CRLF `read_lines_sync` project directory contract.

## How to compile and run

```bash
opal run src/main.op
```
