# _fs_read_contents_happy

A minimal filesystem fixture project reserved for rerunnability coverage of the happy-path `read_contents_sync` project directory contract.

## How to compile and run

```bash
opal run src/main.op
```
