# hello-world

An end-to-end test project for the Opalescent compiler. This project prints a greeting to stdout to verify basic string interpolation and the compiler's ability to compile and run a simple program.

## How to compile and run

Use the Opalescent compiler to compile and execute the program:

```bash
opal src/main.op
```

This will print `Hello world` to stdout.
