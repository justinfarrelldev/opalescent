# fs-directory-operations

An end-to-end Opalescent fixture that reads a directory seed plan, creates a deterministic workspace tree, lists the alpha directory, removes everything it created, and reports matching lifecycle counts.
