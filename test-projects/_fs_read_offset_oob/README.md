# _fs_read_offset_oob

A minimal filesystem fixture project reserved for rerunnability coverage of the out-of-bounds offset-read project directory contract.

## How to compile and run

```bash
opal run src/main.op
```
