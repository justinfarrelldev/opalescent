# _fs_path_parent_directory

A minimal filesystem fixture project reserved for rerunnability coverage of the `path_parent_directory` project directory contract.

## How to compile and run

```bash
opal run src/main.op
```
