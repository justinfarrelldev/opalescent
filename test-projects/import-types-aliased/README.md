# import-types-aliased

Demonstrates importing a type from a `.types.op` file using an alias (`import type User as Account`).

This is a success fixture — it should compile and run successfully.
Expected output: `User 42: Bob`
