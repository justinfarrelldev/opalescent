# _fs_read_lines_lf

A minimal filesystem fixture project reserved for rerunnability coverage of the LF `read_lines_sync` project directory contract.

## How to compile and run

```bash
opal run src/main.op
```
