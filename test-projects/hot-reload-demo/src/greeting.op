
# All public functions need doc comments that are more than 30 chars long

##
  Description: Returns the message exported by the hot-reload demo module.
##
public let greet = f(): string =>
	return 'Hello from hot-reload-demo'
