# hot-reload-demo

A minimal Opalescent project demonstrating module hot-reloading. The `greeting` module exports a `greet()` function that can be hot-swapped at runtime without restarting the host process.

## How to compile and run

```bash
opal src/greeting.op
```
