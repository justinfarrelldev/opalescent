import frame_clock_new from standard

##
  Description: Verifies frame_clock_new rejects zero fps.
##
entry main = f(args: string[]): void errors InvalidFrameRateError =>
    guard frame_clock_new(0) else err =>
        print('fps=0:{err}')
        propagate err

    print('UNEXPECTED_SUCCESS fps=0')
    return void
