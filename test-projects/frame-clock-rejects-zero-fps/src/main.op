##
  Description: Verifies FrameClock constructor rejects zero fps.
##
entry main = f(args: string[]): void errors InvalidFrameRateError =>
    guard new FrameClock:
        frames_per_second: 0
    else err =>
        print('fps=0:{err}')
        propagate err

    print('UNEXPECTED_SUCCESS fps=0')
    return void
