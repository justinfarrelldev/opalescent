##
  Description: Inserts text at a zero-based Unicode scalar index.
##
public let string_insert_at = f(text: string, scalar_index: int64, inserted: string): string errors StringRangeError, AllocationFailureError => {
    return intrinsic
}

##
  Description: Deletes the Unicode scalar range from start inclusive to end exclusive.
##
public let string_delete_range = f(text: string, start: int64, end: int64): string errors StringRangeError, AllocationFailureError => {
    return intrinsic
}

##
  Description: Replaces the Unicode scalar range from start inclusive to end exclusive.
##
public let string_replace_range = f(text: string, start: int64, end: int64, replacement: string): string errors StringRangeError, AllocationFailureError => {
    return intrinsic
}
