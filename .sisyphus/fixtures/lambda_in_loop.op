##
  Description: Task 14 fixture ensuring lambda break does not inherit outer loop targets.
##
entry main = f(args: string[]): void =>
    let mutable i = 0
    while i < 1:
        f(): void => {
            break
        }()
        i = i + 1
    return void
