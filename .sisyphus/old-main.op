entry main = f(): void => {
    print('What is your name?')
    let name = take_input()
    let quiz_num = random_int32(1, 5)
    while true {
        let s = take_input()
        if s is 'x' {
            return void
        }
    }
    return void
}
