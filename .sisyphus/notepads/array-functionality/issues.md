- 2026-05-05T00:00:00-04:00 Initial red fixture pass hit unrelated syntax/doc gates (`missing_doc_comment`, lambda syntax, and numeric-literal typing), so the fixtures were tightened to reach the intended array-feature failures instead.
- 2026-05-05T00:00:00-04:00 `array-append` currently fails with `symbol_not_found` for `append`; `array-push`, `array-pop`, `array-map`, and `array-filter` fail with `unsupported call callee expression`; `array-reduce` still fails before runtime, so no green behavior is implemented yet.
- 2026-05-05T00:00:00-04:00 `test-projects/array-zip` remains intentionally absent in this task, per plan dependency on a later Pair-proof gate.
- 2026-05-05T00:45:20-04:00 Task 3 issue discovered/fixed: guard/propgate array metadata extraction initially risked reading the error pointer as capacity for legacy 3-field result structs; the bridge now falls back to `capacity = length` unless a dedicated capacity field is present.
- 2026-05-05T01:10:00-04:00 Task 4 verification found two pre-existing fixture constraints that can mask member-call behavior: empty-array initialization still needs a workaround in some paths, and `int32[]` fixtures must use explicit `as int32` numeric literals to avoid default `[int64]` inference failures before method dispatch.
- 2026-05-05T01:10:00-04:00 Task 4 intentionally leaves array method semantics RED; `.push(...)` now fails at dedicated array lowering with `array method 'push' is not implemented yet`, which is the expected prerequisite state for Task 6.
- 2026-05-05T00:20:44-04:00 Task 5 issue discovered/fixed: unannotated `let grown = append(...)` initially fell back to non-array codegen inference, so `grown.length` hit product-field access; fixing call-return inference for `append` and preserving `_len`/`_cap` metadata resolved the failure.
- 2026-05-05T00:20:44-04:00 Task 5 issue discovered/fixed: mutable reassignment like `xs = append(xs, value)` initially dropped array metadata after the store; assignment lowering now updates existing `<name>_len`/`<name>_cap` bindings when an array expression leaves pending metadata.
- 2026-05-05T00:33:30-04:00 Task 6 issue discovered/fixed: the push happy-path fixture initially still failed before codegen because annotated empty arrays were compared as `[int32]` vs `[t0]`; allowing annotated-let reconciliation to fall back to structural unification fixed the empty-array start case required for `.push`.
- 2026-05-05T00:33:30-04:00 Task 6 issue discovered/fixed: the push void-misuse negative test initially asserted the wrong diagnostic prefix; the stable compiler message is a type mismatch between `int32` and `unit`, so the assertion now checks the semantic content instead of one exact headline string.
- 2026-05-05T00:45:32-04:00 Task 7 issue discovered/fixed: running `array_pop_runs` and `array_pop_on_empty_array_traps` in parallel produced a false failure because both CLI invocations target the same `target/program` path; sequential execution yields stable evidence for the happy-path and trap cases.
- 2026-05-05T05:01:40.482873+00:00 Task 8 verification gotcha: map-focused integration tests must run sequentially because `opal run` uses the shared `target/program` path; parallel runs can fail spuriously with `Text file busy (os error 26)` even when the map implementation itself is correct.
- 2026-05-05T00:00:00Z Task 8 issue discovered/fixed: a supposedly green empty-map run returned the previous happy-path stdout because multiple `cargo test` invocations were launched at once against the same `target/program`; rerunning sequentially and serializing the integration harness removed the false negative and the later `Text file busy (os error 26)` race.
- 2026-05-05T05:42:26.048762+00:00 Task 8 issue discovered/fixed: `array_map_empty_returns_empty_array` regressed because annotated empty arrays were compared as `[int32]` vs `[t0]`; the checker now treats an empty array literal like other annotation-guided literals and adopts the expected array type during let reconciliation.
- 2026-05-05T05:42:26.048762+00:00 Task 8 issue discovered/fixed: `array_append_runs` was still red after the map fix because `append` only existed as a compiler-lowered codegen special-case, not as an exported `standard` module symbol; adding the builtin module signature restored import/type-check compatibility without changing runtime lowering.
- 2026-05-05T05:44:56.384110+00:00 Task 8 formatting gate: running `cargo fmt --all` and `cargo fmt --all -- --check` in parallel can show stale pre-format diffs; rerun the check sequentially after fmt to evaluate the real gate result.
- 2026-05-05T05:50:00+00:00 Task 9 verification gotcha: even with the per-test mutex inside `tests/array_integration.rs`, launching separate `cargo test` commands in parallel still races on the shared `target/program` binary and can fabricate `Permission denied (os error 13)` or swapped stdout; keep all filter verification commands serialized.
