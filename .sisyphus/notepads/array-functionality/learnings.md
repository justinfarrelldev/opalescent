- 2026-05-05T00:00:00-04:00 Task 2 harness pattern: `tests/array_integration.rs` mirrors `tests/fmt_integration.rs` by resolving `target/debug/opalescent`, `test-projects/<project>/src/main.op`, and `expected/stdout.txt`, then asserting command success and stdout equality.
- 2026-05-05T00:00:00-04:00 Array fixtures need explicit `##` doc blocks on `entry main`; otherwise the compiler stops at the doc-comment gate before exercising array behavior.
- 2026-05-05T00:00:00-04:00 The existing array API surface uses method-style calls for `map`, `filter`, and `reduce` in `language-spec/array_helpers.op`, while the member-call lowering is still missing, so the red fixtures now fail in the intended compiler area.
- 2026-05-05T00:45:20-04:00 Task 3 foundation bridge: runtime `OpalArray<T>` now tracks explicit capacity alongside elements, and codegen array bindings now carry both `_len` and `_cap` metadata so future array operations do not depend only on the old pending-length side channel.
- 2026-05-05T00:45:20-04:00 Compatibility rule: existing 3-field error ABI array results are interpreted as `{value, length, error}` and default capacity to length; this keeps current propagate/guard behavior stable while enabling capacity-aware lowering for later slices.
- 2026-05-05T01:10:00-04:00 Task 4 member-call lowering now intercepts array `Expr::Member` callees in `src/codegen/functions_call.rs`, preserving identifier/lambda call behavior while shifting `.push(...)` failures from `unsupported call callee expression` to method-specific array lowering errors.
- 2026-05-05T01:10:00-04:00 Immutable array receiver rejection is enforced during call type checking in `src/type_system/checker/call_resolution.rs`; the integration compile-fail path asserts diagnostics include both `mutable` and `push`.
- 2026-05-05T00:20:44-04:00 Task 5 append: standard `append` works as a compiler-lowered intrinsic rather than a standalone runtime ABI symbol; codegen returns the new array pointer and hands length/capacity through `pending_array_metadata` so later `.length` and indexing use the grown array metadata.
- 2026-05-05T00:20:44-04:00 Task 5 append: array integration stdout must ignore the CLI's leading compiled-binary path line (`target/program`) to compare only program output against fixture goldens.
- 2026-05-05T00:33:30-04:00 Task 6 push: `src/codegen/functions_call.rs` now lowers `.push` by reusing the append-style growth helper, then immediately stores the new array pointer plus refreshed `_len`/`_cap` back into the mutable local binding so later `.length` and indexing observe the mutation.
- 2026-05-05T00:33:30-04:00 Task 6 push: annotated empty-array locals such as `let mutable xs: int32[] = []` now reconcile through checker unification in `src/type_system/checker/statements.rs`, which unblocks the exact push fixture source without reintroducing the old cast workaround.
- 2026-05-05T00:45:32-04:00 Task 7 pop: `.pop()` lowers in `src/codegen/functions_call.rs` through the same mutable-binding metadata writeback path as `.push`, but returns the removed element and preserves `_cap` while shrinking `_len`.
- 2026-05-05T00:45:32-04:00 Task 7 verification: array integration pop scenarios must run sequentially, not in parallel, because the CLI compiles/runs through a shared `target/program` path and concurrent runs can cross-contaminate stderr/stdout evidence.
- 2026-05-05T00:45:32-04:00 Task 7 pop: `.pop()` lowers in `src/codegen/functions_call.rs` through the same mutable-binding metadata writeback path as `.push`, but returns the removed element and preserves `_cap` while shrinking `_len`.
- 2026-05-05T00:45:32-04:00 Task 7 verification: array integration pop scenarios must run sequentially, not in parallel, because the CLI compiles/runs through a shared `target/program` path and concurrent runs can cross-contaminate stderr/stdout evidence.
- 2026-05-05T05:01:40.482873+00:00 Task 8 map: `.map` lowering in `src/codegen/functions_call.rs` now emits a compiler-generated loop that allocates the output array once from the source length, invokes the callback per element, and carries `_len`/`_cap` through pending array metadata so later `.length` and indexing use the mapped array correctly.
- 2026-05-05T05:01:40.482873+00:00 Task 8 map: unannotated `let out = xs.map(...)` needed `src/codegen/statements.rs` member-call return inference for collection intrinsics; without that, codegen fell back to `int64` and lost the callback result element type even though checker inference already knew `U`.
- 2026-05-05T00:00:00Z Task 8 verification: guarding `run_opal_source` and `run_opal_check` with a shared mutex in `tests/array_integration.rs` makes the map happy-path, empty-input, and compile-fail cases reliable even when the Rust harness itself schedules tests concurrently against the shared `target/program` artifact.
- 2026-05-05T05:42:26.048762+00:00 Task 8 follow-up: the real regression was checker reconciliation for annotated empty array literals; extending `coerce_literal_to_expected` to let `[]` adopt an expected array type restores `let xs: int32[] = []` without changing `.map` semantics.
- 2026-05-05T05:42:26.048762+00:00 Task 8 verification reminder: launching multiple `cargo test` invocations in parallel can still fabricate `length 0` array outputs despite the per-test mutex, because separate test binaries race on the shared `target/program`; run the array suite sequentially.
- 2026-05-05T05:44:56.384110+00:00 Task 8 formatter retry: `cargo fmt --all` only rewrote short function signatures in `src/codegen/functions_call/array.rs` and `src/codegen/functions_call/tail.rs`; inspecting `git diff` confirmed line-wrap-only changes with no semantic edits.
- 2026-05-05T05:50:00+00:00 Task 9 filter: `src/codegen/functions_call/array.rs` lowers `.filter` as a compiler-generated loop with a separate write index, allocating a fresh buffer up front at source length and publishing the actual kept-count through pending array metadata so result order is preserved without mutating the source array.
- 2026-05-05T06:02:53.318549+00:00 Task 10 reduce: seeded `.reduce(initial, f)` now lowers in `src/codegen/functions_call/array.rs` as a compiler-generated left fold that reuses the existing array callback ABI, threads the accumulator through each element, and returns the initial seed unchanged when `length == 0`.
