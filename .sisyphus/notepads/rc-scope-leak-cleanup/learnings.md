
- Cleanup pass: keep the plan file and the rc-scope-leak-cleanup notepad artifacts under `.sisyphus/` tracked together.
- Temporary probe artifacts in `.sisyphus/tmp/` should be removed, not committed.
