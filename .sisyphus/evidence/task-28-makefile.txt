Task 28 — Fix Makefile.toml broken task

RED checks
- Verified install-deps task appears in task listing.
- Verified parse/plan generation works with cargo-make print output.

Change made
- Updated `tasks.install-deps-debian` in `Makefile.toml`:
  - Replaced invalid shell chaining via args array:
    - command = "sudo"
    - args = ["apt-get", "update", "&&", "sudo", "apt-get", "install", ...]
  - With valid cargo-make `script` array:
    - "sudo apt-get update"
    - "sudo apt-get install -y gcc-multilib g++-multilib"

Validation
1) cargo make --list-all-steps
   - PASS
   - `install-deps-debian` listed

2) cargo make --print-steps
   - PASS
   - Parsed execution plan printed successfully

3) Makefile content check
   - `install-deps-debian` uses `script`
   - no `&&` in args array for that task anymore

Regression checks
- cargo test lsp: PASS
- cargo test: PASS
- cargo make lint: PASS
