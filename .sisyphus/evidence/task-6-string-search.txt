Search evidence for task 6 guard-diagnostic migration:

- `tests/integration_e2e/guard_stmt.rs` contained `assert_constraint_reason_contains(...)` and one live call site at the start of the task.
- `src/type_system/tests.rs` still contained raw-text assertions for `success binding is not available inside guard error clause`.
- Structured guard diagnostics already existed in `src/type_system/errors.rs` / `src/type_system/tests.rs` via `TypeError::GuardErrorClauseMissingTerminal`, `GuardPropagateErrNotFinal`, `GuardReturnErrInvalid`, `GuardWrapperSourceInvalid`, and `GuardShorthandRequired`.


Current migration-proof searches:

- `tests/integration_e2e/guard_stmt.rs`: `assert_guard_variant(` appears at the guard cases, with the helper restored only for the remaining reason-string call site.
- `src/type_system/tests.rs`: remaining scope tests now assert `SymbolNotFound` where intended; strict-guard tests assert `TypeError::Guard*` variants.


Verified follow-up searches:

- `tests/integration_e2e/guard_optional_binding.rs` and `tests/integration_e2e/guard_shorthand.rs` now use strict terminal guard forms or explicit compile-fail assertions.
- `src/type_system/tests.rs` and `src/codegen/tests.rs` no longer contain stale strict-guard reason-string expectations in the migrated areas.
