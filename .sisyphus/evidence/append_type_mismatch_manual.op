import append from standard

##
  Description: Ad-hoc manual mismatch check for append.
##
entry main = f(args: string[]): void =>
    let xs: int32[] = [1 as int32]
    let grown = append(xs, 'x')
    print('grown length {grown.length}')
    return void
