Task 3 evidence: array metadata invariants
Date: 2026-05-05

Commands:
  cargo test array_runtime_metadata_preserves_length_and_capacity_invariants
  cargo test array_runtime_rejects_invalid_metadata_invariants
  cargo test --workspace

Results:
  PASS

Observed evidence:
  - runtime::tests::array_runtime_metadata_preserves_length_and_capacity_invariants ... ok
  - runtime::tests::array_runtime_rejects_invalid_metadata_invariants ... ok
  - cargo test --workspace passed

Invariant coverage:
  - explicit-capacity allocation preserves length and capacity metadata
  - append helper increments length exactly and preserves capacity reserve
  - shrink helper preserves capacity while reducing length
  - pop helper decrements length exactly and keeps capacity >= length
  - invalid capacity < length is rejected
  - invalid metadata length > stored element count is rejected
  - codegen bridge treats legacy 3-field error ABI array results as capacity = length
