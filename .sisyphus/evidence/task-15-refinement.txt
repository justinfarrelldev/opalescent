# Task 15 refinement verification

Date: 2026-08-21

Implemented checker support for nominal variant refinement, heterogeneous guard-error unions with branch-local narrowing, and `constrain Type from value` fallibility.

Follow-up coverage added after top-level verification:

- `if event is TerminalInputEvent.Key:` without `into` narrows the direct identifier in the true branch, allowing `event.key`, `event.occurrence`, and `event.modifiers` payload field access.
- The positive guard refinement case now uses a genuinely heterogeneous guard binding from `TerminalSessionOpenError | TerminalSessionReadError`; inside the `TerminalSessionOpenError.InvalidOptions` refined branch, `propagate err` is accepted by an enclosing function declaring only `TerminalSessionOpenError`.
- No checker implementation change was required; the existing narrowing paths already supported both requirements once the proof fixtures were corrected.

Final verification on the follow-up tree:

- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo fmt --all -- --check` - passed
- `bash scripts/check-line-count.sh` - passed
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo test terminal_refinement` - passed: 4 passed, 0 failed
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo test terminal_guard_error` - passed: 2 passed, 0 failed
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo test terminal_proposal` - passed: 23 passed, 0 failed
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo test` - passed: 1452 passed, 0 failed, 8 ignored; doctests 2 passed, 12 ignored
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo check` - passed
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo build` - passed
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo make lint` - passed

LSP diagnostics for `src/type_system/test_integration_modules.rs` were clean before final verification.
