Scenario A: Comments between declarations

Command:
  cargo run -- fmt --output /tmp/test_comments_out.op .sisyphus/tmp/test_comments.op

Formatted output (/tmp/test_comments_out.op):
# File header comment

entry main = f(): void => {
    return void
}

# Section separator

let helper = f(x: int32): int32 => {
    return x
}
