./.sisyphus/evidence/task-1-bytes-audit.txt:   - Classification: to be removed later by plan tasks (parser should consume registry lookup only for propertyless constructors; existing Bytes-specific messages remain intentional for this task).
./.sisyphus/evidence/task-1-bytes-audit.txt:   - Classification: intentional remaining usage (not a constructor special-case; Bytes surface support).
./.sisyphus/evidence/task-7-stringbuilder-doc-audit.txt:./.sisyphus/plans/cleaner-propertyless-constructors.md:Make propertyless opaque constructors a real compiler pattern instead of a `Bytes` parser exception, while preserving `Bytes` behavior and preventing accidental `StringBuilder` enablement.
./.sisyphus/evidence/task-7-stringbuilder-doc-audit.txt:./.sisyphus/plans/cleaner-propertyless-constructors.md:  **What to do**: Create a single source of truth for propertyless constructor metadata in a neutral source module (preferred: `src/intrinsics/propertyless_constructors.rs`; if no `src/intrinsics` module exists, add `src/intrinsics.rs` plus submodule wiring in `src/lib.rs`/crate root as required by existing module style). Define metadata with these fields exactly: `type_name: &'static str`, `runtime_function_name: &'static str`, and helper lookup functions `lookup_propertyless_constructor(type_name: &str) -> Option<PropertylessConstructorIntrinsic>` plus `is_propertyless_constructor_type(type_name: &str) -> bool`. Register exactly one intrinsic: `Bytes -> bytes_new`. Add a focused unit test in the registry module proving `Bytes` resolves to `bytes_new` and `StringBuilder` returns `None`. Before editing consumers, enumerate existing string-based special cases for `Bytes` in non-test Rust sources using `ast_grep_search` or equivalent and record which occurrences are expected to be removed by later tasks versus intentionally remaining (e.g., Bytes builtin registration/runtime implementation).
./.sisyphus/evidence/task-7-stringbuilder-doc-audit.txt:./.sisyphus/plans/cleaner-propertyless-constructors.md:  **What to do**: Update only implementation-adjacent comments and existing Bytes/prelude documentation that mention `new Bytes` as a special parser exception. Keep public docs accurate: `new Bytes` is the preferred construction form and `bytes_new()` remains legacy compatibility. If `stdlib/prelude.op` already documents this correctly, leave it unchanged. Do not update broad language spec pages unless they currently state the old parser-special-case behavior. Add a brief code comment near the registry explaining how future opaque handles (e.g., StringBuilder) can be registered later without parser changes, but explicitly note that only `Bytes` is registered now.
./.sisyphus/drafts/bytes-new-to-new-bytes.completed.md:- Syntax decision: parser may represent propertyless `new <Ident>`, but typechecker gates this plan to `Bytes` only; non-Bytes propertyless constructors remain invalid.
./.sisyphus/notepads/bytes-new-to-new-bytes/learnings.md:- 2026-05-17T00:00:00Z: Task 6 codegen lowering now treats bare zero-field `new Bytes` as a special constructor case in `src/codegen/adts.rs`, emitting the existing `bytes_new` stdlib/runtime declaration and a no-arg call without changing runtime ABI names or legacy `bytes_new()` lowering tests.
./.sisyphus/notepads/bytes-new-to-new-bytes/learnings.md:- 2026-05-17T00:00:00Z: Added Task 5 typechecker coverage for `new Bytes` with explicit and inferred lets plus negative assertions for `new Person`, `new Message.Text`, and `new bytes`. LSP diagnostics are clean on the modified files, but the requested `cargo test` filters are currently blocked by pre-existing compile errors in `src/parser/tests.rs` (invalid `Stmt::Let` destructuring expecting a removed `type_annotation` field at lines 6143 and 6181).
./.sisyphus/notepads/bytes-new-to-new-bytes/learnings.md:- 2026-05-17T00:00:00Z: Task 4 parser/formatter support now parses bare `new Bytes` as a zero-field constructor, preserves `new Type:` and `new Type.Variant:`, rejects `new Bytes()` and `new Bytes:`, and formatter output keeps `new Bytes` without rewriting legacy `bytes_new()` calls.
./.sisyphus/notepads/bytes-new-to-new-bytes/learnings.md:- 2026-05-17T00:00:00Z: Task 5 negative coverage now treats bare non-Bytes constructors as valid front-end rejections whether they fail in parsing or typechecking; with current parser behavior, `new Person`, `new Message.Text`, and `new bytes` are asserted via parser `InvalidSyntax` diagnostics while positive `new Bytes` tests still run through full typechecking.
./.sisyphus/notepads/bytes-new-to-new-bytes/learnings.md:- 2026-05-17T00:00:00Z: Task 5 negative assertions should match the stable front-end rule (`only supported for `Bytes``) instead of overfitting to one exact parser message per rejected callee, because bare non-Bytes constructors can share the same parser diagnostic template.
./.sisyphus/notepads/bytes-new-to-new-bytes/learnings.md:- 2026-05-17T00:00:00Z: Task 7 reconciliation confirmed `new Bytes` end-to-end support in parser/typechecker/codegen/formatter while preserving legacy `bytes_new()` behavior; full gates (`cargo test`, `cargo test --features integration`, pre-commit lint/test/build) passed after normalizing the local `C:\Users\justi\Downloads` fixture directory state for guard integration tests.
./.sisyphus/notepads/cleaner-propertyless-constructors/learnings.md:- The branch consults `lookup_propertyless_constructor(...)` and infers `CoreType::Generic { name: "Bytes", type_args: [] }` for `new Bytes`, aligning inference with parser/type-checker/codegen registry decisions.
./.sisyphus/plans/cleaner-propertyless-constructors.md:> **Summary**: Replace the current `new Bytes` parser/codegen special case with a registry-driven propertyless constructor design while preserving concrete behavior for `Bytes` only. Fix inferred `let buffer = new Bytes` codegen by teaching inference/codegen to derive `Bytes` from constructor metadata instead of falling through to `Int64`.
./.sisyphus/plans/cleaner-propertyless-constructors.md:Implement the cleaner design described in the review: parser accepts bare `new TypeName` generally, type checking decides whether that type has a propertyless constructor, and codegen consults a small intrinsic constructor table. Also resolve finding 1 (`new Bytes` inference/codegen falls through incorrectly) and finding 2 (`new Bytes` is a parser-level one-off).
./.sisyphus/plans/cleaner-propertyless-constructors.md:Make propertyless opaque constructors a real compiler pattern instead of a `Bytes` parser exception, while preserving `Bytes` behavior and preventing accidental `StringBuilder` enablement.
./.sisyphus/plans/cleaner-propertyless-constructors.md:- Must not hard-code `"Bytes"` in parser, type-checker propertyless decision logic, or codegen constructor lowering outside the registry/registration module.
./.sisyphus/plans/cleaner-propertyless-constructors.md:  **What to do**: Create a single source of truth for propertyless constructor metadata in a neutral source module (preferred: `src/intrinsics/propertyless_constructors.rs`; if no `src/intrinsics` module exists, add `src/intrinsics.rs` plus submodule wiring in `src/lib.rs`/crate root as required by existing module style). Define metadata with these fields exactly: `type_name: &'static str`, `runtime_function_name: &'static str`, and helper lookup functions `lookup_propertyless_constructor(type_name: &str) -> Option<PropertylessConstructorIntrinsic>` plus `is_propertyless_constructor_type(type_name: &str) -> bool`. Register exactly one intrinsic: `Bytes -> bytes_new`. Add a focused unit test in the registry module proving `Bytes` resolves to `bytes_new` and `StringBuilder` returns `None`. Before editing consumers, enumerate existing string-based special cases for `Bytes` in non-test Rust sources using `ast_grep_search` or equivalent and record which occurrences are expected to be removed by later tasks versus intentionally remaining (e.g., Bytes builtin registration/runtime implementation).
./.sisyphus/plans/cleaner-propertyless-constructors.md:  **What to do**: In `src/parser/tests.rs`, first add/adjust parser tests that prove syntax acceptance is name-agnostic: `new Bytes`, `new MyEmptyType`, and `new Module.MyOpaque` parse as `Expr::Constructor` with empty `fields`; existing field-block constructor syntax still parses; syntactically invalid forms such as `new Bytes()` still fail. Then update `src/parser/new_expression.rs` to remove the `Bytes`-only branch and accept bare identifier/member callee forms generically when no colon field block follows. The parser must not consult the propertyless constructor registry and must not decide whether the type is semantically allowed.
./.sisyphus/plans/cleaner-propertyless-constructors.md:  - Implementation: `src/parser/new_expression.rs` - `Parser::parse_new_expression` currently contains the `Bytes` exception and diagnostics.
./.sisyphus/plans/cleaner-propertyless-constructors.md:  - Tests: `src/parser/tests.rs` - Existing tests around `new Bytes`, rejection of `new Bytes()`, and constructor syntax.
./.sisyphus/plans/cleaner-propertyless-constructors.md:  - [ ] `cargo test --lib parser::tests::new_expression_rejects_call_postfix` exits 0 and proves `new Bytes()` remains invalid.
./.sisyphus/plans/cleaner-propertyless-constructors.md:    Expected: Command exits 0; test asserts `new Bytes()` returns a parser error.
./.sisyphus/plans/cleaner-propertyless-constructors.md:  - Implementation: `src/codegen/adts.rs` - `codegen_constructor_expression` currently special-cases empty `Bytes` constructors and calls `bytes_new`.
./.sisyphus/plans/cleaner-propertyless-constructors.md:  - [ ] `grep -rn 'name == "Bytes"' src/codegen src/type_system src/parser --include='*.rs'` produces no propertyless constructor decision/lowering occurrences outside the registry or intentional Bytes runtime/member handling.
./.sisyphus/plans/cleaner-propertyless-constructors.md:  **What to do**: Update only implementation-adjacent comments and existing Bytes/prelude documentation that mention `new Bytes` as a special parser exception. Keep public docs accurate: `new Bytes` is the preferred construction form and `bytes_new()` remains legacy compatibility. If `stdlib/prelude.op` already documents this correctly, leave it unchanged. Do not update broad language spec pages unless they currently state the old parser-special-case behavior. Add a brief code comment near the registry explaining how future opaque handles (e.g., StringBuilder) can be registered later without parser changes, but explicitly note that only `Bytes` is registered now.
./.sisyphus/plans/cleaner-propertyless-constructors.md:  - Parser comment: `src/parser/new_expression.rs` - Remove/replace comments that call out `Bytes` parser exception.
./.sisyphus/plans/cleaner-propertyless-constructors.md:  - [ ] Search for stale wording like `parser.*Bytes`, `Bytes.*parser`, or `special.*Bytes` in `src`, `stdlib`, and `language-spec`; no stale claim remains that `new Bytes` is parser-special-cased.
./.sisyphus/plans/cleaner-propertyless-constructors.md:  **What to do**: Perform a final audit of hard-coded `"Bytes"` in non-test Rust sources and document remaining occurrences in `.sisyphus/evidence/task-8-final-bytes-audit.txt`. Acceptable remaining categories: Bytes runtime implementation, Bytes builtin/type registration, standard symbol definitions, Bytes member/method lowering that is not propertyless constructor decision logic, test-only strings. Unacceptable remaining categories: parser accepting/rejecting bare constructor by checking `Bytes`; type checker propertyless acceptance by checking `Bytes`; codegen constructor lowering by checking `Bytes` directly instead of registry. Run the targeted test commands from Tasks 1-7 and then `cargo test --all`. Fix any failures before final verification.
./.sisyphus/plans/cleaner-propertyless-constructors.md:    Expected: No unclassified source hits; no parser/typechecker/codegen propertyless decision logic uses direct `Bytes` checks.
./.sisyphus/plans/cleaner-propertyless-constructors.md:  - Finding 2: parser no longer contains a `Bytes`-specific propertyless constructor exception.
./.sisyphus/plans/bytes-new-to-new-bytes.md:- Chose enforcement layer: parser accepts propertyless `new <Ident>` as an AST shape, typechecker restricts this plan to `Bytes` only and emits diagnostics for other propertyless constructors.
./.sisyphus/plans/bytes-new-to-new-bytes.md:- Before implementation, `set -o pipefail; cargo test --features integration empty_bytes_via_new_bytes 2>&1 | tee .sisyphus/evidence/red-new-bytes.txt` fails and captures parser/typechecker failure text mentioning `new Bytes` or constructor syntax.
./.sisyphus/plans/bytes-new-to-new-bytes.md:  **Must NOT do**: Do not touch parser/typechecker/codegen for `new Bytes`. Do not update docs in this commit.
./.sisyphus/plans/bytes-new-to-new-bytes.md:- [x] 4. Implement parser and formatter support for Bytes-only propertyless constructor syntax
./.sisyphus/plans/bytes-new-to-new-bytes.md:  **What to do**: Update parser handling so `new Bytes` can be parsed as a constructor expression with zero fields while preserving existing `new Type:` and `new Type.Variant:` parsing. Add parser tests for `let buffer: Bytes = new Bytes`, `let buffer = new Bytes`, and malformed `new Bytes()` / `new Bytes:` cases. Update formatter/printer tests so formatting preserves `new Bytes` and does not rewrite `bytes_new()` calls. If parser architecture requires parser-general acceptance of `new <Ident>`, ensure typechecker negative tests in Task 5 reject non-Bytes.
./.sisyphus/plans/bytes-new-to-new-bytes.md:  **What to do**: Integrate Tasks 3-6 into one green feature state. Run targeted parser/typechecker/codegen/formatter tests, the new syntax integration test, the legacy integration test, `cargo test`, and `cargo test --features integration`. Refactor only within touched parser/typechecker/codegen/formatter/test code to remove duplication and clarify Bytes-only constructor handling. Commit all implementation and new-syntax test project changes.
./.sisyphus/plans/bytes-new-to-new-bytes.md:  **Commit**: YES | Message: `feat(bytes): support new Bytes initialization` | Files: [test-projects/bytes-empty-construct-new-syntax/**, tests/integration_e2e/bytes_stdlib.rs, src/parser/**, src/type_system/**, src/codegen/**, tests/fmt_integration.rs or formatter fixtures, .sisyphus/evidence/red-new-bytes.txt if project convention allows evidence commits]
