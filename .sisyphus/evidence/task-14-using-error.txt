Task 14 using cleanup failure-path evidence - retry 3 - 2026-08-21

Top-level retry-3 rejection fixed:

1. Guard success paths now consume cleanup authority without globally consuming failure paths
   - Added runtime consumed-flag support to codegen using cleanup obligations.
   - `codegen_guard_statement` and `codegen_guard_expression` prepare the flag before branching and store `true` only in the success block.
   - Lexical cleanup checks the flag at scope exit and skips the cleanup scaffold only when the close succeeded at runtime.
   - Regression tests assert `using.cleanup.consumed`, `using.cleanup.skip`, and `using.cleanup.run` IR blocks plus exactly one lexical cleanup scaffold.

2. Failed explicit close retains lexical cleanup unless exact transfer result occurs
   - Guard failure/fallback paths leave the runtime consumed flag false, so the later lexical cleanup remains active.
   - Retry-2 propagate transfer handling is preserved: only the exact `terminal_session_close_sync` / `TerminalSessionRestoreError` / `CloseRestorePending` result skips lexical cleanup for the transferred binding.
   - Non-transfer failures still run lexical cleanup, and successful propagate still avoids duplicate cleanup.

3. Checker cleanup-transfer no-op removed
   - Removed `let _has_cleanup_transfer = self.call_has_registered_cleanup_authority_transfer(call);` from `type_check_propagate_expr`.
   - The checker no longer computes and discards cleanup-transfer recognition just to satisfy a search.
   - Existing checker tests still verify the exact transfer helper directly; production behavior is enforced by codegen's transfer branch and success-path cleanup state.

4. Cleanup-aware exits preserved
   - Fallthrough cleanup now respects optional runtime consumed flags.
   - Return, break, continue, propagation, and guard-controlled exits continue through scope cleanup helpers.
   - Fallible cleanup scaffold results are still observed via `using.cleanup.primary`, `using.cleanup.failed`, and `using.cleanup.primary.select`.

Preserved behavior:
- Acquisition/body/cleanup error union checking still passes affine using tests.
- Reverse acquisition cleanup order still passes.
- Fallthrough/return/break/continue/propagation cleanup scaffold tests still pass.
- Successful explicit close still avoids duplicate lexical cleanup.
- Exact transfer behavior remains variant-gated by `CloseRestorePending` comparison.

Verification evidence:
- LSP diagnostics: no errors on all edited Rust files.
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo fmt --all -- --check`: passed.
- `bash scripts/check-line-count.sh`: passed, 309 Rust files within limits.
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo test using`: 16 passed, 0 failed.
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo test affine`: 14 passed, 0 failed.
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo test terminal_proposal`: 23 passed, 0 failed.
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo test`: 1442 passed, 0 failed, 8 ignored; doc-tests 2 passed, 12 ignored.
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo check`: passed.
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo build`: passed.
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo make lint`: passed.
