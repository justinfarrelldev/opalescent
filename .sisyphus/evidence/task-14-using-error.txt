Task 14 using cleanup failure-path evidence - retry 4 - 2026-08-21

Top-level retry-4 rejection fixed:

1. Guard failure/fallback paths now honor exact cleanup-authority transfer
   - Statement and expression guard lowering now split guarded close failures with an exact runtime `strcmp` against `CloseRestorePending`.
   - If the error is `TerminalSessionRestoreError.CloseRestorePending`, the runtime cleanup flag is set and later lexical cleanup skips only that transferred obligation.
   - If the close fails for any ordinary/non-transfer reason, the flag remains false and lexical cleanup still runs.

2. Guard success behavior remains path-sensitive
   - Guard success still stores `true` into the same runtime cleanup flag.
   - No guard path statically consumes the `CodegenEnv` obligation, so recovered fallback paths cannot accidentally suppress cleanup globally.

3. Propagate transfer behavior remains preserved
   - Retry-2 success-only propagate consumption still uses the registered exact tuple: `terminal_session_close_sync` / `TerminalSessionRestoreError` / `CloseRestorePending`.
   - Non-transfer propagate failures retain lexical cleanup and successful propagate avoids duplicate cleanup.

4. Helper extraction kept strict hooks green
   - Statement-side transfer/success helpers live in `src/codegen/statements/using_cleanup.rs`, keeping `src/codegen/statements.rs` below the 1250-line cap.
   - Strict lint shadowing and wildcard-import findings were fixed before final verification.

Verification evidence:
- LSP diagnostics: no errors on all edited Rust files.
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo fmt --all -- --check`: passed.
- `bash scripts/check-line-count.sh`: passed, 309 Rust files within limits.
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo test using`: 16 passed, 0 failed.
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo test affine`: 14 passed, 0 failed.
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo test terminal_proposal`: 23 passed, 0 failed.
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo check`: passed.
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo build`: passed.
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo make lint`: passed.
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo test`: 1442 passed, 0 failed, 8 ignored; doc-tests 2 passed, 12 ignored.
