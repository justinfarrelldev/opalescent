Task 14 using cleanup failure-path evidence - retry 2 - 2026-08-21

Top-level rejection fixed:

1. Failed explicit close no longer consumes before branching
   - Removed unconditional `consume_using_cleanup_obligation_after_success` from raw `codegen_call_expression`.
   - `codegen_propagate_expression` now calls consumption only in the success continuation after checking the fallible error field.
   - Regression: `using_cleanup_propagate_close_consumes_only_success_and_transfers_exact_error` would fail on the rejected implementation because lexical cleanup would be skipped on all close failures.

2. Failed explicit close retains cleanup unless exact transfer result occurs
   - Codegen records the exact transfer tuple on using obligations.
   - Propagate error path compares the returned close error against `CloseRestorePending` with `strcmp`.
   - Transfer branch skips lexical cleanup for the transferred binding; ordinary failure branch runs lexical cleanup.
   - Wrong operation, wrong binding, and wrong variant remain non-transfer cases in checker tests.

3. Cleanup failures are observed rather than ignored
   - Fallible `__opal_using_cleanup_terminal_session_close_sync` call results are extracted as `i8*` error pointers.
   - Cleanup helpers maintain a `using.cleanup.primary` slot and select the first non-null cleanup failure.
   - Cleanup-aware error returns forward cleanup failure as primary under the current single-pointer error ABI; if cleanup succeeds, the original body/propagate error remains primary.
   - Regression: `using_cleanup_error_result_is_observed_as_primary_candidate` asserts the IR stores/tests/selects cleanup error pointers.

4. Transfer predicate is integrated into production checker flow
   - Added `call_has_registered_cleanup_authority_transfer` in `src/type_system/checker/using_cleanup.rs`.
   - `type_check_propagate_expr` invokes the production-facing inspector before success-path consumption.
   - Unit tests now verify exact call-shape transfer recognition for the real close operation and reject wrong operation/binding shapes.

Preserved behavior:
- Acquisition/body/cleanup error union checking still passes existing affine using tests.
- Reverse acquisition cleanup order still passes.
- Fallthrough/return/break/continue/propagation cleanup scaffold tests still pass.
- Successful explicit close still avoids duplicate lexical cleanup.

Verification evidence:
- LSP diagnostics: no errors on all edited Rust files.
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo fmt --all -- --check`: passed.
- `bash scripts/check-line-count.sh`: passed, 309 Rust files within limits.
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo test using`: 14 passed, 0 failed.
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo test affine`: 14 passed, 0 failed.
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo test terminal_proposal`: 23 passed, 0 failed.
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo test`: 1440 passed, 0 failed, 8 ignored; doc-tests 2 passed, 12 ignored.
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo check`: passed.
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo build`: passed.
- `LLVM_SYS_140_PREFIX=/usr/lib/llvm-14 cargo make lint`: passed.
