Task 4 evidence: immutable receiver rejection

Command:
  cargo test --features integration array_push_on_immutable_receiver_fails_at_compile_time -- --nocapture

Result:
  test passed; the compiler rejected immutable array receiver mutation at compile time

Key diagnostic expectations asserted by the test:
  stderr contains both `mutable` and `push`

Notes:
  - The negative fixture uses `let xs: int32[] = [1 as int32]` to avoid unrelated empty-array inference failures.
  - This verifies deterministic compile-time rejection for mutating array methods on immutable receivers.
