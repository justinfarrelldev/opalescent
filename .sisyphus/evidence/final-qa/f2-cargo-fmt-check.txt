Diff in /home/justi/Projects/opalescent/src/app/tests.rs:1:
[31m-    use super::{help_text, run_with_args};
(B[m 
[31m-    #[test]
(B[m[31m-    fn top_level_help_contains_all_commands() {
(B[m[31m-        let help = help_text(None);
(B[m[31m-        assert!(help.contains("<file.op>"));
(B[m[31m-        assert!(help.contains("--run"));
(B[m[31m-        assert!(help.contains("help"));
(B[m[31m-        assert!(help.contains("--help"));
(B[m[31m-        assert!(help.contains("pkg"));
(B[m[31m-        assert!(help.contains("fmt"));
(B[m[31m-        assert!(help.contains("lsp"));
(B[m[31m-        assert!(help.contains("test"));
(B[m[31m-        assert!(help.contains("doc"));
(B[m[31m-        assert!(help.contains("bench"));
(B[m[31m-    }
(B[m[32m+use super::{help_text, run_with_args};
(B[m 
[31m-    #[test]
(B[m[31m-    fn top_level_help_contains_examples_section() {
(B[m[31m-        let help = help_text(None);
(B[m[31m-        assert!(help.contains("Examples:"));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn top_level_help_contains_all_commands() {
(B[m[32m+    let help = help_text(None);
(B[m[32m+    assert!(help.contains("<file.op>"));
(B[m[32m+    assert!(help.contains("--run"));
(B[m[32m+    assert!(help.contains("help"));
(B[m[32m+    assert!(help.contains("--help"));
(B[m[32m+    assert!(help.contains("pkg"));
(B[m[32m+    assert!(help.contains("fmt"));
(B[m[32m+    assert!(help.contains("lsp"));
(B[m[32m+    assert!(help.contains("test"));
(B[m[32m+    assert!(help.contains("doc"));
(B[m[32m+    assert!(help.contains("bench"));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn help_pkg_shows_all_subcommands() {
(B[m[31m-        let help = help_text(Some("pkg"));
(B[m[31m-        assert!(help.contains("init"));
(B[m[31m-        assert!(help.contains("add"));
(B[m[31m-        assert!(help.contains("remove"));
(B[m[31m-        assert!(help.contains("install"));
(B[m[31m-        assert!(help.contains("publish"));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn top_level_help_contains_examples_section() {
(B[m[32m+    let help = help_text(None);
(B[m[32m+    assert!(help.contains("Examples:"));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn help_fmt_shows_all_flags() {
(B[m[31m-        let help = help_text(Some("fmt"));
(B[m[31m-        assert!(help.contains("--check"));
(B[m[31m-        assert!(help.contains("--config"));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn help_pkg_shows_all_subcommands() {
(B[m[32m+    let help = help_text(Some("pkg"));
(B[m[32m+    assert!(help.contains("init"));
(B[m[32m+    assert!(help.contains("add"));
(B[m[32m+    assert!(help.contains("remove"));
(B[m[32m+    assert!(help.contains("install"));
(B[m[32m+    assert!(help.contains("publish"));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn help_lsp_shows_stdio_flag() {
(B[m[31m-        let help = help_text(Some("lsp"));
(B[m[31m-        assert!(help.contains("--stdio"));
(B[m[31m-        assert!(!help.contains("Unknown help topic"));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn help_fmt_shows_all_flags() {
(B[m[32m+    let help = help_text(Some("fmt"));
(B[m[32m+    assert!(help.contains("--check"));
(B[m[32m+    assert!(help.contains("--config"));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn help_test_shows_flags() {
(B[m[31m-        let help = help_text(Some("test"));
(B[m[31m-        assert!(help.contains("--target"));
(B[m[31m-        assert!(help.contains("--filter"));
(B[m[31m-        assert!(!help.contains("Unknown help topic"));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn help_lsp_shows_stdio_flag() {
(B[m[32m+    let help = help_text(Some("lsp"));
(B[m[32m+    assert!(help.contains("--stdio"));
(B[m[32m+    assert!(!help.contains("Unknown help topic"));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn help_doc_shows_format_flag() {
(B[m[31m-        let help = help_text(Some("doc"));
(B[m[31m-        assert!(help.contains("--format"));
(B[m[31m-        assert!(help.contains("md"));
(B[m[31m-        assert!(help.contains("html"));
(B[m[31m-        assert!(!help.contains("Unknown help topic"));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn help_test_shows_flags() {
(B[m[32m+    let help = help_text(Some("test"));
(B[m[32m+    assert!(help.contains("--target"));
(B[m[32m+    assert!(help.contains("--filter"));
(B[m[32m+    assert!(!help.contains("Unknown help topic"));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn help_bench_shows_usage() {
(B[m[31m-        let help = help_text(Some("bench"));
(B[m[31m-        assert!(!help.is_empty());
(B[m[31m-        assert!(!help.contains("Unknown help topic"));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn help_doc_shows_format_flag() {
(B[m[32m+    let help = help_text(Some("doc"));
(B[m[32m+    assert!(help.contains("--format"));
(B[m[32m+    assert!(help.contains("md"));
(B[m[32m+    assert!(help.contains("html"));
(B[m[32m+    assert!(!help.contains("Unknown help topic"));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn help_unknown_topic_contains_error() {
(B[m[31m-        let help = help_text(Some("nonexistent"));
(B[m[31m-        assert!(help.contains("Unknown help topic"));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn help_bench_shows_usage() {
(B[m[32m+    let help = help_text(Some("bench"));
(B[m[32m+    assert!(!help.is_empty());
(B[m[32m+    assert!(!help.contains("Unknown help topic"));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn dash_dash_help_shows_top_level_help() {
(B[m[31m-        let args = ["opal".to_string(), "--help".to_string()];
(B[m[31m-        assert_eq!(run_with_args(&args), Ok(()));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn help_unknown_topic_contains_error() {
(B[m[32m+    let help = help_text(Some("nonexistent"));
(B[m[32m+    assert!(help.contains("Unknown help topic"));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn dash_dash_help_with_topic_shows_topic() {
(B[m[31m-        let args = ["opal".to_string(), "--help".to_string(), "pkg".to_string()];
(B[m[31m-        assert_eq!(run_with_args(&args), Ok(()));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn dash_dash_help_shows_top_level_help() {
(B[m[32m+    let args = ["opal".to_string(), "--help".to_string()];
(B[m[32m+    assert_eq!(run_with_args(&args), Ok(()));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn unimplemented_pkg_returns_error() {
(B[m[31m-        let args = ["opal".to_string(), "pkg".to_string()];
(B[m[31m-        assert_eq!(run_with_args(&args), Err(1));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn dash_dash_help_with_topic_shows_topic() {
(B[m[32m+    let args = ["opal".to_string(), "--help".to_string(), "pkg".to_string()];
(B[m[32m+    assert_eq!(run_with_args(&args), Ok(()));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn fmt_missing_file_returns_error() {
(B[m[31m-        let args = ["opal".to_string(), "fmt".to_string()];
(B[m[31m-        assert_eq!(run_with_args(&args), Err(1));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn unimplemented_pkg_returns_error() {
(B[m[32m+    let args = ["opal".to_string(), "pkg".to_string()];
(B[m[32m+    assert_eq!(run_with_args(&args), Err(1));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn fmt_nonexistent_file_returns_error() {
(B[m[31m-        let args = [
(B[m[31m-            "opal".to_string(),
(B[m[31m-            "fmt".to_string(),
(B[m[31m-            "nonexistent_xyz_abc_123.op".to_string(),
(B[m[31m-        ];
(B[m[31m-        assert_eq!(run_with_args(&args), Err(1));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn fmt_missing_file_returns_error() {
(B[m[32m+    let args = ["opal".to_string(), "fmt".to_string()];
(B[m[32m+    assert_eq!(run_with_args(&args), Err(1));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn fmt_check_mode_returns_ok_when_already_formatted() {
(B[m[31m-        let tmp_path = std::env::temp_dir().join("opal_test_fmt_check.op");
(B[m[31m-        std::fs::write(&tmp_path, "let x = 1\n").unwrap();
(B[m[31m-        let path = tmp_path.to_string_lossy().to_string();
(B[m[31m-        let args = [
(B[m[31m-            "opal".to_string(),
(B[m[31m-            "fmt".to_string(),
(B[m[31m-            "--check".to_string(),
(B[m[31m-            path,
(B[m[31m-        ];
(B[m[31m-        let result = run_with_args(&args);
(B[m[31m-        drop(std::fs::remove_file(&tmp_path));
(B[m[31m-        assert!(result == Ok(()) || result == Err(1));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn fmt_nonexistent_file_returns_error() {
(B[m[32m+    let args = [
(B[m[32m+        "opal".to_string(),
(B[m[32m+        "fmt".to_string(),
(B[m[32m+        "nonexistent_xyz_abc_123.op".to_string(),
(B[m[32m+    ];
(B[m[32m+    assert_eq!(run_with_args(&args), Err(1));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn fmt_formats_file_in_place() {
(B[m[31m-        let tmp_path = std::env::temp_dir().join("opal_test_fmt_inplace.op");
(B[m[31m-        std::fs::write(&tmp_path, "let x = 1\n").unwrap();
(B[m[31m-        let path = tmp_path.to_string_lossy().to_string();
(B[m[31m-        let args = ["opal".to_string(), "fmt".to_string(), path];
(B[m[31m-        let result = run_with_args(&args);
(B[m[31m-        drop(std::fs::remove_file(&tmp_path));
(B[m[31m-        assert!(result == Ok(()) || result == Err(1));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn fmt_check_mode_returns_ok_when_already_formatted() {
(B[m[32m+    let tmp_path = std::env::temp_dir().join("opal_test_fmt_check.op");
(B[m[32m+    std::fs::write(&tmp_path, "let x = 1\n").unwrap();
(B[m[32m+    let path = tmp_path.to_string_lossy().to_string();
(B[m[32m+    let args = [
(B[m[32m+        "opal".to_string(),
(B[m[32m+        "fmt".to_string(),
(B[m[32m+        "--check".to_string(),
(B[m[32m+        path,
(B[m[32m+    ];
(B[m[32m+    let result = run_with_args(&args);
(B[m[32m+    drop(std::fs::remove_file(&tmp_path));
(B[m[32m+    assert!(result == Ok(()) || result == Err(1));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn fmt_config_flag_accepted() {
(B[m[31m-        let tmp_src = std::env::temp_dir().join("opal_test_fmt_cfg_src.op");
(B[m[31m-        let tmp_cfg = std::env::temp_dir().join("opal_test_fmt_cfg.toml");
(B[m[31m-        std::fs::write(&tmp_src, "let x = 1\n").unwrap();
(B[m[31m-        std::fs::write(&tmp_cfg, "indent_size = 4\n").unwrap();
(B[m[31m-        let src_path = tmp_src.to_string_lossy().to_string();
(B[m[31m-        let cfg_path = tmp_cfg.to_string_lossy().to_string();
(B[m[31m-        let args = [
(B[m[31m-            "opal".to_string(),
(B[m[31m-            "fmt".to_string(),
(B[m[31m-            "--config".to_string(),
(B[m[31m-            cfg_path,
(B[m[31m-            src_path,
(B[m[31m-        ];
(B[m[31m-        let result = run_with_args(&args);
(B[m[31m-        drop(std::fs::remove_file(&tmp_src));
(B[m[31m-        drop(std::fs::remove_file(&tmp_cfg));
(B[m[31m-        assert!(result == Ok(()) || result == Err(1));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn fmt_formats_file_in_place() {
(B[m[32m+    let tmp_path = std::env::temp_dir().join("opal_test_fmt_inplace.op");
(B[m[32m+    std::fs::write(&tmp_path, "let x = 1\n").unwrap();
(B[m[32m+    let path = tmp_path.to_string_lossy().to_string();
(B[m[32m+    let args = ["opal".to_string(), "fmt".to_string(), path];
(B[m[32m+    let result = run_with_args(&args);
(B[m[32m+    drop(std::fs::remove_file(&tmp_path));
(B[m[32m+    assert!(result == Ok(()) || result == Err(1));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn unimplemented_lsp_returns_error() {
(B[m[31m-        let args = ["opal".to_string(), "lsp".to_string()];
(B[m[31m-        assert_eq!(run_with_args(&args), Err(1));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn fmt_config_flag_accepted() {
(B[m[32m+    let tmp_src = std::env::temp_dir().join("opal_test_fmt_cfg_src.op");
(B[m[32m+    let tmp_cfg = std::env::temp_dir().join("opal_test_fmt_cfg.toml");
(B[m[32m+    std::fs::write(&tmp_src, "let x = 1\n").unwrap();
(B[m[32m+    std::fs::write(&tmp_cfg, "indent_size = 4\n").unwrap();
(B[m[32m+    let src_path = tmp_src.to_string_lossy().to_string();
(B[m[32m+    let cfg_path = tmp_cfg.to_string_lossy().to_string();
(B[m[32m+    let args = [
(B[m[32m+        "opal".to_string(),
(B[m[32m+        "fmt".to_string(),
(B[m[32m+        "--config".to_string(),
(B[m[32m+        cfg_path,
(B[m[32m+        src_path,
(B[m[32m+    ];
(B[m[32m+    let result = run_with_args(&args);
(B[m[32m+    drop(std::fs::remove_file(&tmp_src));
(B[m[32m+    drop(std::fs::remove_file(&tmp_cfg));
(B[m[32m+    assert!(result == Ok(()) || result == Err(1));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn lsp_starts_server_returns_ok() {
(B[m[31m-        let args = ["opal".to_string(), "lsp".to_string(), "--stdio".to_string()];
(B[m[31m-        assert_eq!(run_with_args(&args), Ok(()));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn unimplemented_lsp_returns_error() {
(B[m[32m+    let args = ["opal".to_string(), "lsp".to_string()];
(B[m[32m+    assert_eq!(run_with_args(&args), Err(1));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn test_command_empty_suite_returns_ok() {
(B[m[31m-        let args = ["opal".to_string(), "test".to_string()];
(B[m[31m-        assert_eq!(run_with_args(&args), Ok(()));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn lsp_starts_server_returns_ok() {
(B[m[32m+    let args = ["opal".to_string(), "lsp".to_string(), "--stdio".to_string()];
(B[m[32m+    assert_eq!(run_with_args(&args), Ok(()));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn test_with_filter_returns_ok() {
(B[m[31m-        let args = [
(B[m[31m-            "opal".to_string(),
(B[m[31m-            "test".to_string(),
(B[m[31m-            "--filter".to_string(),
(B[m[31m-            "my_test".to_string(),
(B[m[31m-        ];
(B[m[31m-        assert_eq!(run_with_args(&args), Ok(()));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn test_command_empty_suite_returns_ok() {
(B[m[32m+    let args = ["opal".to_string(), "test".to_string()];
(B[m[32m+    assert_eq!(run_with_args(&args), Ok(()));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn test_with_target_returns_ok() {
(B[m[31m-        let args = [
(B[m[31m-            "opal".to_string(),
(B[m[31m-            "test".to_string(),
(B[m[31m-            "--target".to_string(),
(B[m[31m-            "x86_64-linux".to_string(),
(B[m[31m-        ];
(B[m[31m-        assert_eq!(run_with_args(&args), Ok(()));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn test_with_filter_returns_ok() {
(B[m[32m+    let args = [
(B[m[32m+        "opal".to_string(),
(B[m[32m+        "test".to_string(),
(B[m[32m+        "--filter".to_string(),
(B[m[32m+        "my_test".to_string(),
(B[m[32m+    ];
(B[m[32m+    assert_eq!(run_with_args(&args), Ok(()));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn unimplemented_doc_returns_error() {
(B[m[31m-        let args = ["opal".to_string(), "doc".to_string()];
(B[m[31m-        assert_eq!(run_with_args(&args), Err(1));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn test_with_target_returns_ok() {
(B[m[32m+    let args = [
(B[m[32m+        "opal".to_string(),
(B[m[32m+        "test".to_string(),
(B[m[32m+        "--target".to_string(),
(B[m[32m+        "x86_64-linux".to_string(),
(B[m[32m+    ];
(B[m[32m+    assert_eq!(run_with_args(&args), Ok(()));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn bench_command_returns_ok() {
(B[m[31m-        let args = ["opal".to_string(), "bench".to_string()];
(B[m[31m-        assert_eq!(run_with_args(&args), Ok(()));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn unimplemented_doc_returns_error() {
(B[m[32m+    let args = ["opal".to_string(), "doc".to_string()];
(B[m[32m+    assert_eq!(run_with_args(&args), Err(1));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn doc_missing_file_returns_error() {
(B[m[31m-        let args = ["opal".to_string(), "doc".to_string()];
(B[m[31m-        assert_eq!(run_with_args(&args), Err(1));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn bench_command_returns_ok() {
(B[m[32m+    let args = ["opal".to_string(), "bench".to_string()];
(B[m[32m+    assert_eq!(run_with_args(&args), Ok(()));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn doc_nonexistent_file_returns_error() {
(B[m[31m-        let args = [
(B[m[31m-            "opal".to_string(),
(B[m[31m-            "doc".to_string(),
(B[m[31m-            "nonexistent_xyz_doc_123.op".to_string(),
(B[m[31m-        ];
(B[m[31m-        assert_eq!(run_with_args(&args), Err(1));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn doc_missing_file_returns_error() {
(B[m[32m+    let args = ["opal".to_string(), "doc".to_string()];
(B[m[32m+    assert_eq!(run_with_args(&args), Err(1));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn doc_with_valid_source_returns_ok() {
(B[m[31m-        let tmp_path = std::env::temp_dir().join("opal_test_doc_valid.op");
(B[m[31m-        std::fs::write(&tmp_path, "let x = 1\n").unwrap();
(B[m[31m-        let path = tmp_path.to_string_lossy().to_string();
(B[m[31m-        let args = ["opal".to_string(), "doc".to_string(), path];
(B[m[31m-        let result = run_with_args(&args);
(B[m[31m-        drop(std::fs::remove_file(&tmp_path));
(B[m[31m-        assert_eq!(result, Ok(()));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn doc_nonexistent_file_returns_error() {
(B[m[32m+    let args = [
(B[m[32m+        "opal".to_string(),
(B[m[32m+        "doc".to_string(),
(B[m[32m+        "nonexistent_xyz_doc_123.op".to_string(),
(B[m[32m+    ];
(B[m[32m+    assert_eq!(run_with_args(&args), Err(1));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn doc_format_flag_accepted() {
(B[m[31m-        let tmp_path = std::env::temp_dir().join("opal_test_doc_fmt.op");
(B[m[31m-        std::fs::write(&tmp_path, "let x = 1\n").unwrap();
(B[m[31m-        let path = tmp_path.to_string_lossy().to_string();
(B[m[31m-        let args = [
(B[m[31m-            "opal".to_string(),
(B[m[31m-            "doc".to_string(),
(B[m[31m-            "--format".to_string(),
(B[m[31m-            "html".to_string(),
(B[m[31m-            path,
(B[m[31m-        ];
(B[m[31m-        let result = run_with_args(&args);
(B[m[31m-        drop(std::fs::remove_file(&tmp_path));
(B[m[31m-        assert!(result == Ok(()) || result == Err(1));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn doc_with_valid_source_returns_ok() {
(B[m[32m+    let tmp_path = std::env::temp_dir().join("opal_test_doc_valid.op");
(B[m[32m+    std::fs::write(&tmp_path, "let x = 1\n").unwrap();
(B[m[32m+    let path = tmp_path.to_string_lossy().to_string();
(B[m[32m+    let args = ["opal".to_string(), "doc".to_string(), path];
(B[m[32m+    let result = run_with_args(&args);
(B[m[32m+    drop(std::fs::remove_file(&tmp_path));
(B[m[32m+    assert_eq!(result, Ok(()));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn help_command_returns_ok() {
(B[m[31m-        let args = ["opal".to_string(), "help".to_string()];
(B[m[31m-        assert_eq!(run_with_args(&args), Ok(()));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn doc_format_flag_accepted() {
(B[m[32m+    let tmp_path = std::env::temp_dir().join("opal_test_doc_fmt.op");
(B[m[32m+    std::fs::write(&tmp_path, "let x = 1\n").unwrap();
(B[m[32m+    let path = tmp_path.to_string_lossy().to_string();
(B[m[32m+    let args = [
(B[m[32m+        "opal".to_string(),
(B[m[32m+        "doc".to_string(),
(B[m[32m+        "--format".to_string(),
(B[m[32m+        "html".to_string(),
(B[m[32m+        path,
(B[m[32m+    ];
(B[m[32m+    let result = run_with_args(&args);
(B[m[32m+    drop(std::fs::remove_file(&tmp_path));
(B[m[32m+    assert!(result == Ok(()) || result == Err(1));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn help_with_topic_returns_ok() {
(B[m[31m-        let args = ["opal".to_string(), "help".to_string(), "pkg".to_string()];
(B[m[31m-        assert_eq!(run_with_args(&args), Ok(()));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn help_command_returns_ok() {
(B[m[32m+    let args = ["opal".to_string(), "help".to_string()];
(B[m[32m+    assert_eq!(run_with_args(&args), Ok(()));
(B[m[32m+}
(B[m 
[31m-    /// Ensures calling CLI without a source file returns error code 1.
(B[m[31m-    #[test]
(B[m[31m-    fn no_args_returns_error() {
(B[m[31m-        let args = ["opal".to_string()];
(B[m[31m-        assert_eq!(run_with_args(&args), Err(1));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn help_with_topic_returns_ok() {
(B[m[32m+    let args = ["opal".to_string(), "help".to_string(), "pkg".to_string()];
(B[m[32m+    assert_eq!(run_with_args(&args), Ok(()));
(B[m[32m+}
(B[m 
[31m-    /// Ensures missing source file input returns error code 1.
(B[m[31m-    #[test]
(B[m[31m-    fn missing_file_returns_error() {
(B[m[31m-        let args = ["opal".to_string(), "nonexistent_file.op".to_string()];
(B[m[31m-        assert_eq!(run_with_args(&args), Err(1));
(B[m[31m-    }
(B[m[32m+/// Ensures calling CLI without a source file returns error code 1.
(B[m[32m+#[test]
(B[m[32m+fn no_args_returns_error() {
(B[m[32m+    let args = ["opal".to_string()];
(B[m[32m+    assert_eq!(run_with_args(&args), Err(1));
(B[m[32m+}
(B[m 
[31m-    /// Ensures `opal run` with no file argument returns error code 1.
(B[m[31m-    #[test]
(B[m[31m-    fn run_subcommand_missing_file_returns_error() {
(B[m[31m-        let args = ["opal".to_string(), "run".to_string()];
(B[m[31m-        assert_eq!(run_with_args(&args), Err(1));
(B[m[31m-    }
(B[m[32m+/// Ensures missing source file input returns error code 1.
(B[m[32m+#[test]
(B[m[32m+fn missing_file_returns_error() {
(B[m[32m+    let args = ["opal".to_string(), "nonexistent_file.op".to_string()];
(B[m[32m+    assert_eq!(run_with_args(&args), Err(1));
(B[m[32m+}
(B[m 
[31m-    /// Ensures `opal run <nonexistent>` returns error code 1.
(B[m[31m-    #[test]
(B[m[31m-    fn run_subcommand_nonexistent_file_returns_error() {
(B[m[31m-        let args = [
(B[m[31m-            "opal".to_string(),
(B[m[31m-            "run".to_string(),
(B[m[31m-            "missing_xyz_run.op".to_string(),
(B[m[31m-        ];
(B[m[31m-        assert_eq!(run_with_args(&args), Err(1));
(B[m[31m-    }
(B[m[32m+/// Ensures `opal run` with no file argument returns error code 1.
(B[m[32m+#[test]
(B[m[32m+fn run_subcommand_missing_file_returns_error() {
(B[m[32m+    let args = ["opal".to_string(), "run".to_string()];
(B[m[32m+    assert_eq!(run_with_args(&args), Err(1));
(B[m[32m+}
(B[m 
[31m-    /// Ensures `opal check` with no file argument returns error code 1.
(B[m[31m-    #[test]
(B[m[31m-    fn check_missing_file_arg_returns_error() {
(B[m[31m-        let args = ["opal".to_string(), "check".to_string()];
(B[m[31m-        assert_eq!(run_with_args(&args), Err(1));
(B[m[31m-    }
(B[m[32m+/// Ensures `opal run <nonexistent>` returns error code 1.
(B[m[32m+#[test]
(B[m[32m+fn run_subcommand_nonexistent_file_returns_error() {
(B[m[32m+    let args = [
(B[m[32m+        "opal".to_string(),
(B[m[32m+        "run".to_string(),
(B[m[32m+        "missing_xyz_run.op".to_string(),
(B[m[32m+    ];
(B[m[32m+    assert_eq!(run_with_args(&args), Err(1));
(B[m[32m+}
(B[m 
[31m-    /// Ensures `opal check <nonexistent>` returns error code 1.
(B[m[31m-    #[test]
(B[m[31m-    fn check_nonexistent_file_returns_error() {
(B[m[31m-        let args = [
(B[m[31m-            "opal".to_string(),
(B[m[31m-            "check".to_string(),
(B[m[31m-            "nonexistent_xyz_check.op".to_string(),
(B[m[31m-        ];
(B[m[31m-        assert_eq!(run_with_args(&args), Err(1));
(B[m[31m-    }
(B[m[32m+/// Ensures `opal check` with no file argument returns error code 1.
(B[m[32m+#[test]
(B[m[32m+fn check_missing_file_arg_returns_error() {
(B[m[32m+    let args = ["opal".to_string(), "check".to_string()];
(B[m[32m+    assert_eq!(run_with_args(&args), Err(1));
(B[m[32m+}
(B[m 
[31m-    /// Ensures `opal check <valid-file>` returns `Ok(())` for valid source.
(B[m[31m-    #[test]
(B[m[31m-    fn check_valid_source_returns_ok() {
(B[m[31m-        let source = "##\n  Description: starting point of the application\n##\nentry main = f(args: string[]): void =>\n    return void\n";
(B[m[31m-        let tmp_path = std::env::temp_dir().join("opal_test_check_valid.op");
(B[m[31m-        std::fs::write(&tmp_path, source).unwrap();
(B[m[31m-        let path = tmp_path.to_string_lossy().to_string();
(B[m[31m-        let args = ["opal".to_string(), "check".to_string(), path];
(B[m[31m-        let result = run_with_args(&args);
(B[m[31m-        drop(std::fs::remove_file(&tmp_path));
(B[m[31m-        assert_eq!(result, Ok(()));
(B[m[31m-    }
(B[m[32m+/// Ensures `opal check <nonexistent>` returns error code 1.
(B[m[32m+#[test]
(B[m[32m+fn check_nonexistent_file_returns_error() {
(B[m[32m+    let args = [
(B[m[32m+        "opal".to_string(),
(B[m[32m+        "check".to_string(),
(B[m[32m+        "nonexistent_xyz_check.op".to_string(),
(B[m[32m+    ];
(B[m[32m+    assert_eq!(run_with_args(&args), Err(1));
(B[m[32m+}
(B[m 
[31m-    /// Ensures `opal check <invalid-source>` returns error code 1 when type-checking fails.
(B[m[31m-    #[test]
(B[m[31m-    fn check_invalid_source_returns_error() {
(B[m[31m-        let source = "##\n  Description: starting point of the application\n##\nentry main = f(args: string[]): void =>\n    let x: int32 = \"not a number\"\n    return void\n";
(B[m[31m-        let tmp_path = std::env::temp_dir().join("opal_test_check_invalid.op");
(B[m[31m-        std::fs::write(&tmp_path, source).unwrap();
(B[m[31m-        let path = tmp_path.to_string_lossy().to_string();
(B[m[31m-        let args = ["opal".to_string(), "check".to_string(), path];
(B[m[31m-        let result = run_with_args(&args);
(B[m[31m-        drop(std::fs::remove_file(&tmp_path));
(B[m[31m-        assert_eq!(result, Err(1));
(B[m[31m-    }
(B[m[32m+/// Ensures `opal check <valid-file>` returns `Ok(())` for valid source.
(B[m[32m+#[test]
(B[m[32m+fn check_valid_source_returns_ok() {
(B[m[32m+    let source = "##\n  Description: starting point of the application\n##\nentry main = f(args: string[]): void =>\n    return void\n";
(B[m[32m+    let tmp_path = std::env::temp_dir().join("opal_test_check_valid.op");
(B[m[32m+    std::fs::write(&tmp_path, source).unwrap();
(B[m[32m+    let path = tmp_path.to_string_lossy().to_string();
(B[m[32m+    let args = ["opal".to_string(), "check".to_string(), path];
(B[m[32m+    let result = run_with_args(&args);
(B[m[32m+    drop(std::fs::remove_file(&tmp_path));
(B[m[32m+    assert_eq!(result, Ok(()));
(B[m[32m+}
(B[m 
[31m-    /// Ensures `opal run <file> -- arg1 arg2` parses args after `--` gracefully.
(B[m[31m-    #[test]
(B[m[31m-    fn run_args_after_double_dash_separated() {
(B[m[31m-        let tmp_path = std::env::temp_dir().join("opal_test_run_dashash.op");
(B[m[31m-        std::fs::write(&tmp_path, "let x = 1\n").unwrap();
(B[m[31m-        let path = tmp_path.to_string_lossy().to_string();
(B[m[31m-        let args = [
(B[m[31m-            "opal".to_string(),
(B[m[31m-            "run".to_string(),
(B[m[31m-            path,
(B[m[31m-            "--".to_string(),
(B[m[31m-            "arg1".to_string(),
(B[m[31m-            "arg2".to_string(),
(B[m[31m-        ];
(B[m[31m-        let result = run_with_args(&args);
(B[m[31m-        drop(std::fs::remove_file(&tmp_path));
(B[m[31m-        assert!(result == Ok(()) || result == Err(1));
(B[m[31m-    }
(B[m[32m+/// Ensures `opal check <invalid-source>` returns error code 1 when type-checking fails.
(B[m[32m+#[test]
(B[m[32m+fn check_invalid_source_returns_error() {
(B[m[32m+    let source = "##\n  Description: starting point of the application\n##\nentry main = f(args: string[]): void =>\n    let x: int32 = \"not a number\"\n    return void\n";
(B[m[32m+    let tmp_path = std::env::temp_dir().join("opal_test_check_invalid.op");
(B[m[32m+    std::fs::write(&tmp_path, source).unwrap();
(B[m[32m+    let path = tmp_path.to_string_lossy().to_string();
(B[m[32m+    let args = ["opal".to_string(), "check".to_string(), path];
(B[m[32m+    let result = run_with_args(&args);
(B[m[32m+    drop(std::fs::remove_file(&tmp_path));
(B[m[32m+    assert_eq!(result, Err(1));
(B[m[32m+}
(B[m 
[31m-    /// Mutex to serialize tests that change the process working directory.
(B[m[31m-    static CWD_MUTEX: std::sync::Mutex<()> = std::sync::Mutex::new(());
(B[m[32m+/// Ensures `opal run <file> -- arg1 arg2` parses args after `--` gracefully.
(B[m[32m+#[test]
(B[m[32m+fn run_args_after_double_dash_separated() {
(B[m[32m+    let tmp_path = std::env::temp_dir().join("opal_test_run_dashash.op");
(B[m[32m+    std::fs::write(&tmp_path, "let x = 1\n").unwrap();
(B[m[32m+    let path = tmp_path.to_string_lossy().to_string();
(B[m[32m+    let args = [
(B[m[32m+        "opal".to_string(),
(B[m[32m+        "run".to_string(),
(B[m[32m+        path,
(B[m[32m+        "--".to_string(),
(B[m[32m+        "arg1".to_string(),
(B[m[32m+        "arg2".to_string(),
(B[m[32m+    ];
(B[m[32m+    let result = run_with_args(&args);
(B[m[32m+    drop(std::fs::remove_file(&tmp_path));
(B[m[32m+    assert!(result == Ok(()) || result == Err(1));
(B[m[32m+}
(B[m 
[31m-    /// Ensures `opal build` returns `Err(1)` when no `opal.toml` exists in the current directory.
(B[m[31m-    #[test]
(B[m[31m-    fn build_no_config_returns_error() {
(B[m[31m-        let _guard = CWD_MUTEX
(B[m[31m-            .lock()
(B[m[31m-            .unwrap_or_else(std::sync::PoisonError::into_inner);
(B[m[31m-        let original = std::env::current_dir().unwrap();
(B[m[31m-        let dir = std::env::temp_dir().join("opal_test_build_no_config");
(B[m[31m-        std::fs::create_dir_all(&dir).unwrap();
(B[m[31m-        drop(std::fs::remove_file(dir.join("opal.toml")));
(B[m[31m-        std::env::set_current_dir(&dir).unwrap();
(B[m[31m-        let result = run_with_args(&["opal".to_string(), "build".to_string()]);
(B[m[31m-        std::env::set_current_dir(&original).unwrap();
(B[m[31m-        assert_eq!(result, Err(1));
(B[m[31m-    }
(B[m[32m+/// Mutex to serialize tests that change the process working directory.
(B[m[32m+static CWD_MUTEX: std::sync::Mutex<()> = std::sync::Mutex::new(());
(B[m 
[31m-    /// Ensures `opal build` dispatches the build path when `opal.toml` and `src/main.op` exist.
(B[m[31m-    #[test]
(B[m[31m-    fn build_with_config_compiles_project() {
(B[m[31m-        let _guard = CWD_MUTEX
(B[m[31m-            .lock()
(B[m[31m-            .unwrap_or_else(std::sync::PoisonError::into_inner);
(B[m[31m-        let original = std::env::current_dir().unwrap();
(B[m[31m-        let dir = std::env::temp_dir().join("opal_test_build_with_config");
(B[m[31m-        std::fs::create_dir_all(dir.join("src")).unwrap();
(B[m[31m-        std::fs::write(
(B[m[31m-            dir.join("opal.toml"),
(B[m[31m-            "name = \"test\"\nversion = \"0.1.0\"\n",
(B[m[31m-        )
(B[m[31m-        .unwrap();
(B[m[31m-        std::fs::write(dir.join("src").join("main.op"), "let x = 1\n").unwrap();
(B[m[31m-        std::env::set_current_dir(&dir).unwrap();
(B[m[31m-        let result = run_with_args(&["opal".to_string(), "build".to_string()]);
(B[m[31m-        std::env::set_current_dir(&original).unwrap();
(B[m[31m-        assert!(result == Ok(()) || result == Err(1));
(B[m[31m-    }
(B[m[32m+/// Ensures `opal build` returns `Err(1)` when no `opal.toml` exists in the current directory.
(B[m[32m+#[test]
(B[m[32m+fn build_no_config_returns_error() {
(B[m[32m+    let _guard = CWD_MUTEX
(B[m[32m+        .lock()
(B[m[32m+        .unwrap_or_else(std::sync::PoisonError::into_inner);
(B[m[32m+    let original = std::env::current_dir().unwrap();
(B[m[32m+    let dir = std::env::temp_dir().join("opal_test_build_no_config");
(B[m[32m+    std::fs::create_dir_all(&dir).unwrap();
(B[m[32m+    drop(std::fs::remove_file(dir.join("opal.toml")));
(B[m[32m+    std::env::set_current_dir(&dir).unwrap();
(B[m[32m+    let result = run_with_args(&["opal".to_string(), "build".to_string()]);
(B[m[32m+    std::env::set_current_dir(&original).unwrap();
(B[m[32m+    assert_eq!(result, Err(1));
(B[m[32m+}
(B[m 
[31m-    /// Ensures `opal watch` with no file arg returns `Err(1)`.
(B[m[31m-    #[test]
(B[m[31m-    fn watch_missing_file_arg_returns_error() {
(B[m[31m-        let args = ["opal".to_string(), "watch".to_string()];
(B[m[31m-        assert_eq!(run_with_args(&args), Err(1));
(B[m[31m-    }
(B[m[32m+/// Ensures `opal build` dispatches the build path when `opal.toml` and `src/main.op` exist.
(B[m[32m+#[test]
(B[m[32m+fn build_with_config_compiles_project() {
(B[m[32m+    let _guard = CWD_MUTEX
(B[m[32m+        .lock()
(B[m[32m+        .unwrap_or_else(std::sync::PoisonError::into_inner);
(B[m[32m+    let original = std::env::current_dir().unwrap();
(B[m[32m+    let dir = std::env::temp_dir().join("opal_test_build_with_config");
(B[m[32m+    std::fs::create_dir_all(dir.join("src")).unwrap();
(B[m[32m+    std::fs::write(
(B[m[32m+        dir.join("opal.toml"),
(B[m[32m+        "name = \"test\"\nversion = \"0.1.0\"\n",
(B[m[32m+    )
(B[m[32m+    .unwrap();
(B[m[32m+    std::fs::write(dir.join("src").join("main.op"), "let x = 1\n").unwrap();
(B[m[32m+    std::env::set_current_dir(&dir).unwrap();
(B[m[32m+    let result = run_with_args(&["opal".to_string(), "build".to_string()]);
(B[m[32m+    std::env::set_current_dir(&original).unwrap();
(B[m[32m+    assert!(result == Ok(()) || result == Err(1));
(B[m[32m+}
(B[m 
[31m-    /// Ensures `opal watch <nonexistent>` returns `Err(1)`.
(B[m[31m-    #[test]
(B[m[31m-    fn watch_nonexistent_file_returns_error() {
(B[m[31m-        let args = [
(B[m[31m-            "opal".to_string(),
(B[m[31m-            "watch".to_string(),
(B[m[31m-            "nonexistent_xyz.op".to_string(),
(B[m[31m-        ];
(B[m[31m-        assert_eq!(run_with_args(&args), Err(1));
(B[m[31m-    }
(B[m[32m+/// Ensures `opal watch` with no file arg returns `Err(1)`.
(B[m[32m+#[test]
(B[m[32m+fn watch_missing_file_arg_returns_error() {
(B[m[32m+    let args = ["opal".to_string(), "watch".to_string()];
(B[m[32m+    assert_eq!(run_with_args(&args), Err(1));
(B[m[32m+}
(B[m 
[31m-    /// Verifies `help_text` run command contains expected keywords.
(B[m[31m-    #[test]
(B[m[31m-    fn help_text_run_contains_usage() {
(B[m[31m-        let help = help_text(Some("run"));
(B[m[31m-        assert!(help.contains("opal run") && help.contains("file") && help.contains("args"));
(B[m[31m-    }
(B[m[32m+/// Ensures `opal watch <nonexistent>` returns `Err(1)`.
(B[m[32m+#[test]
(B[m[32m+fn watch_nonexistent_file_returns_error() {
(B[m[32m+    let args = [
(B[m[32m+        "opal".to_string(),
(B[m[32m+        "watch".to_string(),
(B[m[32m+        "nonexistent_xyz.op".to_string(),
(B[m[32m+    ];
(B[m[32m+    assert_eq!(run_with_args(&args), Err(1));
(B[m[32m+}
(B[m 
[31m-    /// Verifies `help_text` check command contains expected keywords.
(B[m[31m-    #[test]
(B[m[31m-    fn help_text_check_contains_usage() {
(B[m[31m-        let help = help_text(Some("check"));
(B[m[31m-        assert!(help.contains("opal check") && help.contains("typecheck"));
(B[m[31m-    }
(B[m[32m+/// Verifies `help_text` run command contains expected keywords.
(B[m[32m+#[test]
(B[m[32m+fn help_text_run_contains_usage() {
(B[m[32m+    let help = help_text(Some("run"));
(B[m[32m+    assert!(help.contains("opal run") && help.contains("file") && help.contains("args"));
(B[m[32m+}
(B[m 
[31m-    /// Verifies `help_text` build command contains expected keywords.
(B[m[31m-    #[test]
(B[m[31m-    fn help_text_build_contains_usage() {
(B[m[31m-        let help = help_text(Some("build"));
(B[m[31m-        assert!(help.contains("opal build") && help.contains("opal.toml"));
(B[m[31m-    }
(B[m[32m+/// Verifies `help_text` check command contains expected keywords.
(B[m[32m+#[test]
(B[m[32m+fn help_text_check_contains_usage() {
(B[m[32m+    let help = help_text(Some("check"));
(B[m[32m+    assert!(help.contains("opal check") && help.contains("typecheck"));
(B[m[32m+}
(B[m 
[31m-    /// Verifies `help_text` watch command contains expected keywords.
(B[m[31m-    #[test]
(B[m[31m-    fn help_text_watch_contains_usage() {
(B[m[31m-        let help = help_text(Some("watch"));
(B[m[31m-        assert!(help.contains("opal watch") && help.contains("recompile"));
(B[m[31m-    }
(B[m[32m+/// Verifies `help_text` build command contains expected keywords.
(B[m[32m+#[test]
(B[m[32m+fn help_text_build_contains_usage() {
(B[m[32m+    let help = help_text(Some("build"));
(B[m[32m+    assert!(help.contains("opal build") && help.contains("opal.toml"));
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn help_text_none_lists_all_commands() {
(B[m[31m-        let help = help_text(None);
(B[m[31m-        assert!(
(B[m[31m-            help.contains("run")
(B[m[31m-                && help.contains("check")
(B[m[31m-                && help.contains("build")
(B[m[31m-                && help.contains("watch")
(B[m[31m-        );
(B[m[31m-    }
(B[m[31m-    #[test]
(B[m[31m-    fn test_all_commands_no_unimplemented() {
(B[m[31m-        let commands: Vec<&str> = vec![
(B[m[31m-            "fmt", "lsp", "test", "doc", "bench", "run", "check", "build", "watch",
(B[m[31m-        ];
(B[m[31m-        for cmd in commands {
(B[m[31m-            let args = ["opal".to_owned(), cmd.to_owned()];
(B[m[31m-            let result = run_with_args(&args);
(B[m[31m-            match cmd {
(B[m[31m-                "test" | "bench" => {
(B[m[31m-                    assert_eq!(result, Ok(()), "{cmd} should be wired and executable");
(B[m[31m-                }
(B[m[31m-                _ => assert_eq!(
(B[m[31m-                    result,
(B[m[31m-                    Err(1),
(B[m[31m-                    "{cmd} should be wired and return argument/file errors, not unimplemented fallback"
(B[m[31m-                ),
(B[m[32m+/// Verifies `help_text` watch command contains expected keywords.
(B[m[32m+#[test]
(B[m[32m+fn help_text_watch_contains_usage() {
(B[m[32m+    let help = help_text(Some("watch"));
(B[m[32m+    assert!(help.contains("opal watch") && help.contains("recompile"));
(B[m[32m+}
(B[m[32m+
(B[m[32m+#[test]
(B[m[32m+fn help_text_none_lists_all_commands() {
(B[m[32m+    let help = help_text(None);
(B[m[32m+    assert!(
(B[m[32m+        help.contains("run")
(B[m[32m+            && help.contains("check")
(B[m[32m+            && help.contains("build")
(B[m[32m+            && help.contains("watch")
(B[m[32m+    );
(B[m[32m+}
(B[m[32m+#[test]
(B[m[32m+fn test_all_commands_no_unimplemented() {
(B[m[32m+    let commands: Vec<&str> = vec![
(B[m[32m+        "fmt", "lsp", "test", "doc", "bench", "run", "check", "build", "watch",
(B[m[32m+    ];
(B[m[32m+    for cmd in commands {
(B[m[32m+        let args = ["opal".to_owned(), cmd.to_owned()];
(B[m[32m+        let result = run_with_args(&args);
(B[m[32m+        match cmd {
(B[m[32m+            "test" | "bench" => {
(B[m[32m+                assert_eq!(result, Ok(()), "{cmd} should be wired and executable");
(B[m             }
[32m+            _ => assert_eq!(
(B[m[32m+                result,
(B[m[32m+                Err(1),
(B[m[32m+                "{cmd} should be wired and return argument/file errors, not unimplemented fallback"
(B[m[32m+            ),
(B[m         }
     }
[31m-    #[test]
(B[m[31m-    fn test_pkg_still_unimplemented() {
(B[m[31m-        let args = ["opal".to_owned(), "pkg".to_owned(), "status".to_owned()];
(B[m[31m-        assert_eq!(run_with_args(&args), Err(1));
(B[m[32m+}
(B[m[32m+#[test]
(B[m[32m+fn test_pkg_still_unimplemented() {
(B[m[32m+    let args = ["opal".to_owned(), "pkg".to_owned(), "status".to_owned()];
(B[m[32m+    assert_eq!(run_with_args(&args), Err(1));
(B[m[32m+}
(B[m[32m+#[test]
(B[m[32m+fn test_run_is_alternative_to_run_flag() {
(B[m[32m+    let subcommand_args = [
(B[m[32m+        "opal".to_owned(),
(B[m[32m+        "run".to_owned(),
(B[m[32m+        "nonexistent_alt_run.op".to_owned(),
(B[m[32m+    ];
(B[m[32m+    let flag_args = [
(B[m[32m+        "opal".to_owned(),
(B[m[32m+        "nonexistent_alt_run.op".to_owned(),
(B[m[32m+        "--run".to_owned(),
(B[m[32m+    ];
(B[m[32m+    assert_eq!(run_with_args(&subcommand_args), Err(1));
(B[m[32m+    assert_eq!(run_with_args(&flag_args), Err(1));
(B[m[32m+}
(B[m[32m+#[test]
(B[m[32m+fn test_help_lists_all_commands_integration() {
(B[m[32m+    let help = help_text(None);
(B[m[32m+    for cmd in [
(B[m[32m+        "pkg", "fmt", "lsp", "test", "doc", "bench", "run", "check", "build", "watch",
(B[m[32m+    ] {
(B[m[32m+        assert!(help.contains(cmd), "help text should list command: {cmd}");
(B[m     }
[31m-    #[test]
(B[m[31m-    fn test_run_is_alternative_to_run_flag() {
(B[m[31m-        let subcommand_args = [
(B[m[31m-            "opal".to_owned(),
(B[m[31m-            "run".to_owned(),
(B[m[31m-            "nonexistent_alt_run.op".to_owned(),
(B[m[31m-        ];
(B[m[31m-        let flag_args = [
(B[m[31m-            "opal".to_owned(),
(B[m[31m-            "nonexistent_alt_run.op".to_owned(),
(B[m[31m-            "--run".to_owned(),
(B[m[31m-        ];
(B[m[31m-        assert_eq!(run_with_args(&subcommand_args), Err(1));
(B[m[31m-        assert_eq!(run_with_args(&flag_args), Err(1));
(B[m[31m-    }
(B[m[31m-    #[test]
(B[m[31m-    fn test_help_lists_all_commands_integration() {
(B[m[31m-        let help = help_text(None);
(B[m[31m-        for cmd in [
(B[m[31m-            "pkg", "fmt", "lsp", "test", "doc", "bench", "run", "check", "build", "watch",
(B[m[31m-        ] {
(B[m[31m-            assert!(help.contains(cmd), "help text should list command: {cmd}");
(B[m[31m-        }
(B[m[31m-    }
(B[m[32m+}
(B[m 
[31m-    #[test]
(B[m[31m-    fn cli_rejects_invalid_target() {
(B[m[31m-        let args = [
(B[m[31m-            "opal".to_string(),
(B[m[31m-            "run".to_string(),
(B[m[31m-            "test-projects/hello-world/src/main.op".to_string(),
(B[m[31m-            "--target".to_string(),
(B[m[31m-            "banana-pi-linux".to_string(),
(B[m[31m-        ];
(B[m[31m-        let result = run_with_args(&args);
(B[m[31m-        assert_eq!(result, Err(1));
(B[m[31m-    }
(B[m[32m+#[test]
(B[m[32m+fn cli_rejects_invalid_target() {
(B[m[32m+    let args = [
(B[m[32m+        "opal".to_string(),
(B[m[32m+        "run".to_string(),
(B[m[32m+        "test-projects/hello-world/src/main.op".to_string(),
(B[m[32m+        "--target".to_string(),
(B[m[32m+        "banana-pi-linux".to_string(),
(B[m[32m+    ];
(B[m[32m+    let result = run_with_args(&args);
(B[m[32m+    assert_eq!(result, Err(1));
(B[m[32m+}
(B[m 
Diff in /home/justi/Projects/opalescent/src/codegen/adts.rs:69:
         )));
     };
 
[31m-    if let Some(lowered) =
(B[m[31m-        codegen_intrinsic_member_access(
(B[m[31m-            codegen_context,
(B[m[31m-            env,
(B[m[31m-            effective_receiver_name.as_str(),
(B[m[31m-            &binding,
(B[m[31m-            effective_member_name,
(B[m[31m-        )?
(B[m[31m-    {
(B[m[32m+    if let Some(lowered) = codegen_intrinsic_member_access(
(B[m[32m+        codegen_context,
(B[m[32m+        env,
(B[m[32m+        effective_receiver_name.as_str(),
(B[m[32m+        &binding,
(B[m[32m+        effective_member_name,
(B[m[32m+    )? {
(B[m         return Ok(lowered);
     }
 
Diff in /home/justi/Projects/opalescent/src/codegen/adts.rs:174:
     }
 
     if matches!(binding.core_type, CoreType::Array(_)) && member_name == "length" {
[31m-        let length_value = resolve_array_length_value(codegen_context, env, receiver_name, binding)?;
(B[m[32m+        let length_value =
(B[m[32m+            resolve_array_length_value(codegen_context, env, receiver_name, binding)?;
(B[m         return Ok(Some(length_value.as_basic_value_enum()));
     }
 
Diff in /home/justi/Projects/opalescent/src/codegen/control_flow.rs:251:
                     (array_ptr, array_length, element_core_type.as_ref().clone())
                 }
                 Expr::Array { ref elements, .. } => {
[31m-                    let element_core_type = elements.first().map_or(CoreType::Int64, |first| {
(B[m[31m-                        match *first {
(B[m[31m-                            Expr::Literal {
(B[m[31m-                                value: crate::ast::LiteralValue::Float(_),
(B[m[31m-                                ..
(B[m[31m-                            } => CoreType::Float64,
(B[m[31m-                            Expr::Literal {
(B[m[31m-                                value: crate::ast::LiteralValue::String(_),
(B[m[31m-                                ..
(B[m[31m-                            } => CoreType::String,
(B[m[31m-                            Expr::Literal {
(B[m[31m-                                value: crate::ast::LiteralValue::Boolean(_),
(B[m[31m-                                ..
(B[m[31m-                            } => CoreType::Boolean,
(B[m[31m-                            _ => CoreType::Int64,
(B[m[31m-                        }
(B[m[31m-                    });
(B[m[32m+                    let element_core_type =
(B[m[32m+                        elements
(B[m[32m+                            .first()
(B[m[32m+                            .map_or(CoreType::Int64, |first| match *first {
(B[m[32m+                                Expr::Literal {
(B[m[32m+                                    value: crate::ast::LiteralValue::Float(_),
(B[m[32m+                                    ..
(B[m[32m+                                } => CoreType::Float64,
(B[m[32m+                                Expr::Literal {
(B[m[32m+                                    value: crate::ast::LiteralValue::String(_),
(B[m[32m+                                    ..
(B[m[32m+                                } => CoreType::String,
(B[m[32m+                                Expr::Literal {
(B[m[32m+                                    value: crate::ast::LiteralValue::Boolean(_),
(B[m[32m+                                    ..
(B[m[32m+                                } => CoreType::Boolean,
(B[m[32m+                                _ => CoreType::Int64,
(B[m[32m+                            });
(B[m                     let iterable_expected_type =
                         CoreType::Array(Box::new(element_core_type.clone()));
                     let iterable_value = codegen_expression(
Diff in /home/justi/Projects/opalescent/src/codegen/functions_call_tests.rs:48:
 
     let return_type_text = function_type
         .get_return_type()
[31m-        .map_or_else(String::new, |return_type| return_type.print_to_string().to_string());
(B[m[32m+        .map_or_else(String::new, |return_type| {
(B[m[32m+            return_type.print_to_string().to_string()
(B[m[32m+        });
(B[m     assert_eq!(return_type_text, "i32", "puts return type should be i32");
 
     let parameter_types = function_type.get_param_types();
Diff in /home/justi/Projects/opalescent/src/codegen/functions_call_tests.rs:70:
     let codegen_context = CodegenContext::new(&context, "resolve_printf_to_variadic_printf");
     let mut env = CodegenEnv::new(true);
 
[31m-    let result =
(B[m[31m-        resolve_callee_function(&codegen_context, &mut env, &identifier("printf"), None);
(B[m[32m+    let result = resolve_callee_function(&codegen_context, &mut env, &identifier("printf"), None);
(B[m     assert!(
         result.is_ok(),
         "printf should resolve successfully to libc printf"
Diff in /home/justi/Projects/opalescent/src/codegen/functions_call_tests.rs:94:
 
     let return_type_text = function_type
         .get_return_type()
[31m-        .map_or_else(String::new, |return_type| return_type.print_to_string().to_string());
(B[m[32m+        .map_or_else(String::new, |return_type| {
(B[m[32m+            return_type.print_to_string().to_string()
(B[m[32m+        });
(B[m     assert_eq!(return_type_text, "i32", "printf return type should be i32");
 
     let parameter_types = function_type.get_param_types();
Diff in /home/justi/Projects/opalescent/src/codegen/functions_stdlib.rs:4:
 use crate::codegen::error::CodegenError;
 use alloc::format;
 use alloc::string::String;
[31m-use inkwell::attributes::{Attribute, AttributeLoc};
(B[m use inkwell::AddressSpace;
[32m+use inkwell::attributes::{Attribute, AttributeLoc};
(B[m use inkwell::values::FunctionValue;
 
 #[doc = "Declare a stdlib function in the LLVM module if not already present."]
Diff in /home/justi/Projects/opalescent/src/codegen/functions_stdlib.rs:301:
                 false,
             );
             let function = module.add_function("read_lines_sync", ft, None);
[31m-            apply_sret_attr_if_needed(codegen_context, function, fs_string_array_result_type.into());
(B[m[32m+            apply_sret_attr_if_needed(
(B[m[32m+                codegen_context,
(B[m[32m+                function,
(B[m[32m+                fs_string_array_result_type.into(),
(B[m[32m+            );
(B[m             Some(function)
         }),
         "read_bytes_at_offset_sync" => {
Diff in /home/justi/Projects/opalescent/src/codegen/statements.rs:306:
                     .builder
                     .build_store(success_alloca, success_value.get_type().const_zero())?;
                 let len_binding_name = format!("{success_binding}_len");
[31m-                let len_alloca = if matches!(success_core_type, CoreType::Array(_)) && field_count >= 3 {
(B[m[31m-                    let length_alloca = codegen_context
(B[m[31m-                        .builder
(B[m[31m-                        .build_alloca(codegen_context.context.i64_type(), len_binding_name.as_str())?;
(B[m[31m-                    let _length_init = codegen_context.builder.build_store(
(B[m[31m-                        length_alloca,
(B[m[31m-                        codegen_context.context.i64_type().const_zero(),
(B[m[31m-                    )?;
(B[m[31m-                    Some(length_alloca)
(B[m[31m-                } else {
(B[m[31m-                    None
(B[m[31m-                };
(B[m[32m+                let len_alloca =
(B[m[32m+                    if matches!(success_core_type, CoreType::Array(_)) && field_count >= 3 {
(B[m[32m+                        let length_alloca = codegen_context.builder.build_alloca(
(B[m[32m+                            codegen_context.context.i64_type(),
(B[m[32m+                            len_binding_name.as_str(),
(B[m[32m+                        )?;
(B[m[32m+                        let _length_init = codegen_context.builder.build_store(
(B[m[32m+                            length_alloca,
(B[m[32m+                            codegen_context.context.i64_type().const_zero(),
(B[m[32m+                        )?;
(B[m[32m+                        Some(length_alloca)
(B[m[32m+                    } else {
(B[m[32m+                        None
(B[m[32m+                    };
(B[m 
                 let current_fn = current_function(codegen_context)?;
                 let success_block = codegen_context
Diff in /home/justi/Projects/opalescent/src/codegen/statements.rs:530:
         } => {
             let object_type = infer_core_type_from_expr(codegen_context, env, object);
             match object_type {
[31m-                CoreType::String | CoreType::Array(_) if member == "length" => {
(B[m[31m-                    CoreType::Int64
(B[m[31m-                }
(B[m[32m+                CoreType::String | CoreType::Array(_) if member == "length" => CoreType::Int64,
(B[m                 CoreType::Generic {
                     ref name,
                     ref type_args,
Diff in /home/justi/Projects/opalescent/src/compiler.rs:97:
             .map_err(CompileError::Io)?;
         std::fs::write(dir.join("opal_rc.h"), OPAL_RC_H).map_err(CompileError::Io)?;
         std::fs::write(dir.join("opal_runtime.h"), OPAL_RUNTIME_H).map_err(CompileError::Io)?;
[31m-        std::fs::write(dir.join("opal_fs_errors.h"), OPAL_FS_ERRORS_H)
(B[m[31m-            .map_err(CompileError::Io)?;
(B[m[32m+        std::fs::write(dir.join("opal_fs_errors.h"), OPAL_FS_ERRORS_H).map_err(CompileError::Io)?;
(B[m 
         Ok(Self { dir, source_file })
     }
Diff in /home/justi/Projects/opalescent/src/compiler.rs:649:
                     continue;
                 }
 
[31m-                let resolved_path_result =
(B[m[31m-                    resolve_import_path(module_path, import_source.as_str());
(B[m[32m+                let resolved_path_result = resolve_import_path(module_path, import_source.as_str());
(B[m                 let Ok(resolved_path) = resolved_path_result else {
                     continue;
                 };
Diff in /home/justi/Projects/opalescent/src/compiler.rs:657:
                 if let Some(discovered_interface) = discovered_interfaces.get(&resolved_path) {
                     let mut source_keyed_interface = discovered_interface.clone();
[31m-                    source_keyed_interface
(B[m[31m-                        .module_path
(B[m[31m-                        .clone_from(import_source);
(B[m[32m+                    source_keyed_interface.module_path.clone_from(import_source);
(B[m                     checker.register_module_interface(source_keyed_interface);
                 }
             }
Diff in /home/justi/Projects/opalescent/src/type_system/module_resolver/standard_symbols_core_io_and_bytes.rs:299:
                         type_var: crate::type_system::types::TypeVar::new(9_001, "T".to_owned()),
                         constraints: Vec::new(),
                     }],
[31m-                    parameters: vec![CoreType::Array(alloc::boxed::Box::new(
(B[m[31m-                        CoreType::Variable(crate::type_system::types::TypeVar::new(
(B[m[31m-                            9_001,
(B[m[31m-                            "T".to_owned(),
(B[m[31m-                        )),
(B[m[31m-                    ))],
(B[m[32m+                    parameters: vec![CoreType::Array(alloc::boxed::Box::new(CoreType::Variable(
(B[m[32m+                        crate::type_system::types::TypeVar::new(9_001, "T".to_owned()),
(B[m[32m+                    )))],
(B[m                     return_types: vec![CoreType::Int64],
                     error_types: Vec::new(),
                 },
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/compile_failures.rs:359:
 #[test]
 fn ref_basic_fails_to_compile() {
     let cwd = std::env::current_dir();
[31m-    assert!(cwd.is_ok(), "current working directory should be readable for integration tests");
(B[m[32m+    assert!(
(B[m[32m+        cwd.is_ok(),
(B[m[32m+        "current working directory should be readable for integration tests"
(B[m[32m+    );
(B[m     let Ok(cwd_path) = cwd else {
         return;
     };
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/compile_failures.rs:367:
     let project_dir = cwd_path.join("test-projects/ref-basic");
     let temp_dir = project_dir.join("target");
     let prepare = prepare_dir(&temp_dir);
[31m-    assert!(prepare.is_ok(), "ref-basic target directory should be created");
(B[m[32m+    assert!(
(B[m[32m+        prepare.is_ok(),
(B[m[32m+        "ref-basic target directory should be created"
(B[m[32m+    );
(B[m 
     let execution_result: Result<(), String> = (|| {
         let binary_result =
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/compile_failures.rs:374:
             opalescent::compiler::compile_project(&project_dir, &temp_dir, &TargetTriple::host());
         if binary_result.is_ok() {
[31m-            return Err("ref-basic project should fail to compile, but compilation succeeded".to_owned());
(B[m[32m+            return Err(
(B[m[32m+                "ref-basic project should fail to compile, but compilation succeeded".to_owned(),
(B[m[32m+            );
(B[m         }
         Ok(())
     })();
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/compile_failures.rs:380:
 
     let cleanup = cleanup_dir(&temp_dir);
[31m-    assert!(cleanup.is_ok(), "ref-basic target directory should be removed");
(B[m[32m+    assert!(
(B[m[32m+        cleanup.is_ok(),
(B[m[32m+        "ref-basic target directory should be removed"
(B[m[32m+    );
(B[m     let failure_message = execution_result.err().unwrap_or_default();
[31m-    assert!(failure_message.is_empty(), "ref-basic should be rejected at compile time: {failure_message}");
(B[m[32m+    assert!(
(B[m[32m+        failure_message.is_empty(),
(B[m[32m+        "ref-basic should be rejected at compile time: {failure_message}"
(B[m[32m+    );
(B[m }
 
 #[test]
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/compile_failures.rs:388:
 fn mutable_ref_fails_to_compile() {
     let cwd = std::env::current_dir();
[31m-    assert!(cwd.is_ok(), "current working directory should be readable for integration tests");
(B[m[32m+    assert!(
(B[m[32m+        cwd.is_ok(),
(B[m[32m+        "current working directory should be readable for integration tests"
(B[m[32m+    );
(B[m     let Ok(cwd_path) = cwd else {
         return;
     };
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/compile_failures.rs:395:
     let project_dir = cwd_path.join("test-projects/mutable-ref");
     let temp_dir = project_dir.join("target");
     let prepare = prepare_dir(&temp_dir);
[31m-    assert!(prepare.is_ok(), "mutable-ref target directory should be created");
(B[m[32m+    assert!(
(B[m[32m+        prepare.is_ok(),
(B[m[32m+        "mutable-ref target directory should be created"
(B[m[32m+    );
(B[m 
     let execution_result: Result<(), String> = (|| {
         let binary_result =
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/compile_failures.rs:402:
             opalescent::compiler::compile_project(&project_dir, &temp_dir, &TargetTriple::host());
         if binary_result.is_ok() {
[31m-            return Err("mutable-ref project should fail to compile, but compilation succeeded".to_owned());
(B[m[32m+            return Err(
(B[m[32m+                "mutable-ref project should fail to compile, but compilation succeeded".to_owned(),
(B[m[32m+            );
(B[m         }
         Ok(())
     })();
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/compile_failures.rs:408:
 
     let cleanup = cleanup_dir(&temp_dir);
[31m-    assert!(cleanup.is_ok(), "mutable-ref target directory should be removed");
(B[m[32m+    assert!(
(B[m[32m+        cleanup.is_ok(),
(B[m[32m+        "mutable-ref target directory should be removed"
(B[m[32m+    );
(B[m     let failure_message = execution_result.err().unwrap_or_default();
[31m-    assert!(failure_message.is_empty(), "mutable-ref should be rejected at compile time: {failure_message}");
(B[m[32m+    assert!(
(B[m[32m+        failure_message.is_empty(),
(B[m[32m+        "mutable-ref should be rejected at compile time: {failure_message}"
(B[m[32m+    );
(B[m }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_absolute_path_sync.rs:1:
 #![cfg(feature = "integration")]
 
[32m+use super::fs_helpers::{
(B[m[32m+    FsStateGuard, assert_workspace_empty, strip_crlf, unique_probe_target_dir,
(B[m[32m+};
(B[m use super::*;
[31m-use super::fs_helpers::{FsStateGuard, assert_workspace_empty, strip_crlf, unique_probe_target_dir};
(B[m use serial_test::serial;
 
 fn stringify_error<E: core::fmt::Display>(error: E) -> String {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_absolute_path_sync.rs:30:
         let project_dir = cwd_path.join("test-projects/_absolute_path_sync");
         let temp_dir = unique_probe_target_dir("absolute-path-sync-fixture");
 
[31m-        let binary_result = opalescent::compiler::compile_project(
(B[m[31m-            &project_dir,
(B[m[31m-            &temp_dir,
(B[m[31m-            &TargetTriple::host(),
(B[m[31m-        );
(B[m[32m+        let binary_result =
(B[m[32m+            opalescent::compiler::compile_project(&project_dir, &temp_dir, &TargetTriple::host());
(B[m         assert!(
             binary_result.is_ok(),
             "_absolute_path_sync fixture should compile into a binary: {}",
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_absolute_path_sync.rs:61:
         };
 
         let stdout = strip_crlf(&String::from_utf8_lossy(&run_output.stdout));
[31m-        let lines: Vec<&str> = stdout
(B[m[31m-            .lines()
(B[m[31m-            .map(str::trim)
(B[m[31m-            .collect();
(B[m[32m+        let lines: Vec<&str> = stdout.lines().map(str::trim).collect();
(B[m 
         assert_eq!(
             lines.len(),
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_absolute_path_sync.rs:94:
         );
 
         let root_line = lines.get(3).copied().unwrap_or_default();
[31m-        assert_eq!(root_line, "/ -> /", "already absolute root path should remain root");
(B[m[32m+        assert_eq!(
(B[m[32m+            root_line, "/ -> /",
(B[m[32m+            "already absolute root path should remain root"
(B[m[32m+        );
(B[m 
         assert!(
             run_output.status.success(),
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_append_file_string.rs:40:
     )
 }
 
[31m-
(B[m[31m-fn compile_and_run_inline_program(source: &str, temp_dir: &Path) -> Result<std::process::Output, String> {
(B[m[32m+fn compile_and_run_inline_program(
(B[m[32m+    source: &str,
(B[m[32m+    temp_dir: &Path,
(B[m[32m+) -> Result<std::process::Output, String> {
(B[m     let binary_result = compile_program(
         Path::new("test-projects/_t22_append_file_string/src/main.op"),
         source,
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_append_file_string.rs:63:
         .map_err(|error| format!("t22 append_text probe binary should execute: {error}"))
 }
 
[31m-fn extract_between_markers(stdout: &str, start_marker: &str, end_marker: &str) -> Result<String, String> {
(B[m[31m-    let start_idx = stdout
(B[m[31m-        .find(start_marker)
(B[m[31m-        .ok_or_else(|| format!("stdout should contain start marker '{start_marker}', got:\n{stdout}"))?;
(B[m[31m-    let content_start = start_idx
(B[m[31m-        .checked_add(start_marker.len())
(B[m[31m-        .ok_or_else(|| format!("stdout marker offset overflowed for '{start_marker}', got:\n{stdout}"))?;
(B[m[32m+fn extract_between_markers(
(B[m[32m+    stdout: &str,
(B[m[32m+    start_marker: &str,
(B[m[32m+    end_marker: &str,
(B[m[32m+) -> Result<String, String> {
(B[m[32m+    let start_idx = stdout.find(start_marker).ok_or_else(|| {
(B[m[32m+        format!("stdout should contain start marker '{start_marker}', got:\n{stdout}")
(B[m[32m+    })?;
(B[m[32m+    let content_start = start_idx.checked_add(start_marker.len()).ok_or_else(|| {
(B[m[32m+        format!("stdout marker offset overflowed for '{start_marker}', got:\n{stdout}")
(B[m[32m+    })?;
(B[m 
     let tail = stdout
         .get(content_start..)
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_append_file_string.rs:76:
         .ok_or_else(|| format!("stdout marker offset should be a valid UTF-8 boundary for '{start_marker}', got:\n{stdout}"))?;
[31m-    let end_rel = tail
(B[m[31m-        .find(end_marker)
(B[m[31m-        .ok_or_else(|| format!("stdout should contain end marker '{end_marker}', got:\n{stdout}"))?;
(B[m[31m-    let mut extracted = tail
(B[m[31m-        .get(..end_rel)
(B[m[31m-        .ok_or_else(|| format!("stdout end marker should align to a UTF-8 boundary for '{end_marker}', got:\n{stdout}"))?;
(B[m[32m+    let end_rel = tail.find(end_marker).ok_or_else(|| {
(B[m[32m+        format!("stdout should contain end marker '{end_marker}', got:\n{stdout}")
(B[m[32m+    })?;
(B[m[32m+    let mut extracted = tail.get(..end_rel).ok_or_else(|| {
(B[m[32m+        format!(
(B[m[32m+            "stdout end marker should align to a UTF-8 boundary for '{end_marker}', got:\n{stdout}"
(B[m[32m+        )
(B[m[32m+    })?;
(B[m 
     if let Some(rest) = extracted.strip_prefix("\r\n") {
         extracted = rest;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_append_file_string.rs:104:
 
     let compile_dir = unique_probe_target_dir("append-not-found");
     let prepare = prepare_dir(&compile_dir);
[31m-    assert!(prepare.is_ok(), "t22 not-found temp directory should be created");
(B[m[32m+    assert!(
(B[m[32m+        prepare.is_ok(),
(B[m[32m+        "t22 not-found temp directory should be created"
(B[m[32m+    );
(B[m 
     let missing_dir = make_temp_path("missing-parent");
     let missing_file = missing_dir.join("nested").join("file.txt");
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_append_file_string.rs:138:
     })();
 
     let cleanup = cleanup_dir(&compile_dir);
[31m-    assert!(cleanup.is_ok(), "t22 not-found temp directory should be removed");
(B[m[32m+    assert!(
(B[m[32m+        cleanup.is_ok(),
(B[m[32m+        "t22 not-found temp directory should be removed"
(B[m[32m+    );
(B[m 
     let failure_message = match execution_result {
         Ok(()) => String::new(),
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_append_file_string.rs:163:
 
     let compile_dir = unique_probe_target_dir("append-perm");
     let prepare = prepare_dir(&compile_dir);
[31m-    assert!(prepare.is_ok(), "t22 permission temp directory should be created");
(B[m[32m+    assert!(
(B[m[32m+        prepare.is_ok(),
(B[m[32m+        "t22 permission temp directory should be created"
(B[m[32m+    );
(B[m 
     let fixture_dir = make_temp_path("perm");
     let fixture_file = fixture_dir.join("blocked.txt");
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_append_file_string.rs:213:
     drop(fs::remove_dir_all(&fixture_dir));
 
     let cleanup = cleanup_dir(&compile_dir);
[31m-    assert!(cleanup.is_ok(), "t22 permission temp directory should be removed");
(B[m[32m+    assert!(
(B[m[32m+        cleanup.is_ok(),
(B[m[32m+        "t22 permission temp directory should be removed"
(B[m[32m+    );
(B[m 
     let failure_message = match execution_result {
         Ok(()) => String::new(),
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_append_file_string.rs:233:
 
     let compile_dir = unique_probe_target_dir("append-isdir");
     let prepare = prepare_dir(&compile_dir);
[31m-    assert!(prepare.is_ok(), "t22 is-directory temp directory should be created");
(B[m[32m+    assert!(
(B[m[32m+        prepare.is_ok(),
(B[m[32m+        "t22 is-directory temp directory should be created"
(B[m[32m+    );
(B[m 
     let directory_path = make_temp_path("dir-target");
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_append_file_string.rs:271:
     drop(fs::remove_dir_all(&directory_path));
 
     let cleanup = cleanup_dir(&compile_dir);
[31m-    assert!(cleanup.is_ok(), "t22 is-directory temp directory should be removed");
(B[m[32m+    assert!(
(B[m[32m+        cleanup.is_ok(),
(B[m[32m+        "t22 is-directory temp directory should be removed"
(B[m[32m+    );
(B[m 
     let failure_message = match execution_result {
         Ok(()) => String::new(),
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_append_file_string.rs:291:
 
     let compile_dir = unique_probe_target_dir("append-creates-new");
     let prepare = prepare_dir(&compile_dir);
[31m-    assert!(prepare.is_ok(), "t22 create-new temp directory should be created");
(B[m[32m+    assert!(
(B[m[32m+        prepare.is_ok(),
(B[m[32m+        "t22 create-new temp directory should be created"
(B[m[32m+    );
(B[m 
     let fixture_dir = make_temp_path("create-new");
     let fixture_file = fixture_dir.join("new.txt");
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_append_file_string.rs:311:
         }
 
         let stdout = String::from_utf8_lossy(&run_output.stdout);
[31m-        let extracted = extract_between_markers(&stdout, "APPEND_OUTPUT_START", "APPEND_OUTPUT_END")?;
(B[m[32m+        let extracted =
(B[m[32m+            extract_between_markers(&stdout, "APPEND_OUTPUT_START", "APPEND_OUTPUT_END")?;
(B[m         if extracted != "B" {
             return Err(format!(
                 "append create-new output should be exactly 'B', got '{extracted}'"
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_append_file_string.rs:333:
     drop(fs::remove_dir_all(&fixture_dir));
 
     let cleanup = cleanup_dir(&compile_dir);
[31m-    assert!(cleanup.is_ok(), "t22 create-new temp directory should be removed");
(B[m[32m+    assert!(
(B[m[32m+        cleanup.is_ok(),
(B[m[32m+        "t22 create-new temp directory should be removed"
(B[m[32m+    );
(B[m 
     let failure_message = match execution_result {
         Ok(()) => String::new(),
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_append_file_string.rs:353:
 
     let compile_dir = unique_probe_target_dir("append-existing");
     let prepare = prepare_dir(&compile_dir);
[31m-    assert!(prepare.is_ok(), "t22 append-existing temp directory should be created");
(B[m[32m+    assert!(
(B[m[32m+        prepare.is_ok(),
(B[m[32m+        "t22 append-existing temp directory should be created"
(B[m[32m+    );
(B[m 
     let fixture_dir = make_temp_path("append-existing");
     let fixture_file = fixture_dir.join("existing.txt");
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_append_file_string.rs:375:
         }
 
         let stdout = String::from_utf8_lossy(&run_output.stdout);
[31m-        let extracted = extract_between_markers(&stdout, "APPEND_OUTPUT_START", "APPEND_OUTPUT_END")?;
(B[m[32m+        let extracted =
(B[m[32m+            extract_between_markers(&stdout, "APPEND_OUTPUT_START", "APPEND_OUTPUT_END")?;
(B[m         if extracted != "AB" {
             return Err(format!(
                 "append-existing output should be exactly 'AB', got '{extracted}'"
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_append_file_string.rs:397:
     drop(fs::remove_dir_all(&fixture_dir));
 
     let cleanup = cleanup_dir(&compile_dir);
[31m-    assert!(cleanup.is_ok(), "t22 append-existing temp directory should be removed");
(B[m[32m+    assert!(
(B[m[32m+        cleanup.is_ok(),
(B[m[32m+        "t22 append-existing temp directory should be removed"
(B[m[32m+    );
(B[m 
     let failure_message = match execution_result {
         Ok(()) => String::new(),
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_append_log.rs:1:
 #![cfg(feature = "integration")]
 
[32m+use super::fs_helpers::{
(B[m[32m+    FsStateGuard, assert_workspace_empty, strip_crlf, unique_probe_target_dir,
(B[m[32m+};
(B[m use super::*;
[31m-use super::fs_helpers::{FsStateGuard, assert_workspace_empty, strip_crlf, unique_probe_target_dir};
(B[m use serial_test::serial;
 use std::path::Path;
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_append_log.rs:8:
 fn append_log_path() -> String {
     std::env::temp_dir()
[31m-        .join(format!("opalescent-fs-append-log-{}.txt", std::process::id()))
(B[m[32m+        .join(format!(
(B[m[32m+            "opalescent-fs-append-log-{}.txt",
(B[m[32m+            std::process::id()
(B[m[32m+        ))
(B[m         .to_string_lossy()
         .into_owned()
 }
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_append_log.rs:46:
         let project_dir = cwd_path.join("test-projects/_fs_append_log");
         let temp_dir = unique_probe_target_dir("append-log-fixture");
 
[31m-        let binary_result = opalescent::compiler::compile_project(
(B[m[31m-            &project_dir,
(B[m[31m-            &temp_dir,
(B[m[31m-            &TargetTriple::host(),
(B[m[31m-        );
(B[m[32m+        let binary_result =
(B[m[32m+            opalescent::compiler::compile_project(&project_dir, &temp_dir, &TargetTriple::host());
(B[m         assert!(
             binary_result.is_ok(),
             "_fs_append_log fixture should compile into a binary: {}",
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_append_log.rs:57:
[31m-            binary_result
(B[m[31m-                .as_ref()
(B[m[31m-                .err()
(B[m[31m-                .map_or_else(|| String::from("unknown compile error"), |error| format!("{error}"))
(B[m[32m+            binary_result.as_ref().err().map_or_else(
(B[m[32m+                || String::from("unknown compile error"),
(B[m[32m+                |error| format!("{error}")
(B[m[32m+            )
(B[m         );
         let Ok(binary_path) = binary_result else {
             return;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_append_log.rs:70:
         assert!(
             output_result.is_ok(),
             "_fs_append_log compiled binary should execute: {}",
[31m-            output_result
(B[m[31m-                .as_ref()
(B[m[31m-                .err()
(B[m[31m-                .map_or_else(|| String::from("unknown execution error"), |error| format!("{error}"))
(B[m[32m+            output_result.as_ref().err().map_or_else(
(B[m[32m+                || String::from("unknown execution error"),
(B[m[32m+                |error| format!("{error}")
(B[m[32m+            )
(B[m         );
         let Ok(run_output) = output_result else {
             return;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_append_log.rs:112:
 
         let temp_dir = unique_probe_target_dir("append-log-monotonic");
         let prepare = prepare_dir(&temp_dir);
[31m-        assert!(prepare.is_ok(), "_fs_append_log target directory should be created");
(B[m[32m+        assert!(
(B[m[32m+            prepare.is_ok(),
(B[m[32m+            "_fs_append_log target directory should be created"
(B[m[32m+        );
(B[m 
         let mut previous_size: u64 = 0;
         for idx in 0_usize..5_usize {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_append_log.rs:128:
             assert!(
                 binary_result.is_ok(),
                 "monotonic append inline source should compile: {}",
[31m-                binary_result
(B[m[31m-                    .as_ref()
(B[m[31m-                    .err()
(B[m[31m-                    .map_or_else(|| String::from("unknown compile error"), |error| format!("{error}"))
(B[m[32m+                binary_result.as_ref().err().map_or_else(
(B[m[32m+                    || String::from("unknown compile error"),
(B[m[32m+                    |error| format!("{error}")
(B[m[32m+                )
(B[m             );
             let Ok(binary_path) = binary_result else {
                 return;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_append_log.rs:141:
             assert!(
                 output_result.is_ok(),
                 "monotonic append binary should execute: {}",
[31m-                output_result
(B[m[31m-                    .as_ref()
(B[m[31m-                    .err()
(B[m[31m-                    .map_or_else(|| String::from("unknown execution error"), |error| format!("{error}"))
(B[m[32m+                output_result.as_ref().err().map_or_else(
(B[m[32m+                    || String::from("unknown execution error"),
(B[m[32m+                    |error| format!("{error}")
(B[m[32m+                )
(B[m             );
             let Ok(run_output) = output_result else {
                 return;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_append_log.rs:162:
                 metadata_result.is_ok(),
                 "log file metadata should be readable after append {}: {}",
                 idx + 1_usize,
[31m-                metadata_result
(B[m[31m-                    .as_ref()
(B[m[31m-                    .err()
(B[m[31m-                    .map_or_else(|| String::from("unknown metadata error"), |error| format!("{error}"))
(B[m[32m+                metadata_result.as_ref().err().map_or_else(
(B[m[32m+                    || String::from("unknown metadata error"),
(B[m[32m+                    |error| format!("{error}")
(B[m[32m+                )
(B[m             );
             let Ok(metadata) = metadata_result else {
                 return;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_append_log.rs:182:
         }
 
         let cleanup = cleanup_dir(&temp_dir);
[31m-        assert!(cleanup.is_ok(), "_fs_append_log target directory should be removed");
(B[m[32m+        assert!(
(B[m[32m+            cleanup.is_ok(),
(B[m[32m+            "_fs_append_log target directory should be removed"
(B[m[32m+        );
(B[m     }
 
     drop(fs::remove_file(append_log_path()));
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_copy_file.rs:1:
 #![cfg(feature = "integration")]
 
[31m-use super::fs_helpers::{assert_workspace_empty, fs_project_root, unique_probe_target_dir, FsStateGuard};
(B[m[32m+use super::fs_helpers::{
(B[m[32m+    FsStateGuard, assert_workspace_empty, fs_project_root, unique_probe_target_dir,
(B[m[32m+};
(B[m use super::*;
 use serial_test::serial;
 use sha2::{Digest, Sha256};
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_copy_file.rs:40:
     )
 }
 
[31m-fn compile_and_run_inline_program(source: &str, temp_dir: &Path) -> Result<std::process::Output, String> {
(B[m[32m+fn compile_and_run_inline_program(
(B[m[32m+    source: &str,
(B[m[32m+    temp_dir: &Path,
(B[m[32m+) -> Result<std::process::Output, String> {
(B[m     let binary_result = compile_program(
         Path::new("test-projects/_t26_copy_file/src/main.op"),
         source,
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_copy_file.rs:66:
     let mut hasher = Sha256::new();
     hasher.update(data);
     let digest = hasher.finalize();
[31m-    let capacity = digest
(B[m[31m-        .len()
(B[m[31m-        .checked_mul(2)
(B[m[31m-        .unwrap_or_default();
(B[m[32m+    let capacity = digest.len().checked_mul(2).unwrap_or_default();
(B[m     let mut out = String::with_capacity(capacity);
     for b in digest {
         use std::fmt::Write;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_copy_file.rs:90:
 
     let temp_dir = unique_probe_target_dir("copy-src-missing");
     let prepare = prepare_dir(&temp_dir);
[31m-    assert!(prepare.is_ok(), "t26 src-missing temp directory should be created");
(B[m[32m+    assert!(
(B[m[32m+        prepare.is_ok(),
(B[m[32m+        "t26 src-missing temp directory should be created"
(B[m[32m+    );
(B[m 
     let base = make_temp_path("src-missing");
     let source_path = base.join("missing.bin");
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_copy_file.rs:134:
     drop(fs::remove_dir_all(&base));
 
     let cleanup = cleanup_dir(&temp_dir);
[31m-    assert!(cleanup.is_ok(), "t26 src-missing temp directory should be removed");
(B[m[32m+    assert!(
(B[m[32m+        cleanup.is_ok(),
(B[m[32m+        "t26 src-missing temp directory should be removed"
(B[m[32m+    );
(B[m 
     let failure_message = match execution_result {
         Ok(()) => String::new(),
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_copy_file.rs:234:
 
     let temp_dir = unique_probe_target_dir("copy-perm");
     let prepare = prepare_dir(&temp_dir);
[31m-    assert!(prepare.is_ok(), "t26 permission temp directory should be created");
(B[m[32m+    assert!(
(B[m[32m+        prepare.is_ok(),
(B[m[32m+        "t26 permission temp directory should be created"
(B[m[32m+    );
(B[m 
     let base = make_temp_path("perm");
     let source_path = base.join("source.bin");
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_copy_file.rs:297:
     drop(fs::remove_dir_all(&base));
 
     let cleanup = cleanup_dir(&temp_dir);
[31m-    assert!(cleanup.is_ok(), "t26 permission temp directory should be removed");
(B[m[32m+    assert!(
(B[m[32m+        cleanup.is_ok(),
(B[m[32m+        "t26 permission temp directory should be removed"
(B[m[32m+    );
(B[m 
     let failure_message = match execution_result {
         Ok(()) => String::new(),
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_copy_file.rs:318:
 
     let temp_dir = unique_probe_target_dir("copy-src-isdir");
     let prepare = prepare_dir(&temp_dir);
[31m-    assert!(prepare.is_ok(), "t26 src-isdir temp directory should be created");
(B[m[32m+    assert!(
(B[m[32m+        prepare.is_ok(),
(B[m[32m+        "t26 src-isdir temp directory should be created"
(B[m[32m+    );
(B[m 
     let base = make_temp_path("src-isdir");
     let source_dir = base.join("source-dir");
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_copy_file.rs:362:
     drop(fs::remove_dir_all(&base));
 
     let cleanup = cleanup_dir(&temp_dir);
[31m-    assert!(cleanup.is_ok(), "t26 src-isdir temp directory should be removed");
(B[m[32m+    assert!(
(B[m[32m+        cleanup.is_ok(),
(B[m[32m+        "t26 src-isdir temp directory should be removed"
(B[m[32m+    );
(B[m 
     let failure_message = match execution_result {
         Ok(()) => String::new(),
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_copy_file.rs:383:
 
     let temp_dir = unique_probe_target_dir("copy-dest-isdir");
     let prepare = prepare_dir(&temp_dir);
[31m-    assert!(prepare.is_ok(), "t26 dest-isdir temp directory should be created");
(B[m[32m+    assert!(
(B[m[32m+        prepare.is_ok(),
(B[m[32m+        "t26 dest-isdir temp directory should be created"
(B[m[32m+    );
(B[m 
     let base = make_temp_path("dest-isdir");
     let source_file = base.join("source.bin");
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_copy_file.rs:429:
     drop(fs::remove_dir_all(&base));
 
     let cleanup = cleanup_dir(&temp_dir);
[31m-    assert!(cleanup.is_ok(), "t26 dest-isdir temp directory should be removed");
(B[m[32m+    assert!(
(B[m[32m+        cleanup.is_ok(),
(B[m[32m+        "t26 dest-isdir temp directory should be removed"
(B[m[32m+    );
(B[m 
     let failure_message = match execution_result {
         Ok(()) => String::new(),
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_dir_inventory.rs:1:
 #![cfg(feature = "integration")]
 
[32m+use super::fs_helpers::{
(B[m[32m+    FsStateGuard, assert_workspace_empty, strip_crlf, unique_probe_target_dir,
(B[m[32m+};
(B[m use super::*;
[31m-use super::fs_helpers::{FsStateGuard, assert_workspace_empty, strip_crlf, unique_probe_target_dir};
(B[m use serial_test::serial;
 use std::path::Path;
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_dir_inventory.rs:8:
 fn seed_inventory_files(base_dir: &Path) -> Result<(), String> {
[31m-    fs::create_dir_all(base_dir.join("workspace").join("inventory"))
(B[m[31m-        .map_err(|e| format!("fs_dir_inventory harness precreate inventory dir should succeed: {e}"))?;
(B[m[32m+    fs::create_dir_all(base_dir.join("workspace").join("inventory")).map_err(|e| {
(B[m[32m+        format!("fs_dir_inventory harness precreate inventory dir should succeed: {e}")
(B[m[32m+    })?;
(B[m 
[31m-    fs::write(base_dir.join("workspace").join("inventory").join("a.txt"), "alpha")
(B[m[31m-        .map_err(|e| format!("fs_dir_inventory harness seed a.txt should succeed: {e}"))?;
(B[m[31m-    fs::write(base_dir.join("workspace").join("inventory").join("b.txt"), "beta")
(B[m[31m-        .map_err(|e| format!("fs_dir_inventory harness seed b.txt should succeed: {e}"))?;
(B[m[31m-    fs::write(base_dir.join("workspace").join("inventory").join("c.txt"), "gamma")
(B[m[31m-        .map_err(|e| format!("fs_dir_inventory harness seed c.txt should succeed: {e}"))?;
(B[m[32m+    fs::write(
(B[m[32m+        base_dir.join("workspace").join("inventory").join("a.txt"),
(B[m[32m+        "alpha",
(B[m[32m+    )
(B[m[32m+    .map_err(|e| format!("fs_dir_inventory harness seed a.txt should succeed: {e}"))?;
(B[m[32m+    fs::write(
(B[m[32m+        base_dir.join("workspace").join("inventory").join("b.txt"),
(B[m[32m+        "beta",
(B[m[32m+    )
(B[m[32m+    .map_err(|e| format!("fs_dir_inventory harness seed b.txt should succeed: {e}"))?;
(B[m[32m+    fs::write(
(B[m[32m+        base_dir.join("workspace").join("inventory").join("c.txt"),
(B[m[32m+        "gamma",
(B[m[32m+    )
(B[m[32m+    .map_err(|e| format!("fs_dir_inventory harness seed c.txt should succeed: {e}"))?;
(B[m     Ok(())
 }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_dir_inventory.rs:83:
 "#
 }
 
[31m-fn compile_list_harness(harness_c: &std::path::Path, harness_bin: &std::path::Path) -> Result<(), String> {
(B[m[32m+fn compile_list_harness(
(B[m[32m+    harness_c: &std::path::Path,
(B[m[32m+    harness_bin: &std::path::Path,
(B[m[32m+) -> Result<(), String> {
(B[m     let compile = std::process::Command::new("cc")
         .arg("-std=gnu11")
         .arg("-I.")
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_dir_inventory.rs:99:
         let stdout = String::from_utf8_lossy(&compile.stdout);
         return Err(format!(
             "fs_dir_inventory harness compile should succeed, status={:?}, stdout='{}', stderr='{}'",
[31m-            compile.status.code(), stdout, stderr
(B[m[32m+            compile.status.code(),
(B[m[32m+            stdout,
(B[m[32m+            stderr
(B[m         ));
     }
     Ok(())
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_dir_inventory.rs:182:
             "_fs_dir_inventory target directory should be created before compile"
         );
 
[31m-        let binary_result = opalescent::compiler::compile_project(
(B[m[31m-            &project_dir,
(B[m[31m-            &temp_dir,
(B[m[31m-            &TargetTriple::host(),
(B[m[31m-        );
(B[m[32m+        let binary_result =
(B[m[32m+            opalescent::compiler::compile_project(&project_dir, &temp_dir, &TargetTriple::host());
(B[m         assert!(
             binary_result.is_ok(),
             "_fs_dir_inventory fixture should compile into a binary: {}",
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_dir_inventory.rs:193:
[31m-            binary_result
(B[m[31m-                .as_ref()
(B[m[31m-                .err()
(B[m[31m-                .map_or_else(|| String::from("unknown compile error"), |error| format!("{error}"))
(B[m[32m+            binary_result.as_ref().err().map_or_else(
(B[m[32m+                || String::from("unknown compile error"),
(B[m[32m+                |error| format!("{error}")
(B[m[32m+            )
(B[m         );
         let Ok(binary_path) = binary_result else {
             return;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_dir_inventory.rs:203:
         assert!(
             output_result.is_ok(),
             "_fs_dir_inventory compiled binary should execute: {}",
[31m-            output_result
(B[m[31m-                .as_ref()
(B[m[31m-                .err()
(B[m[31m-                .map_or_else(|| String::from("unknown execution error"), |error| format!("{error}"))
(B[m[32m+            output_result.as_ref().err().map_or_else(
(B[m[32m+                || String::from("unknown execution error"),
(B[m[32m+                |error| format!("{error}")
(B[m[32m+            )
(B[m         );
         let Ok(run_output) = output_result else {
             return;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_directories.rs:1:
 #![cfg(feature = "integration")]
 
[31m-use super::fs_helpers::{assert_workspace_empty, fs_project_root, unique_probe_target_dir, FsStateGuard};
(B[m[32m+use super::fs_helpers::{
(B[m[32m+    FsStateGuard, assert_workspace_empty, fs_project_root, unique_probe_target_dir,
(B[m[32m+};
(B[m use super::*;
 use serial_test::serial;
 use std::path::{Path, PathBuf};
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_directories.rs:96:
         let stdout = String::from_utf8_lossy(&compile.stdout);
         return Err(format!(
             "t28 harness compile should succeed, status={:?}, stdout='{}', stderr='{}'",
[31m-            compile.status.code(), stdout, stderr
(B[m[32m+            compile.status.code(),
(B[m[32m+            stdout,
(B[m[32m+            stderr
(B[m         ));
     }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_directories.rs:206:
     let fixture_dir = make_temp_path("mkdir-rmdir-roundtrip");
 
     let execution_result: Result<(), String> = (|| {
[31m-        let (mkdir_code, mkdir_stdout, mkdir_stderr) = run_harness("mkdir", &fixture_dir, &temp_dir)?;
(B[m[32m+        let (mkdir_code, mkdir_stdout, mkdir_stderr) =
(B[m[32m+            run_harness("mkdir", &fixture_dir, &temp_dir)?;
(B[m         if mkdir_code != 0_i32 || !mkdir_stdout.contains("OK") {
             return Err(format!(
                 "mkdir_rmdir_roundtrip mkdir should succeed, code={mkdir_code}, stdout='{mkdir_stdout}', stderr='{mkdir_stderr}'"
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_directories.rs:219:
             ));
         }
 
[31m-        let (rmdir_code, rmdir_stdout, rmdir_stderr) = run_harness("rmdir", &fixture_dir, &temp_dir)?;
(B[m[32m+        let (rmdir_code, rmdir_stdout, rmdir_stderr) =
(B[m[32m+            run_harness("rmdir", &fixture_dir, &temp_dir)?;
(B[m         if rmdir_code != 0_i32 || !rmdir_stdout.contains("OK") {
             return Err(format!(
                 "mkdir_rmdir_roundtrip rmdir should succeed, code={rmdir_code}, stdout='{rmdir_stdout}', stderr='{rmdir_stderr}'"
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_directory_operations.rs:1:
 #![cfg(feature = "integration")]
 
[32m+use super::fs_helpers::{
(B[m[32m+    FsStateGuard, assert_workspace_empty, strip_crlf, unique_probe_target_dir,
(B[m[32m+};
(B[m use super::*;
[31m-use super::fs_helpers::{FsStateGuard, assert_workspace_empty, strip_crlf, unique_probe_target_dir};
(B[m use serial_test::serial;
 
 #[cfg(feature = "integration")]
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_directory_operations.rs:26:
         let project_dir = cwd_path.join("test-projects/fs-directory-operations");
         let temp_dir = unique_probe_target_dir("directory-operations-fixture");
 
[31m-        let binary_result = opalescent::compiler::compile_project(
(B[m[31m-            &project_dir,
(B[m[31m-            &temp_dir,
(B[m[31m-            &TargetTriple::host(),
(B[m[31m-        );
(B[m[32m+        let binary_result =
(B[m[32m+            opalescent::compiler::compile_project(&project_dir, &temp_dir, &TargetTriple::host());
(B[m         assert!(
             binary_result.is_ok(),
             "fs-directory-operations fixture should compile into a binary: {}",
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_directory_operations.rs:37:
[31m-            binary_result
(B[m[31m-                .as_ref()
(B[m[31m-                .err()
(B[m[31m-                .map_or_else(|| String::from("unknown compile error"), |error| format!("{error}"))
(B[m[32m+            binary_result.as_ref().err().map_or_else(
(B[m[32m+                || String::from("unknown compile error"),
(B[m[32m+                |error| format!("{error}")
(B[m[32m+            )
(B[m         );
         let Ok(binary_path) = binary_result else {
             return;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_directory_operations.rs:47:
         assert!(
             output_result.is_ok(),
             "fs-directory-operations compiled binary should execute: {}",
[31m-            output_result
(B[m[31m-                .as_ref()
(B[m[31m-                .err()
(B[m[31m-                .map_or_else(|| String::from("unknown execution error"), |error| format!("{error}"))
(B[m[32m+            output_result.as_ref().err().map_or_else(
(B[m[32m+                || String::from("unknown execution error"),
(B[m[32m+                |error| format!("{error}")
(B[m[32m+            )
(B[m         );
         let Ok(run_output) = output_result else {
             return;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_directory_operations.rs:71:
             stdout.contains("all match"),
             "fs-directory-operations output should report all match, got: {stdout:?}"
         );
[31m-
(B[m     }
 
     assert_workspace_empty("fs-directory-operations");
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_helpers.rs:8:
 /// Example: `fs_project_root("_fs_path_from")` → `<repo>/test-projects/_fs_path_from`
 pub fn fs_project_root(name: &str) -> PathBuf {
     let repo_root = env!("CARGO_MANIFEST_DIR");
[31m-    PathBuf::from(repo_root)
(B[m[31m-        .join("test-projects")
(B[m[31m-        .join(name)
(B[m[32m+    PathBuf::from(repo_root).join("test-projects").join(name)
(B[m }
 
 /// Reads an evidence file from `.sisyphus/evidence/`.
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_helpers.rs:17:
 /// Convenience helper for asserting evidence in tests.
[31m-#[expect(dead_code, reason = "Evidence helper is used selectively by targeted integration checks")]
(B[m[32m+#[expect(
(B[m[32m+    dead_code,
(B[m[32m+    reason = "Evidence helper is used selectively by targeted integration checks"
(B[m[32m+)]
(B[m pub fn read_evidence(name: &str, scenario: &str) -> String {
     let repo_root = env!("CARGO_MANIFEST_DIR");
     let evidence_path = PathBuf::from(repo_root)
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_helpers.rs:35:
     let target_dir = project_path.join("target");
     if target_dir.exists() {
         let entries_result = fs::read_dir(&target_dir);
[31m-        assert!(entries_result.is_ok(), "Failed to read target dir for {project}");
(B[m[32m+        assert!(
(B[m[32m+            entries_result.is_ok(),
(B[m[32m+            "Failed to read target dir for {project}"
(B[m[32m+        );
(B[m         let Ok(entries) = entries_result else {
             return;
         };
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_helpers.rs:51:
     let workspace_dir = project_path.join("workspace");
     if workspace_dir.exists() {
         let entries_result = fs::read_dir(&workspace_dir);
[31m-        assert!(entries_result.is_ok(), "Failed to read workspace dir for {project}");
(B[m[32m+        assert!(
(B[m[32m+            entries_result.is_ok(),
(B[m[32m+            "Failed to read workspace dir for {project}"
(B[m[32m+        );
(B[m         let Ok(entries) = entries_result else {
             return;
         };
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_join_path_components.rs:1:
 #![cfg(feature = "integration")]
 
[32m+use super::fs_helpers::{
(B[m[32m+    FsStateGuard, assert_workspace_empty, strip_crlf, unique_probe_target_dir,
(B[m[32m+};
(B[m use super::*;
[31m-use super::fs_helpers::{FsStateGuard, assert_workspace_empty, strip_crlf, unique_probe_target_dir};
(B[m use serial_test::serial;
 
 fn stringify_error<E: core::fmt::Display>(error: E) -> String {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_join_path_components.rs:103:
         let project_dir = cwd_path.join("test-projects/_fs_join_path_components");
         let temp_dir = unique_probe_target_dir("join-fixture-showcase");
 
[31m-        let binary_result = opalescent::compiler::compile_project(
(B[m[31m-            &project_dir,
(B[m[31m-            &temp_dir,
(B[m[31m-            &TargetTriple::host(),
(B[m[31m-        );
(B[m[32m+        let binary_result =
(B[m[32m+            opalescent::compiler::compile_project(&project_dir, &temp_dir, &TargetTriple::host());
(B[m         assert!(
             binary_result.is_ok(),
             "_fs_join_path_components fixture should compile into a binary: {}",
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_join_path_components.rs:114:
[31m-            binary_result
(B[m[31m-                .as_ref()
(B[m[31m-                .err()
(B[m[31m-                .map_or_else(|| String::from("unknown compile error"), |error| format!("{error}"))
(B[m[32m+            binary_result.as_ref().err().map_or_else(
(B[m[32m+                || String::from("unknown compile error"),
(B[m[32m+                |error| format!("{error}")
(B[m[32m+            )
(B[m         );
         let Ok(binary_path) = binary_result else {
             return;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_join_path_components.rs:124:
         assert!(
             output_result.is_ok(),
             "_fs_join_path_components compiled binary should execute: {}",
[31m-            output_result
(B[m[31m-                .as_ref()
(B[m[31m-                .err()
(B[m[31m-                .map_or_else(|| String::from("unknown execution error"), |error| format!("{error}"))
(B[m[32m+            output_result.as_ref().err().map_or_else(
(B[m[32m+                || String::from("unknown execution error"),
(B[m[32m+                |error| format!("{error}")
(B[m[32m+            )
(B[m         );
         let Ok(run_output) = output_result else {
             return;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_join_path_components.rs:134:
         };
 
         let stdout = strip_crlf(&String::from_utf8_lossy(&run_output.stdout));
[31m-        let lines: Vec<&str> = stdout
(B[m[31m-            .lines()
(B[m[31m-            .map(str::trim)
(B[m[31m-            .collect();
(B[m[32m+        let lines: Vec<&str> = stdout.lines().map(str::trim).collect();
(B[m 
         let expected = vec![
             "home + [user, docs] -> home/user/docs",
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_join_path_components.rs:148:
         ];
 
         assert_eq!(
[31m-            lines,
(B[m[31m-            expected,
(B[m[32m+            lines, expected,
(B[m             "_fs_join_path_components fixture should print the 5 locked join cases"
         );
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_join_path_components.rs:209:
         assert!(
             binary_result.is_ok(),
             "join matrix source should compile into a binary: {}",
[31m-            binary_result
(B[m[31m-                .as_ref()
(B[m[31m-                .err()
(B[m[31m-                .map_or_else(|| String::from("unknown compile error"), |error| format!("{error}"))
(B[m[32m+            binary_result.as_ref().err().map_or_else(
(B[m[32m+                || String::from("unknown compile error"),
(B[m[32m+                |error| format!("{error}")
(B[m[32m+            )
(B[m         );
         let Ok(binary_path) = binary_result else {
             return;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_join_path_components.rs:222:
         assert!(
             output_result.is_ok(),
             "join matrix compiled binary should execute: {}",
[31m-            output_result
(B[m[31m-                .as_ref()
(B[m[31m-                .err()
(B[m[31m-                .map_or_else(|| String::from("unknown execution error"), |error| format!("{error}"))
(B[m[32m+            output_result.as_ref().err().map_or_else(
(B[m[32m+                || String::from("unknown execution error"),
(B[m[32m+                |error| format!("{error}")
(B[m[32m+            )
(B[m         );
         let Ok(run_output) = output_result else {
             return;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_join_path_components.rs:247:
         ];
 
         assert_eq!(
[31m-            lines,
(B[m[31m-            expected,
(B[m[32m+            lines, expected,
(B[m             "join_path_components should satisfy the canonical join matrix"
         );
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_markdown_roundtrip.rs:1:
 #![cfg(feature = "integration")]
 
[32m+use super::fs_helpers::{
(B[m[32m+    FsStateGuard, assert_workspace_empty, fs_project_root, strip_crlf, unique_probe_target_dir,
(B[m[32m+};
(B[m use super::*;
[31m-use super::fs_helpers::{FsStateGuard, assert_workspace_empty, fs_project_root, strip_crlf, unique_probe_target_dir};
(B[m use serial_test::serial;
 
 #[cfg(feature = "integration")]
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_markdown_roundtrip.rs:28:
         );
 
         let temp_dir = unique_probe_target_dir("markdown-roundtrip-fixture");
[31m-        let binary_result = opalescent::compiler::compile_project(
(B[m[31m-            &project_dir,
(B[m[31m-            &temp_dir,
(B[m[31m-            &TargetTriple::host(),
(B[m[31m-        );
(B[m[32m+        let binary_result =
(B[m[32m+            opalescent::compiler::compile_project(&project_dir, &temp_dir, &TargetTriple::host());
(B[m         assert!(
             binary_result.is_ok(),
             "fs-markdown-roundtrip fixture should compile into a binary: {}",
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_markdown_roundtrip.rs:39:
[31m-            binary_result
(B[m[31m-                .as_ref()
(B[m[31m-                .err()
(B[m[31m-                .map_or_else(|| String::from("unknown compile error"), ToString::to_string)
(B[m[32m+            binary_result.as_ref().err().map_or_else(
(B[m[32m+                || String::from("unknown compile error"),
(B[m[32m+                ToString::to_string
(B[m[32m+            )
(B[m         );
         let Ok(binary_path) = binary_result else {
             return;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_markdown_roundtrip.rs:49:
         assert!(
             output_result.is_ok(),
             "fs-markdown-roundtrip compiled binary should execute: {}",
[31m-            output_result
(B[m[31m-                .as_ref()
(B[m[31m-                .err()
(B[m[31m-                .map_or_else(|| String::from("unknown execution error"), ToString::to_string)
(B[m[32m+            output_result.as_ref().err().map_or_else(
(B[m[32m+                || String::from("unknown execution error"),
(B[m[32m+                ToString::to_string
(B[m[32m+            )
(B[m         );
         let Ok(run_output) = output_result else {
             return;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_metadata.rs:86:
         let stdout = String::from_utf8_lossy(&compile.stdout);
         return Err(format!(
             "t25 metadata harness compile should succeed, status={:?}, stdout='{}', stderr='{}'",
[31m-            compile.status.code(), stdout, stderr
(B[m[32m+            compile.status.code(),
(B[m[32m+            stdout,
(B[m[32m+            stderr
(B[m         ));
     }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_metadata.rs:132:
     )
 }
 
[31m-fn compile_and_run_inline_program(source: &str, temp_dir: &Path) -> Result<std::process::Output, String> {
(B[m[32m+fn compile_and_run_inline_program(
(B[m[32m+    source: &str,
(B[m[32m+    temp_dir: &Path,
(B[m[32m+) -> Result<std::process::Output, String> {
(B[m     let binary_result = compile_program(
         Path::new("test-projects/_t25_fs_metadata/src/main.op"),
         source,
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_metadata.rs:212:
                 };
             }
         }
[31m-        let is_file = is_file.ok_or_else(|| format!("metadata output missing is_file line: {stdout}"))?;
(B[m[32m+        let is_file =
(B[m[32m+            is_file.ok_or_else(|| format!("metadata output missing is_file line: {stdout}"))?;
(B[m 
         if size != 5 {
             return Err(format!("metadata size should be 5 bytes, got {size}"));
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_metadata.rs:219:
         }
         if mtime <= 0 {
[31m-            return Err(format!("metadata mtime should be positive unix seconds, got {mtime}"));
(B[m[32m+            return Err(format!(
(B[m[32m+                "metadata mtime should be positive unix seconds, got {mtime}"
(B[m[32m+            ));
(B[m         }
         if is_directory {
[31m-            return Err(String::from("metadata is_directory should be false for regular file"));
(B[m[32m+            return Err(String::from(
(B[m[32m+                "metadata is_directory should be false for regular file",
(B[m[32m+            ));
(B[m         }
         if !is_file {
[31m-            return Err(String::from("metadata is_file should be true for regular file"));
(B[m[32m+            return Err(String::from(
(B[m[32m+                "metadata is_file should be true for regular file",
(B[m[32m+            ));
(B[m         }
 
         Ok(())
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_metadata.rs:272:
         if combined.contains("UNEXPECTED_SUCCESS") {
             return Err(format!(
                 "remove-file-not-found probe unexpectedly succeeded, status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
         if !combined.contains("FileNotFoundError:") {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_metadata.rs:279:
             return Err(format!(
                 "remove-file-not-found output should contain FileNotFoundError prefix, status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_metadata.rs:327:
         if combined.contains("UNEXPECTED_SUCCESS") {
             return Err(format!(
                 "remove-file-isdir probe unexpectedly succeeded, status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
         if !combined.contains("IsADirectoryError:") {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_metadata.rs:334:
             return Err(format!(
                 "remove-file-isdir output should contain IsADirectoryError prefix, status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_metadata.rs:400:
         if combined.contains("UNEXPECTED_SUCCESS") {
             return Err(format!(
                 "remove-file-perm probe unexpectedly succeeded, status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
         if !combined.contains("PermissionDeniedError:") {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_metadata.rs:407:
             return Err(format!(
                 "remove-file-perm output should contain PermissionDeniedError prefix, status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_normalize_path.rs:1:
 #![cfg(feature = "integration")]
 
[32m+use super::fs_helpers::{
(B[m[32m+    FsStateGuard, assert_workspace_empty, strip_crlf, unique_probe_target_dir,
(B[m[32m+};
(B[m use super::*;
[31m-use super::fs_helpers::{FsStateGuard, assert_workspace_empty, strip_crlf, unique_probe_target_dir};
(B[m use serial_test::serial;
 
 fn stringify_error<E: core::fmt::Display>(error: E) -> String {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_normalize_path.rs:100:
         ];
 
         assert_eq!(
[31m-            lines,
(B[m[31m-            expected,
(B[m[32m+            lines, expected,
(B[m             "normalize_path should satisfy the canonical lexical normalization matrix"
         );
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_normalize_path.rs:142:
         let project_dir = cwd_path.join("test-projects/_fs_normalize_path");
         let temp_dir = unique_probe_target_dir("normalize-fixture-showcase");
 
[31m-        let binary_result = opalescent::compiler::compile_project(
(B[m[31m-            &project_dir,
(B[m[31m-            &temp_dir,
(B[m[31m-            &TargetTriple::host(),
(B[m[31m-        );
(B[m[32m+        let binary_result =
(B[m[32m+            opalescent::compiler::compile_project(&project_dir, &temp_dir, &TargetTriple::host());
(B[m         assert!(
             binary_result.is_ok(),
             "_fs_normalize_path fixture should compile into a binary: {}",
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_normalize_path.rs:153:
[31m-            binary_result
(B[m[31m-                .as_ref()
(B[m[31m-                .err()
(B[m[31m-                .map_or_else(|| String::from("unknown compile error"), |error| format!("{error}"))
(B[m[32m+            binary_result.as_ref().err().map_or_else(
(B[m[32m+                || String::from("unknown compile error"),
(B[m[32m+                |error| format!("{error}")
(B[m[32m+            )
(B[m         );
         let Ok(binary_path) = binary_result else {
             return;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_normalize_path.rs:163:
         assert!(
             output_result.is_ok(),
             "_fs_normalize_path compiled binary should execute: {}",
[31m-            output_result
(B[m[31m-                .as_ref()
(B[m[31m-                .err()
(B[m[31m-                .map_or_else(|| String::from("unknown execution error"), |error| format!("{error}"))
(B[m[32m+            output_result.as_ref().err().map_or_else(
(B[m[32m+                || String::from("unknown execution error"),
(B[m[32m+                |error| format!("{error}")
(B[m[32m+            )
(B[m         );
         let Ok(run_output) = output_result else {
             return;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_normalize_path.rs:173:
         };
 
         let stdout = strip_crlf(&String::from_utf8_lossy(&run_output.stdout));
[31m-        let lines: Vec<&str> = stdout
(B[m[31m-            .lines()
(B[m[31m-            .map(str::trim)
(B[m[31m-            .collect();
(B[m[32m+        let lines: Vec<&str> = stdout.lines().map(str::trim).collect();
(B[m 
         let expected = vec![
             "a//b -> a/b",
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_normalize_path.rs:187:
             "/a/b/../../.. -> empty-sentinel",
         ];
         assert_eq!(
[31m-            lines,
(B[m[31m-            expected,
(B[m[32m+            lines, expected,
(B[m             "_fs_normalize_path fixture should print the 6 canonical normalize_path showcase lines"
         );
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_normalize_path.rs:239:
         assert!(
             binary_result.is_ok(),
             "normalize root-escape source should compile into a binary: {}",
[31m-            binary_result
(B[m[31m-                .as_ref()
(B[m[31m-                .err()
(B[m[31m-                .map_or_else(|| String::from("unknown compile error"), |error| format!("{error}"))
(B[m[32m+            binary_result.as_ref().err().map_or_else(
(B[m[32m+                || String::from("unknown compile error"),
(B[m[32m+                |error| format!("{error}")
(B[m[32m+            )
(B[m         );
         let Ok(binary_path) = binary_result else {
             return;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_normalize_path.rs:252:
         assert!(
             output_result.is_ok(),
             "normalize root-escape compiled binary should execute: {}",
[31m-            output_result
(B[m[31m-                .as_ref()
(B[m[31m-                .err()
(B[m[31m-                .map_or_else(|| String::from("unknown execution error"), |error| format!("{error}"))
(B[m[32m+            output_result.as_ref().err().map_or_else(
(B[m[32m+                || String::from("unknown execution error"),
(B[m[32m+                |error| format!("{error}")
(B[m[32m+            )
(B[m         );
         let Ok(run_output) = output_result else {
             return;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_path_from.rs:1:
 #![cfg(feature = "integration")]
 
[32m+use super::fs_helpers::{
(B[m[32m+    FsStateGuard, assert_workspace_empty, strip_crlf, unique_probe_target_dir,
(B[m[32m+};
(B[m use super::*;
[31m-use super::fs_helpers::{FsStateGuard, assert_workspace_empty, strip_crlf, unique_probe_target_dir};
(B[m use serial_test::serial;
 
 fn stringify_error<E: core::fmt::Display>(error: E) -> String {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_path_from.rs:91:
             "nested_name=world",
         ];
         assert_eq!(
[31m-            lines,
(B[m[31m-            expected,
(B[m[32m+            lines, expected,
(B[m             "path_from should preserve non-empty input and emit empty sentinel for empty input"
         );
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_path_from.rs:133:
         let project_dir = cwd_path.join("test-projects/_fs_path_from");
         let temp_dir = unique_probe_target_dir("path-from-smoke");
 
[31m-        let binary_result = opalescent::compiler::compile_project(
(B[m[31m-            &project_dir,
(B[m[31m-            &temp_dir,
(B[m[31m-            &TargetTriple::host(),
(B[m[31m-        );
(B[m[32m+        let binary_result =
(B[m[32m+            opalescent::compiler::compile_project(&project_dir, &temp_dir, &TargetTriple::host());
(B[m         assert!(
             binary_result.is_ok(),
             "_fs_path_from fixture should compile into a binary: {}",
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_path_from.rs:144:
[31m-            binary_result
(B[m[31m-                .as_ref()
(B[m[31m-                .err()
(B[m[31m-                .map_or_else(|| String::from("unknown compile error"), |error| format!("{error}"))
(B[m[32m+            binary_result.as_ref().err().map_or_else(
(B[m[32m+                || String::from("unknown compile error"),
(B[m[32m+                |error| format!("{error}")
(B[m[32m+            )
(B[m         );
         let Ok(binary_path) = binary_result else {
             return;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_path_from.rs:154:
         assert!(
             output_result.is_ok(),
             "_fs_path_from compiled binary should execute: {}",
[31m-            output_result
(B[m[31m-                .as_ref()
(B[m[31m-                .err()
(B[m[31m-                .map_or_else(|| String::from("unknown execution error"), |error| format!("{error}"))
(B[m[32m+            output_result.as_ref().err().map_or_else(
(B[m[32m+                || String::from("unknown execution error"),
(B[m[32m+                |error| format!("{error}")
(B[m[32m+            )
(B[m         );
         let Ok(run_output) = output_result else {
             return;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_path_from.rs:164:
         };
 
         let stdout = strip_crlf(&String::from_utf8_lossy(&run_output.stdout));
[31m-        let lines: Vec<&str> = stdout
(B[m[31m-            .lines()
(B[m[31m-            .map(str::trim)
(B[m[31m-            .collect();
(B[m[32m+        let lines: Vec<&str> = stdout.lines().map(str::trim).collect();
(B[m 
         let expected = vec![
             "error: invalid",
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_path_from.rs:176:
             "path=hello/",
         ];
         assert_eq!(
[31m-            lines,
(B[m[31m-            expected,
(B[m[32m+            lines, expected,
(B[m             "_fs_path_from should exercise 4 cases: empty (error), simple, nested, trailing slash"
         );
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_path_helpers_query.rs:1:
 #![cfg(feature = "integration")]
 
[32m+use super::fs_helpers::{
(B[m[32m+    FsStateGuard, assert_workspace_empty, strip_crlf, unique_probe_target_dir,
(B[m[32m+};
(B[m use super::*;
[31m-use super::fs_helpers::{FsStateGuard, assert_workspace_empty, strip_crlf, unique_probe_target_dir};
(B[m use serial_test::serial;
 
 fn stringify_error<E: core::fmt::Display>(error: E) -> String {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_path_helpers_query.rs:30:
         let project_dir = cwd_path.join("test-projects/_fs_path_helpers_query");
         let temp_dir = unique_probe_target_dir("path-helpers-query-fixture");
 
[31m-        let binary_result = opalescent::compiler::compile_project(
(B[m[31m-            &project_dir,
(B[m[31m-            &temp_dir,
(B[m[31m-            &TargetTriple::host(),
(B[m[31m-        );
(B[m[32m+        let binary_result =
(B[m[32m+            opalescent::compiler::compile_project(&project_dir, &temp_dir, &TargetTriple::host());
(B[m         assert!(
             binary_result.is_ok(),
             "_fs_path_helpers_query fixture should compile into a binary: {}",
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_path_helpers_query.rs:61:
         };
 
         let stdout = strip_crlf(&String::from_utf8_lossy(&run_output.stdout));
[31m-        let lines: Vec<&str> = stdout
(B[m[31m-            .lines()
(B[m[31m-            .map(str::trim)
(B[m[31m-            .collect();
(B[m[32m+        let lines: Vec<&str> = stdout.lines().map(str::trim).collect();
(B[m 
         let expected = vec![
             "/home/user/doc.pdf: ext=pdf, name=doc.pdf, parent=/home/user",
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_path_helpers_query.rs:75:
         ];
 
         assert_eq!(
[31m-            lines,
(B[m[31m-            expected,
(B[m[32m+            lines, expected,
(B[m             "_fs_path_helpers_query fixture should print the exact 5-case helper matrix"
         );
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_path_manipulation.rs:1:
 #![cfg(feature = "integration")]
 
[32m+use super::fs_helpers::{
(B[m[32m+    FsStateGuard, assert_workspace_empty, strip_crlf, unique_probe_target_dir,
(B[m[32m+};
(B[m use super::*;
[31m-use super::fs_helpers::{FsStateGuard, assert_workspace_empty, strip_crlf, unique_probe_target_dir};
(B[m use serial_test::serial;
 
 #[cfg(feature = "integration")]
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_path_manipulation.rs:26:
         let project_dir = cwd_path.join("test-projects/fs-path-manipulation");
         let temp_dir = unique_probe_target_dir("path-manipulation-fixture");
 
[31m-        let binary_result = opalescent::compiler::compile_project(
(B[m[31m-            &project_dir,
(B[m[31m-            &temp_dir,
(B[m[31m-            &TargetTriple::host(),
(B[m[31m-        );
(B[m[32m+        let binary_result =
(B[m[32m+            opalescent::compiler::compile_project(&project_dir, &temp_dir, &TargetTriple::host());
(B[m         assert!(
             binary_result.is_ok(),
             "fs-path-manipulation fixture should compile into a binary: {}",
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_path_manipulation.rs:37:
[31m-            binary_result
(B[m[31m-                .as_ref()
(B[m[31m-                .err()
(B[m[31m-                .map_or_else(|| String::from("unknown compile error"), |error| format!("{error}"))
(B[m[32m+            binary_result.as_ref().err().map_or_else(
(B[m[32m+                || String::from("unknown compile error"),
(B[m[32m+                |error| format!("{error}")
(B[m[32m+            )
(B[m         );
         let Ok(binary_path) = binary_result else {
             return;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_path_manipulation.rs:47:
         assert!(
             output_result.is_ok(),
             "fs-path-manipulation compiled binary should execute: {}",
[31m-            output_result
(B[m[31m-                .as_ref()
(B[m[31m-                .err()
(B[m[31m-                .map_or_else(|| String::from("unknown execution error"), |error| format!("{error}"))
(B[m[32m+            output_result.as_ref().err().map_or_else(
(B[m[32m+                || String::from("unknown execution error"),
(B[m[32m+                |error| format!("{error}")
(B[m[32m+            )
(B[m         );
         let Ok(run_output) = output_result else {
             return;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_predicates.rs:26:
     )
 }
 
[31m-fn compile_and_run_inline_program(source: &str, temp_dir: &Path) -> Result<std::process::Output, String> {
(B[m[32m+fn compile_and_run_inline_program(
(B[m[32m+    source: &str,
(B[m[32m+    temp_dir: &Path,
(B[m[32m+) -> Result<std::process::Output, String> {
(B[m     let binary_result = compile_program(
         Path::new("test-projects/_t25_fs_predicates/src/main.op"),
         source,
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_predicates.rs:72:
         }
     }
 
[31m-    let exists = exists.ok_or_else(|| format!("predicate output missing exists= line: {stdout}"))?;
(B[m[32m+    let exists =
(B[m[32m+        exists.ok_or_else(|| format!("predicate output missing exists= line: {stdout}"))?;
(B[m     let file = file.ok_or_else(|| format!("predicate output missing file= line: {stdout}"))?;
     let dir = dir.ok_or_else(|| format!("predicate output missing dir= line: {stdout}"))?;
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_bytes.rs:26:
     )
 }
 
[31m-fn compile_and_run_inline_program(source: &str, temp_dir: &Path) -> Result<std::process::Output, String> {
(B[m[32m+fn compile_and_run_inline_program(
(B[m[32m+    source: &str,
(B[m[32m+    temp_dir: &Path,
(B[m[32m+) -> Result<std::process::Output, String> {
(B[m     let binary_result = compile_program(
         Path::new("test-projects/_t16_read_bytes/src/main.op"),
         source,
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_bytes.rs:99:
         let count_offset = count_start
             .checked_add("COUNT_START".len())
             .ok_or_else(|| format!("COUNT_START marker offset overflowed, got:\n{stdout}"))?;
[31m-        let count_tail = stdout
(B[m[31m-            .get(count_offset..)
(B[m[31m-            .ok_or_else(|| format!("COUNT_START marker should align to a UTF-8 boundary, got:\n{stdout}"))?;
(B[m[32m+        let count_tail = stdout.get(count_offset..).ok_or_else(|| {
(B[m[32m+            format!("COUNT_START marker should align to a UTF-8 boundary, got:\n{stdout}")
(B[m[32m+        })?;
(B[m         let count_end_rel = count_tail
             .find("COUNT_END")
             .ok_or_else(|| format!("stdout should include COUNT_END marker, got:\n{stdout}"))?;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_bytes.rs:108:
         let count_payload = count_tail
             .get(..count_end_rel)
[31m-            .ok_or_else(|| format!("COUNT_END marker should align to a UTF-8 boundary, got:\n{stdout}"))?
(B[m[32m+            .ok_or_else(|| {
(B[m[32m+                format!("COUNT_END marker should align to a UTF-8 boundary, got:\n{stdout}")
(B[m[32m+            })?
(B[m             .trim_matches(|c| c == '\n' || c == '\r' || c == ' ' || c == '\t');
 
         if count_payload != "256" {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_bytes.rs:122:
         let hex_offset = hex_start
             .checked_add("HEX_START".len())
             .ok_or_else(|| format!("HEX_START marker offset overflowed, got:\n{stdout}"))?;
[31m-        let hex_tail = stdout
(B[m[31m-            .get(hex_offset..)
(B[m[31m-            .ok_or_else(|| format!("HEX_START marker should align to a UTF-8 boundary, got:\n{stdout}"))?;
(B[m[32m+        let hex_tail = stdout.get(hex_offset..).ok_or_else(|| {
(B[m[32m+            format!("HEX_START marker should align to a UTF-8 boundary, got:\n{stdout}")
(B[m[32m+        })?;
(B[m         let hex_end_rel = hex_tail
             .find("HEX_END")
             .ok_or_else(|| format!("stdout should include HEX_END marker, got:\n{stdout}"))?;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_bytes.rs:131:
         let hex_payload = hex_tail
             .get(..hex_end_rel)
[31m-            .ok_or_else(|| format!("HEX_END marker should align to a UTF-8 boundary, got:\n{stdout}"))?
(B[m[32m+            .ok_or_else(|| {
(B[m[32m+                format!("HEX_END marker should align to a UTF-8 boundary, got:\n{stdout}")
(B[m[32m+            })?
(B[m             .trim_matches(|c| c == '\n' || c == '\r' || c == ' ' || c == '\t');
 
         if hex_payload.len() != 512 {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_bytes.rs:216:
         let count_offset = count_start
             .checked_add("COUNT_START".len())
             .ok_or_else(|| format!("COUNT_START marker offset overflowed, got:\n{stdout}"))?;
[31m-        let count_tail = stdout
(B[m[31m-            .get(count_offset..)
(B[m[31m-            .ok_or_else(|| format!("COUNT_START marker should align to a UTF-8 boundary, got:\n{stdout}"))?;
(B[m[32m+        let count_tail = stdout.get(count_offset..).ok_or_else(|| {
(B[m[32m+            format!("COUNT_START marker should align to a UTF-8 boundary, got:\n{stdout}")
(B[m[32m+        })?;
(B[m         let count_end_rel = count_tail
             .find("COUNT_END")
             .ok_or_else(|| format!("stdout should include COUNT_END marker, got:\n{stdout}"))?;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_bytes.rs:225:
         let count_payload = count_tail
             .get(..count_end_rel)
[31m-            .ok_or_else(|| format!("COUNT_END marker should align to a UTF-8 boundary, got:\n{stdout}"))?
(B[m[32m+            .ok_or_else(|| {
(B[m[32m+                format!("COUNT_END marker should align to a UTF-8 boundary, got:\n{stdout}")
(B[m[32m+            })?
(B[m             .trim_matches(|c| c == '\n' || c == '\r' || c == ' ' || c == '\t');
 
         if count_payload != "0" {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_bytes.rs:239:
         let hex_offset = hex_start
             .checked_add("HEX_START".len())
             .ok_or_else(|| format!("HEX_START marker offset overflowed, got:\n{stdout}"))?;
[31m-        let hex_tail = stdout
(B[m[31m-            .get(hex_offset..)
(B[m[31m-            .ok_or_else(|| format!("HEX_START marker should align to a UTF-8 boundary, got:\n{stdout}"))?;
(B[m[32m+        let hex_tail = stdout.get(hex_offset..).ok_or_else(|| {
(B[m[32m+            format!("HEX_START marker should align to a UTF-8 boundary, got:\n{stdout}")
(B[m[32m+        })?;
(B[m         let hex_end_rel = hex_tail
             .find("HEX_END")
             .ok_or_else(|| format!("stdout should include HEX_END marker, got:\n{stdout}"))?;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_bytes.rs:248:
         let hex_payload = hex_tail
             .get(..hex_end_rel)
[31m-            .ok_or_else(|| format!("HEX_END marker should align to a UTF-8 boundary, got:\n{stdout}"))?
(B[m[32m+            .ok_or_else(|| {
(B[m[32m+                format!("HEX_END marker should align to a UTF-8 boundary, got:\n{stdout}")
(B[m[32m+            })?
(B[m             .trim_matches(|c| c == '\n' || c == '\r' || c == ' ' || c == '\t');
 
         if !hex_payload.is_empty() {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_bytes.rs:300:
         if combined.contains("UNEXPECTED_SUCCESS") {
             return Err(format!(
                 "missing-path bytes probe unexpectedly succeeded, status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
         if !combined.contains("FileNotFoundError:") {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_bytes.rs:307:
             return Err(format!(
                 "missing-path bytes output should contain 'FileNotFoundError:', status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_bytes.rs:352:
         fs::write(&fixture_file, [0xAA_u8, 0xBB, 0xCC])
             .map_err(|e| format!("permission fixture file should be created: {e}"))?;
 
[31m-        let lock_permissions = fs::set_permissions(&fixture_file, fs::Permissions::from_mode(0o000));
(B[m[32m+        let lock_permissions =
(B[m[32m+            fs::set_permissions(&fixture_file, fs::Permissions::from_mode(0o000));
(B[m         if let Err(error) = lock_permissions {
             return Err(format!(
                 "permission fixture should be chmod 000 for denied read test: {error}"
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_bytes.rs:362:
         let source = build_read_bytes_error_source(&fixture_file.to_string_lossy());
         let run_output = compile_and_run_inline_program(&source, &temp_dir)?;
 
[31m-        let restore_result =
(B[m[31m-            fs::set_permissions(&fixture_file, fs::Permissions::from_mode(0o644));
(B[m[32m+        let restore_result = fs::set_permissions(&fixture_file, fs::Permissions::from_mode(0o644));
(B[m         if let Err(error) = restore_result {
             return Err(format!(
                 "permission fixture permissions should be restored after test run: {error}"
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_bytes.rs:376:
         if combined.contains("UNEXPECTED_SUCCESS") {
             return Err(format!(
                 "permission-denied bytes probe unexpectedly succeeded, status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
         if !combined.contains("PermissionDeniedError:") {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_bytes.rs:383:
             return Err(format!(
                 "permission-denied bytes output should contain 'PermissionDeniedError:', status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_bytes.rs:433:
         if combined.contains("UNEXPECTED_SUCCESS") {
             return Err(format!(
                 "is-directory bytes probe unexpectedly succeeded, status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
         if !combined.contains("IsADirectoryError:") {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_bytes.rs:440:
             return Err(format!(
                 "directory-path bytes output should contain 'IsADirectoryError:', status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_bytes.rs:481:
             .to_digit(16)
             .ok_or_else(|| format!("invalid hex character '{low}'"))?;
         let combined = (hi << 4_u32) | lo;
[31m-        let byte = u8::try_from(combined)
(B[m[31m-            .map_err(|error| format!("hex byte value should fit into u8, got {combined}: {error}"))?;
(B[m[32m+        let byte = u8::try_from(combined).map_err(|error| {
(B[m[32m+            format!("hex byte value should fit into u8, got {combined}: {error}")
(B[m[32m+        })?;
(B[m         out.push(byte);
     }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_first_line.rs:38:
     )
 }
 
[31m-fn compile_and_run_inline_program(source: &str, temp_dir: &Path) -> Result<std::process::Output, String> {
(B[m[32m+fn compile_and_run_inline_program(
(B[m[32m+    source: &str,
(B[m[32m+    temp_dir: &Path,
(B[m[32m+) -> Result<std::process::Output, String> {
(B[m     let binary_result = compile_program(
         Path::new("test-projects/_t18_read_first_line/src/main.op"),
         source,
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_first_line.rs:71:
         .checked_add(start_marker.len())
         .ok_or_else(|| format!("stdout marker offset overflowed, got:\n{stdout}"))?;
 
[31m-    let tail = stdout
(B[m[31m-        .get(content_start..)
(B[m[31m-        .ok_or_else(|| format!("stdout marker offset should be a valid UTF-8 boundary, got:\n{stdout}"))?;
(B[m[32m+    let tail = stdout.get(content_start..).ok_or_else(|| {
(B[m[32m+        format!("stdout marker offset should be a valid UTF-8 boundary, got:\n{stdout}")
(B[m[32m+    })?;
(B[m     let end_rel = tail
         .find(end_marker)
         .ok_or_else(|| format!("stdout should contain end marker, got:\n{stdout}"))?;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_first_line.rs:80:
[31m-    let mut extracted = tail
(B[m[31m-        .get(..end_rel)
(B[m[31m-        .ok_or_else(|| format!("stdout end marker should align to a UTF-8 boundary, got:\n{stdout}"))?;
(B[m[32m+    let mut extracted = tail.get(..end_rel).ok_or_else(|| {
(B[m[32m+        format!("stdout end marker should align to a UTF-8 boundary, got:\n{stdout}")
(B[m[32m+    })?;
(B[m 
     if let Some(rest) = extracted.strip_prefix("\r\n") {
         extracted = rest;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_first_line.rs:143:
         let stdout = String::from_utf8_lossy(&compile.stdout);
         return Err(format!(
             "t18 harness compile should succeed, status={:?}, stdout='{}', stderr='{}'",
[31m-            compile.status.code(), stdout, stderr
(B[m[32m+            compile.status.code(),
(B[m[32m+            stdout,
(B[m[32m+            stderr
(B[m         ));
     }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_first_line.rs:178:
         if combined.contains("UNEXPECTED_SUCCESS") {
             return Err(format!(
                 "empty file probe unexpectedly succeeded, status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
         if !combined.contains("OffsetOutOfRangeError: file is empty") {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_first_line.rs:185:
             return Err(format!(
                 "empty file output should contain 'OffsetOutOfRangeError: file is empty', status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_first_line.rs:410:
         if combined.contains("UNEXPECTED_SUCCESS") {
             return Err(format!(
                 "permission-denied probe unexpectedly succeeded, status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
         if !combined.contains("PermissionDeniedError:") {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_first_line.rs:417:
             return Err(format!(
                 "permission-denied output should contain 'PermissionDeniedError:', status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_first_line.rs:467:
         if combined.contains("UNEXPECTED_SUCCESS") {
             return Err(format!(
                 "is-directory probe unexpectedly succeeded, status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
         if !combined.contains("IsADirectoryError:") {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_first_line.rs:474:
             return Err(format!(
                 "is-directory output should contain 'IsADirectoryError:', status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_first_line.rs:520:
         if combined.contains("UNEXPECTED_SUCCESS") {
             return Err(format!(
                 "not-found probe unexpectedly succeeded, status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
         if !combined.contains("FileNotFoundError:") {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_first_line.rs:527:
             return Err(format!(
                 "not-found output should contain 'FileNotFoundError:', status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_first_line.rs:613:
     drop(fs::remove_file(&fixture_file));
     drop(fs::remove_dir_all(&fixture_dir));
     drop(fs::remove_file(harness_dir.join("read_first_line_harness")));
[31m-    drop(fs::remove_file(harness_dir.join("read_first_line_harness.c")));
(B[m[32m+    drop(fs::remove_file(
(B[m[32m+        harness_dir.join("read_first_line_harness.c"),
(B[m[32m+    ));
(B[m     drop(fs::remove_dir_all(&harness_dir));
 
     let failure_message = match execution_result {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_lines.rs:70:
         let stdout = String::from_utf8_lossy(&compile.stdout);
         return Err(format!(
             "t17 harness compile should succeed, status={:?}, stdout='{}', stderr='{}'",
[31m-            compile.status.code(), stdout, stderr
(B[m[32m+            compile.status.code(),
(B[m[32m+            stdout,
(B[m[32m+            stderr
(B[m         ));
     }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_lines.rs:98:
         let stdout = String::from_utf8_lossy(&output.stdout);
         return Err(format!(
             "t17 harness should exit 0, status={:?}, stdout='{}', stderr='{}'",
[31m-            output.status.code(), stdout, stderr
(B[m[32m+            output.status.code(),
(B[m[32m+            stdout,
(B[m[32m+            stderr
(B[m         ));
     }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_lines.rs:155:
 
         let expected = ["a", "b", "c"];
         if lines != expected {
[31m-            return Err(format!("LF input should produce a,b,c lines, got {lines:?}"));
(B[m[32m+            return Err(format!(
(B[m[32m+                "LF input should produce a,b,c lines, got {lines:?}"
(B[m[32m+            ));
(B[m         }
 
         Ok(())
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_lines.rs:194:
 
         let expected = ["a", "b"];
         if lines != expected {
[31m-            return Err(format!("CRLF input should normalize and split to a,b, got {lines:?}"));
(B[m[32m+            return Err(format!(
(B[m[32m+                "CRLF input should normalize and split to a,b, got {lines:?}"
(B[m[32m+            ));
(B[m         }
 
         Ok(())
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_lines.rs:233:
 
         let expected = ["a", "b", "c"];
         if lines != expected {
[31m-            return Err(format!("mixed line endings should produce a,b,c, got {lines:?}"));
(B[m[32m+            return Err(format!(
(B[m[32m+                "mixed line endings should produce a,b,c, got {lines:?}"
(B[m[32m+            ));
(B[m         }
 
         Ok(())
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_lines.rs:274:
 
         let expected = ["a", "b", "c"];
         if lines != expected {
[31m-            return Err(format!("trailing newline should not append empty element, got {lines:?}"));
(B[m[32m+            return Err(format!(
(B[m[32m+                "trailing newline should not append empty element, got {lines:?}"
(B[m[32m+            ));
(B[m         }
 
         Ok(())
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_lines.rs:312:
         }
 
         if !lines.is_empty() {
[31m-            return Err(format!("empty file should produce no line entries, got {lines:?}"));
(B[m[32m+            return Err(format!(
(B[m[32m+                "empty file should produce no line entries, got {lines:?}"
(B[m[32m+            ));
(B[m         }
 
         Ok(())
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_text.rs:23:
     )
 }
 
[31m-fn compile_and_run_inline_program(source: &str, temp_dir: &Path) -> Result<std::process::Output, String> {
(B[m[32m+fn compile_and_run_inline_program(
(B[m[32m+    source: &str,
(B[m[32m+    temp_dir: &Path,
(B[m[32m+) -> Result<std::process::Output, String> {
(B[m     let binary_result = compile_program(
         Path::new("test-projects/_t15_read_text/src/main.op"),
         source,
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_text.rs:79:
         if combined.contains("UNEXPECTED_SUCCESS") {
             return Err(format!(
                 "missing-path probe unexpectedly succeeded, status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
         if !combined.contains("FileNotFoundError:") {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_text.rs:86:
             return Err(format!(
                 "missing-path output should contain 'FileNotFoundError:', status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_text.rs:135:
         fs::write(&fixture_file, "permission denied fixture")
             .map_err(|e| format!("permission fixture file should be created: {e}"))?;
 
[31m-        let lock_permissions = fs::set_permissions(&fixture_file, fs::Permissions::from_mode(0o000));
(B[m[32m+        let lock_permissions =
(B[m[32m+            fs::set_permissions(&fixture_file, fs::Permissions::from_mode(0o000));
(B[m         if let Err(error) = lock_permissions {
             return Err(format!(
                 "permission fixture should be chmod 000 for denied read test: {error}"
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_text.rs:145:
         let source = build_read_text_error_source(&fixture_file.to_string_lossy());
         let run_output = compile_and_run_inline_program(&source, &temp_dir)?;
 
[31m-        let restore_result =
(B[m[31m-            fs::set_permissions(&fixture_file, fs::Permissions::from_mode(0o644));
(B[m[32m+        let restore_result = fs::set_permissions(&fixture_file, fs::Permissions::from_mode(0o644));
(B[m         if let Err(error) = restore_result {
             return Err(format!(
                 "permission fixture permissions should be restored after test run: {error}"
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_text.rs:159:
         if combined.contains("UNEXPECTED_SUCCESS") {
             return Err(format!(
                 "permission-denied probe unexpectedly succeeded, status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
         if !combined.contains("PermissionDeniedError:") {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_text.rs:166:
             return Err(format!(
                 "permission-denied output should contain 'PermissionDeniedError:', status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_text.rs:216:
         if combined.contains("UNEXPECTED_SUCCESS") {
             return Err(format!(
                 "is-directory probe unexpectedly succeeded, status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
         if !combined.contains("IsADirectoryError:") {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_text.rs:223:
             return Err(format!(
                 "directory-path output should contain 'IsADirectoryError:', status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_text.rs:275:
         if combined.contains("UNEXPECTED_SUCCESS") {
             return Err(format!(
                 "invalid-utf8 probe unexpectedly succeeded, status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
         if !combined.contains("InvalidUtf8Error: 0") {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_text.rs:282:
             return Err(format!(
                 "invalid-utf8 output should contain 'InvalidUtf8Error: 0', status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_text.rs:346:
             .ok_or_else(|| format!("success stdout should contain start marker, got:\n{stdout}"))?;
         let content_start = start_idx + start_marker.len();
 
[31m-        let tail = stdout
(B[m[31m-            .get(content_start..)
(B[m[31m-            .ok_or_else(|| format!("success stdout marker offset should be a valid UTF-8 boundary, got:\n{stdout}"))?;
(B[m[32m+        let tail = stdout.get(content_start..).ok_or_else(|| {
(B[m[32m+            format!("success stdout marker offset should be a valid UTF-8 boundary, got:\n{stdout}")
(B[m[32m+        })?;
(B[m         let end_rel = tail
             .find(end_marker)
             .ok_or_else(|| format!("success stdout should contain end marker, got:\n{stdout}"))?;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_text.rs:355:
[31m-        let mut extracted = tail
(B[m[31m-            .get(..end_rel)
(B[m[31m-            .ok_or_else(|| format!("success stdout end marker should align to a UTF-8 boundary, got:\n{stdout}"))?;
(B[m[32m+        let mut extracted = tail.get(..end_rel).ok_or_else(|| {
(B[m[32m+            format!("success stdout end marker should align to a UTF-8 boundary, got:\n{stdout}")
(B[m[32m+        })?;
(B[m 
         if let Some(rest) = extracted.strip_prefix("\r\n") {
             extracted = rest;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_text_lines.rs:1:
 #![cfg(feature = "integration")]
 
[32m+use super::fs_helpers::{
(B[m[32m+    FsStateGuard, assert_workspace_empty, strip_crlf, unique_probe_target_dir,
(B[m[32m+};
(B[m use super::*;
[31m-use super::fs_helpers::{FsStateGuard, assert_workspace_empty, strip_crlf, unique_probe_target_dir};
(B[m use serial_test::serial;
 
 #[cfg(feature = "integration")]
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_text_lines.rs:26:
         let project_dir = cwd_path.join("test-projects/_fs_read_text_lines");
         let temp_dir = unique_probe_target_dir("read-text-lines-fixture");
 
[31m-        let binary_result = opalescent::compiler::compile_project(
(B[m[31m-            &project_dir,
(B[m[31m-            &temp_dir,
(B[m[31m-            &TargetTriple::host(),
(B[m[31m-        );
(B[m[32m+        let binary_result =
(B[m[32m+            opalescent::compiler::compile_project(&project_dir, &temp_dir, &TargetTriple::host());
(B[m         assert!(
             binary_result.is_ok(),
             "_fs_read_text_lines fixture should compile into a binary: {}",
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_text_lines.rs:37:
[31m-            binary_result
(B[m[31m-                .as_ref()
(B[m[31m-                .err()
(B[m[31m-                .map_or_else(|| String::from("unknown compile error"), |error| format!("{error}"))
(B[m[32m+            binary_result.as_ref().err().map_or_else(
(B[m[32m+                || String::from("unknown compile error"),
(B[m[32m+                |error| format!("{error}")
(B[m[32m+            )
(B[m         );
         let Ok(binary_path) = binary_result else {
             return;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_text_lines.rs:47:
         assert!(
             output_result.is_ok(),
             "_fs_read_text_lines compiled binary should execute: {}",
[31m-            output_result
(B[m[31m-                .as_ref()
(B[m[31m-                .err()
(B[m[31m-                .map_or_else(|| String::from("unknown execution error"), |error| format!("{error}"))
(B[m[32m+            output_result.as_ref().err().map_or_else(
(B[m[32m+                || String::from("unknown execution error"),
(B[m[32m+                |error| format!("{error}")
(B[m[32m+            )
(B[m         );
         let Ok(run_output) = output_result else {
             return;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_read_text_lines.rs:57:
         };
 
         let stdout = strip_crlf(&String::from_utf8_lossy(&run_output.stdout));
[31m-        let lines: Vec<&str> = stdout
(B[m[31m-            .lines()
(B[m[31m-            .map(str::trim)
(B[m[31m-            .collect();
(B[m[32m+        let lines: Vec<&str> = stdout.lines().map(str::trim).collect();
(B[m 
         assert!(
             lines.contains(&"lines=4"),
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_rename_path.rs:8:
 use std::process::Command;
 use std::time::{SystemTime, UNIX_EPOCH};
 
[31m-#[cfg(unix)]
(B[m[31m-use std::os::unix::fs::PermissionsExt;
(B[m #[cfg(target_os = "linux")]
 use std::os::unix::fs::MetadataExt;
[32m+#[cfg(unix)]
(B[m[32m+use std::os::unix::fs::PermissionsExt;
(B[m 
 fn make_temp_path(label: &str) -> PathBuf {
     let nanos = SystemTime::now()
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_rename_path.rs:70:
         let stdout = String::from_utf8_lossy(&compile.stdout);
         return Err(format!(
             "t27 harness compile should succeed, status={:?}, stdout='{}', stderr='{}'",
[31m-            compile.status.code(), stdout, stderr
(B[m[32m+            compile.status.code(),
(B[m[32m+            stdout,
(B[m[32m+            stderr
(B[m         ));
     }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_rename_path.rs:77:
     Ok(harness_bin)
 }
 
[31m-fn run_rename_harness(src: &Path, dst: &Path, temp_dir: &Path) -> Result<(i32, String, String), String> {
(B[m[32m+fn run_rename_harness(
(B[m[32m+    src: &Path,
(B[m[32m+    dst: &Path,
(B[m[32m+    temp_dir: &Path,
(B[m[32m+) -> Result<(i32, String, String), String> {
(B[m     let harness_bin = build_rename_harness(temp_dir)?;
 
     let output = Command::new(&harness_bin)
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_rename_path.rs:103:
 
     let temp_dir = unique_probe_target_dir("rename-within-fs");
     let prepare = prepare_dir(&temp_dir);
[31m-    assert!(prepare.is_ok(), "t27 within-fs temp directory should be created");
(B[m[32m+    assert!(
(B[m[32m+        prepare.is_ok(),
(B[m[32m+        "t27 within-fs temp directory should be created"
(B[m[32m+    );
(B[m 
     let fixture_dir = make_temp_path("within-fs");
     let src = fixture_dir.join("from.txt");
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_rename_path.rs:142:
     drop(fs::remove_dir_all(&fixture_dir));
 
     let cleanup = cleanup_dir(&temp_dir);
[31m-    assert!(cleanup.is_ok(), "t27 within-fs temp directory should be removed");
(B[m[32m+    assert!(
(B[m[32m+        cleanup.is_ok(),
(B[m[32m+        "t27 within-fs temp directory should be removed"
(B[m[32m+    );
(B[m 
     let failure_message = match execution_result {
         Ok(()) => String::new(),
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_rename_path.rs:162:
 
     let temp_dir = unique_probe_target_dir("rename-not-found");
     let prepare = prepare_dir(&temp_dir);
[31m-    assert!(prepare.is_ok(), "t27 not-found temp directory should be created");
(B[m[32m+    assert!(
(B[m[32m+        prepare.is_ok(),
(B[m[32m+        "t27 not-found temp directory should be created"
(B[m[32m+    );
(B[m 
     let fixture_dir = make_temp_path("not-found");
     let src = fixture_dir.join("missing.txt");
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_rename_path.rs:191:
     drop(fs::remove_dir_all(&fixture_dir));
 
     let cleanup = cleanup_dir(&temp_dir);
[31m-    assert!(cleanup.is_ok(), "t27 not-found temp directory should be removed");
(B[m[32m+    assert!(
(B[m[32m+        cleanup.is_ok(),
(B[m[32m+        "t27 not-found temp directory should be removed"
(B[m[32m+    );
(B[m 
     let failure_message = match execution_result {
         Ok(()) => String::new(),
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_rename_path.rs:281:
 
     let temp_dir = unique_probe_target_dir("rename-overwrite");
     let prepare = prepare_dir(&temp_dir);
[31m-    assert!(prepare.is_ok(), "t27 overwrite temp directory should be created");
(B[m[32m+    assert!(
(B[m[32m+        prepare.is_ok(),
(B[m[32m+        "t27 overwrite temp directory should be created"
(B[m[32m+    );
(B[m 
     let fixture_dir = make_temp_path("overwrite");
     let src = fixture_dir.join("from.txt");
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_rename_path.rs:322:
     drop(fs::remove_dir_all(&fixture_dir));
 
     let cleanup = cleanup_dir(&temp_dir);
[31m-    assert!(cleanup.is_ok(), "t27 overwrite temp directory should be removed");
(B[m[32m+    assert!(
(B[m[32m+        cleanup.is_ok(),
(B[m[32m+        "t27 overwrite temp directory should be removed"
(B[m[32m+    );
(B[m 
     let failure_message = match execution_result {
         Ok(()) => String::new(),
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_rename_path.rs:343:
 
     let temp_dir = unique_probe_target_dir("rename-cross-device");
     let prepare = prepare_dir(&temp_dir);
[31m-    assert!(prepare.is_ok(), "t27 cross-device temp directory should be created");
(B[m[32m+    assert!(
(B[m[32m+        prepare.is_ok(),
(B[m[32m+        "t27 cross-device temp directory should be created"
(B[m[32m+    );
(B[m 
     let src = make_temp_path("cross-device-src");
[31m-    let dst = PathBuf::from(format!("/dev/shm/opal-t27-cross-device-{}", std::process::id()));
(B[m[32m+    let dst = PathBuf::from(format!(
(B[m[32m+        "/dev/shm/opal-t27-cross-device-{}",
(B[m[32m+        std::process::id()
(B[m[32m+    ));
(B[m 
     let execution_result: Result<(), String> = (|| {
         let dst_parent = Path::new("/dev/shm");
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_rename_path.rs:363:
             .map_err(|e| format!("cross-device source parent metadata should be readable: {e}"))?
             .dev();
         let dst_dev = fs::metadata(dst_parent)
[31m-            .map_err(|e| format!("cross-device destination parent metadata should be readable: {e}"))?
(B[m[32m+            .map_err(|e| {
(B[m[32m+                format!("cross-device destination parent metadata should be readable: {e}")
(B[m[32m+            })?
(B[m             .dev();
 
         if src_dev == dst_dev {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_rename_path.rs:383:
             ));
         }
 
[31m-        let expected = "ERR:Io: EXDEV: cross-device rename not supported (caller should copy+delete)";
(B[m[32m+        let expected =
(B[m[32m+            "ERR:Io: EXDEV: cross-device rename not supported (caller should copy+delete)";
(B[m         if !stdout.contains(expected) {
             return Err(format!(
                 "rename-cross-device should contain explicit EXDEV message '{expected}', stdout='{stdout}', stderr='{stderr}'"
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_rename_path.rs:397:
     drop(fs::remove_file(&dst));
 
     let cleanup = cleanup_dir(&temp_dir);
[31m-    assert!(cleanup.is_ok(), "t27 cross-device temp directory should be removed");
(B[m[32m+    assert!(
(B[m[32m+        cleanup.is_ok(),
(B[m[32m+        "t27 cross-device temp directory should be removed"
(B[m[32m+    );
(B[m 
     let failure_message = match execution_result {
         Ok(()) => String::new(),
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_rerunnability.rs:104:
 
         let gitignore = fs::read_to_string(&gitignore_path)?;
         if !gitignore.lines().any(|line| line == "workspace/") {
[31m-            failures.push(format!("missing workspace/ in {}", gitignore_path.display()));
(B[m[32m+            failures.push(format!(
(B[m[32m+                "missing workspace/ in {}",
(B[m[32m+                gitignore_path.display()
(B[m[32m+            ));
(B[m         }
         if !gitignore.lines().any(|line| line == "target/") {
             failures.push(format!("missing target/ in {}", gitignore_path.display()));
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_state_guard.rs:218:
     fs::create_dir_all(root.join("src"))?;
     fs::create_dir_all(root.join("tests/fixtures"))?;
 
[31m-    fs::write(root.join("src/main.op"), "entry main = f(): void => { return void }\n")?;
(B[m[32m+    fs::write(
(B[m[32m+        root.join("src/main.op"),
(B[m[32m+        "entry main = f(): void => { return void }\n",
(B[m[32m+    )?;
(B[m     fs::write(root.join("tests/fixtures/sample.txt"), "fixture\n")?;
[31m-    fs::write(root.join("opal.toml"), "name = \"guard\"\nversion = \"1.0.0\"\n")?;
(B[m[32m+    fs::write(
(B[m[32m+        root.join("opal.toml"),
(B[m[32m+        "name = \"guard\"\nversion = \"1.0.0\"\n",
(B[m[32m+    )?;
(B[m     fs::write(root.join("README.md"), "FsStateGuard smoke fixture\n")?;
     fs::write(root.join(".gitignore"), "target/\nworkspace/\n")?;
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_state_guard.rs:242:
     let project_root = project.path().to_path_buf();
 
     {
[31m-        let _guard =
(B[m[31m-            FsStateGuard::new(&project_root).expect("FsStateGuard should initialize and reset dirs");
(B[m[32m+        let _guard = FsStateGuard::new(&project_root)
(B[m[32m+            .expect("FsStateGuard should initialize and reset dirs");
(B[m 
         let target = project_root.join("target");
         let workspace = project_root.join("workspace");
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_state_guard.rs:250:
         assert!(target.is_dir(), "target/ should exist after guard init");
[31m-        assert!(workspace.is_dir(), "workspace/ should exist after guard init");
(B[m         assert!(
[32m+            workspace.is_dir(),
(B[m[32m+            "workspace/ should exist after guard init"
(B[m[32m+        );
(B[m[32m+        assert!(
(B[m             dir_is_empty(&target).expect("target emptiness should be readable"),
             "target/ should be empty after guard init"
         );
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_state_guard.rs:267:
     let target = project_root.join("target");
     let workspace = project_root.join("workspace");
     assert!(target.is_dir(), "target/ should exist after guard drop");
[31m-    assert!(workspace.is_dir(), "workspace/ should exist after guard drop");
(B[m     assert!(
[32m+        workspace.is_dir(),
(B[m[32m+        "workspace/ should exist after guard drop"
(B[m[32m+    );
(B[m[32m+    assert!(
(B[m         dir_is_empty(&target).expect("target emptiness should be readable after drop"),
         "target/ should be empty after guard drop"
     );
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_state_guard.rs:322:
 
     let target = project_root.join("target");
     let workspace = project_root.join("workspace");
[31m-    assert!(target.is_dir(), "target/ should exist after panic path drop");
(B[m     assert!(
[32m+        target.is_dir(),
(B[m[32m+        "target/ should exist after panic path drop"
(B[m[32m+    );
(B[m[32m+    assert!(
(B[m         dir_is_empty(&target).expect("target emptiness should be readable after panic path"),
         "target/ should be empty after panic path drop"
     );
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_state_guard.rs:330:
[31m-    assert!(workspace.is_dir(), "workspace/ should exist after panic path drop");
(B[m[32m+    assert!(
(B[m[32m+        workspace.is_dir(),
(B[m[32m+        "workspace/ should exist after panic path drop"
(B[m[32m+    );
(B[m     assert!(
         dir_is_empty(&workspace).expect("workspace emptiness should be readable after panic path"),
         "workspace/ should be empty after panic path drop"
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_file_bytes.rs:1:
 #![cfg(feature = "integration")]
 
[32m+use super::fs_helpers::{
(B[m[32m+    FsStateGuard, assert_workspace_empty, fs_project_root, unique_probe_target_dir,
(B[m[32m+};
(B[m use super::*;
[31m-use super::fs_helpers::{FsStateGuard, assert_workspace_empty, fs_project_root, unique_probe_target_dir};
(B[m use serial_test::serial;
 use sha2::{Digest, Sha256};
 use std::path::{Path, PathBuf};
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_file_bytes.rs:23:
 }
 
 fn bytes_hex_literal(bytes: &[u8]) -> String {
[31m-    let capacity = bytes
(B[m[31m-        .len()
(B[m[31m-        .checked_mul(2)
(B[m[31m-        .unwrap_or_default();
(B[m[32m+    let capacity = bytes.len().checked_mul(2).unwrap_or_default();
(B[m     let mut out = String::with_capacity(capacity);
     for b in bytes {
         use std::fmt::Write;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_file_bytes.rs:62:
     )
 }
 
[31m-fn compile_and_run_inline_program(source: &str, temp_dir: &Path) -> Result<std::process::Output, String> {
(B[m[32m+fn compile_and_run_inline_program(
(B[m[32m+    source: &str,
(B[m[32m+    temp_dir: &Path,
(B[m[32m+) -> Result<std::process::Output, String> {
(B[m     let binary_result = compile_program(
         Path::new("test-projects/_t21_write_file_bytes/src/main.op"),
         source,
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_file_bytes.rs:114:
         if combined.contains("UNEXPECTED_SUCCESS") {
             return Err(format!(
                 "missing-parent bytes probe unexpectedly succeeded, status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
         if !combined.contains("FileNotFoundError:") {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_file_bytes.rs:121:
             return Err(format!(
                 "missing-parent bytes output should contain 'FileNotFoundError:', status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_file_bytes.rs:186:
         if combined.contains("UNEXPECTED_SUCCESS") {
             return Err(format!(
                 "permission bytes probe unexpectedly succeeded, status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
         if !combined.contains("PermissionDeniedError:") {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_file_bytes.rs:193:
             return Err(format!(
                 "permission bytes output should contain 'PermissionDeniedError:', status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_file_bytes.rs:243:
             .map_err(|e| format!("is-directory fixture folder should be created: {e}"))?;
 
         let payload_hex = bytes_hex_literal(&[0x0A, 0x0B, 0x0C]);
[31m-        let source = build_write_bytes_error_source(&directory_path.to_string_lossy(), &payload_hex);
(B[m[32m+        let source =
(B[m[32m+            build_write_bytes_error_source(&directory_path.to_string_lossy(), &payload_hex);
(B[m         let run_output = compile_and_run_inline_program(&source, &temp_dir)?;
 
         let stderr = String::from_utf8_lossy(&run_output.stderr);
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_file_bytes.rs:252:
         if combined.contains("UNEXPECTED_SUCCESS") {
             return Err(format!(
                 "is-directory bytes probe unexpectedly succeeded, status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
         if !combined.contains("IsADirectoryError:") {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_file_bytes.rs:259:
             return Err(format!(
                 "is-directory bytes output should contain 'IsADirectoryError:', status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_file_bytes.rs:307:
 
         let expected_bytes: Vec<u8> = (0_u8..=255_u8).collect();
         let payload_hex = bytes_hex_literal(&expected_bytes);
[31m-        let source = build_write_bytes_success_source(&fixture_file.to_string_lossy(), &payload_hex);
(B[m[32m+        let source =
(B[m[32m+            build_write_bytes_success_source(&fixture_file.to_string_lossy(), &payload_hex);
(B[m         let run_output = compile_and_run_inline_program(&source, &temp_dir)?;
 
         if !run_output.status.success() {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_file_bytes.rs:321:
         let start_marker = "HEX_START";
         let end_marker = "HEX_END";
 
[31m-        let start_idx = stdout
(B[m[31m-            .find(start_marker)
(B[m[31m-            .ok_or_else(|| format!("roundtrip stdout should contain start marker, got:\n{stdout}"))?;
(B[m[32m+        let start_idx = stdout.find(start_marker).ok_or_else(|| {
(B[m[32m+            format!("roundtrip stdout should contain start marker, got:\n{stdout}")
(B[m[32m+        })?;
(B[m         let content_start = start_idx + start_marker.len();
 
[31m-        let tail = stdout
(B[m[31m-            .get(content_start..)
(B[m[31m-            .ok_or_else(|| format!("roundtrip stdout marker offset should be a valid UTF-8 boundary, got:\n{stdout}"))?;
(B[m[32m+        let tail = stdout.get(content_start..).ok_or_else(|| {
(B[m[32m+            format!(
(B[m[32m+                "roundtrip stdout marker offset should be a valid UTF-8 boundary, got:\n{stdout}"
(B[m[32m+            )
(B[m[32m+        })?;
(B[m         let end_rel = tail
             .find(end_marker)
             .ok_or_else(|| format!("roundtrip stdout should contain end marker, got:\n{stdout}"))?;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_file_bytes.rs:335:
[31m-        let mut extracted = tail
(B[m[31m-            .get(..end_rel)
(B[m[31m-            .ok_or_else(|| format!("roundtrip stdout end marker should align to a UTF-8 boundary, got:\n{stdout}"))?;
(B[m[32m+        let mut extracted = tail.get(..end_rel).ok_or_else(|| {
(B[m[32m+            format!("roundtrip stdout end marker should align to a UTF-8 boundary, got:\n{stdout}")
(B[m[32m+        })?;
(B[m 
         if let Some(rest) = extracted.strip_prefix("\r\n") {
             extracted = rest;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_file_string.rs:40:
     )
 }
 
[31m-fn compile_and_run_inline_program(source: &str, temp_dir: &Path) -> Result<std::process::Output, String> {
(B[m[32m+fn compile_and_run_inline_program(
(B[m[32m+    source: &str,
(B[m[32m+    temp_dir: &Path,
(B[m[32m+) -> Result<std::process::Output, String> {
(B[m     let binary_result = compile_program(
         Path::new("test-projects/_t20_write_file_string/src/main.op"),
         source,
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_file_string.rs:86:
         if combined.contains("UNEXPECTED_SUCCESS") {
             return Err(format!(
                 "missing-parent probe unexpectedly succeeded, status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
         if !combined.contains("FileNotFoundError:") {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_file_string.rs:93:
             return Err(format!(
                 "missing-parent output should contain 'FileNotFoundError:', status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_file_string.rs:152:
         if combined.contains("UNEXPECTED_SUCCESS") {
             return Err(format!(
                 "permission probe unexpectedly succeeded, status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
         if !combined.contains("PermissionDeniedError:") {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_file_string.rs:159:
             return Err(format!(
                 "permission output should contain 'PermissionDeniedError:', status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_file_string.rs:212:
         if combined.contains("UNEXPECTED_SUCCESS") {
             return Err(format!(
                 "is-directory probe unexpectedly succeeded, status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
         if !combined.contains("IsADirectoryError:") {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_file_string.rs:219:
             return Err(format!(
                 "is-directory output should contain 'IsADirectoryError:', status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_file_string.rs:265:
         if combined.contains("UNEXPECTED_SUCCESS") {
             return Err(format!(
                 "disk-full probe unexpectedly succeeded, status={:?}, stdout='{}', stderr='{}'",
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
         if !(combined.contains("Io:")
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_file_string.rs:272:
[31m-            && (combined.contains("No space left") || combined.contains("space left") || combined.contains("ENOSPC")))
(B[m[32m+            && (combined.contains("No space left")
(B[m[32m+                || combined.contains("space left")
(B[m[32m+                || combined.contains("ENOSPC")))
(B[m         {
             return Err(format!(
                 "disk-full output should contain IO prefix and ENOSPC text, status={:?}, stdout='{}', stderr='{}'",
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_file_string.rs:276:
[31m-                run_output.status.code(), stdout, stderr
(B[m[32m+                run_output.status.code(),
(B[m[32m+                stdout,
(B[m[32m+                stderr
(B[m             ));
         }
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_file_string.rs:314:
         fs::create_dir_all(&fixture_dir)
             .map_err(|e| format!("success fixture directory should be created: {e}"))?;
 
[31m-        let source = build_write_text_success_source(&fixture_file.to_string_lossy(), expected_text);
(B[m[32m+        let source =
(B[m[32m+            build_write_text_success_source(&fixture_file.to_string_lossy(), expected_text);
(B[m         let run_output = compile_and_run_inline_program(&source, &temp_dir)?;
 
         if !run_output.status.success() {
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_file_string.rs:335:
             .checked_add(start_marker.len())
             .ok_or_else(|| format!("success stdout marker offset overflowed, got:\n{stdout}"))?;
 
[31m-        let tail = stdout
(B[m[31m-            .get(content_start..)
(B[m[31m-            .ok_or_else(|| format!("success stdout marker offset should be a valid UTF-8 boundary, got:\n{stdout}"))?;
(B[m[32m+        let tail = stdout.get(content_start..).ok_or_else(|| {
(B[m[32m+            format!("success stdout marker offset should be a valid UTF-8 boundary, got:\n{stdout}")
(B[m[32m+        })?;
(B[m         let end_rel = tail
             .find(end_marker)
             .ok_or_else(|| format!("success stdout should contain end marker, got:\n{stdout}"))?;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_file_string.rs:344:
[31m-        let mut extracted = tail
(B[m[31m-            .get(..end_rel)
(B[m[31m-            .ok_or_else(|| format!("success stdout end marker should align to a UTF-8 boundary, got:\n{stdout}"))?;
(B[m[32m+        let mut extracted = tail.get(..end_rel).ok_or_else(|| {
(B[m[32m+            format!("success stdout end marker should align to a UTF-8 boundary, got:\n{stdout}")
(B[m[32m+        })?;
(B[m 
         if let Some(rest) = extracted.strip_prefix("\r\n") {
             extracted = rest;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_text_atomic.rs:1:
 #![cfg(feature = "integration")]
 
[32m+use super::fs_helpers::{
(B[m[32m+    FsStateGuard, assert_workspace_empty, strip_crlf, unique_probe_target_dir,
(B[m[32m+};
(B[m use super::*;
[31m-use super::fs_helpers::{FsStateGuard, assert_workspace_empty, strip_crlf, unique_probe_target_dir};
(B[m use serial_test::serial;
 use std::path::Path;
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_text_atomic.rs:38:
         let project_dir = cwd_path.join("test-projects/_fs_write_text_atomic");
         let temp_dir = unique_probe_target_dir("write-text-atomic-fixture");
 
[31m-        let binary_result = opalescent::compiler::compile_project(
(B[m[31m-            &project_dir,
(B[m[31m-            &temp_dir,
(B[m[31m-            &TargetTriple::host(),
(B[m[31m-        );
(B[m[32m+        let binary_result =
(B[m[32m+            opalescent::compiler::compile_project(&project_dir, &temp_dir, &TargetTriple::host());
(B[m         assert!(
             binary_result.is_ok(),
             "_fs_write_text_atomic fixture should compile into a binary: {}",
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_text_atomic.rs:49:
[31m-            binary_result
(B[m[31m-                .as_ref()
(B[m[31m-                .err()
(B[m[31m-                .map_or_else(|| String::from("unknown compile error"), |error| format!("{error}"))
(B[m[32m+            binary_result.as_ref().err().map_or_else(
(B[m[32m+                || String::from("unknown compile error"),
(B[m[32m+                |error| format!("{error}")
(B[m[32m+            )
(B[m         );
         let Ok(binary_path) = binary_result else {
             return;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/fs_write_text_atomic.rs:59:
         assert!(
             output_result.is_ok(),
             "_fs_write_text_atomic compiled binary should execute: {}",
[31m-            output_result
(B[m[31m-                .as_ref()
(B[m[31m-                .err()
(B[m[31m-                .map_or_else(|| String::from("unknown execution error"), |error| format!("{error}"))
(B[m[32m+            output_result.as_ref().err().map_or_else(
(B[m[32m+                || String::from("unknown execution error"),
(B[m[32m+                |error| format!("{error}")
(B[m[32m+            )
(B[m         );
         let Ok(run_output) = output_result else {
             return;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/project_execution.rs:973:
         "import-types-multiple should compile, run, print expected output, and exit cleanly: {failure_message}"
     );
 }
[31m-
(B[m 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/project_execution_rc.rs:5:
 #[test]
 fn rc_basic_compiles_and_runs() {
     let cwd = std::env::current_dir();
[31m-    assert!(cwd.is_ok(), "current working directory should be readable for integration tests");
(B[m[32m+    assert!(
(B[m[32m+        cwd.is_ok(),
(B[m[32m+        "current working directory should be readable for integration tests"
(B[m[32m+    );
(B[m     let Ok(cwd_path) = cwd else {
         return;
     };
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/project_execution_rc.rs:13:
     let project_dir = cwd_path.join("test-projects/rc-basic");
     let temp_dir = project_dir.join("target");
     let prepare = prepare_dir(&temp_dir);
[31m-    assert!(prepare.is_ok(), "rc-basic target directory should be created");
(B[m[32m+    assert!(
(B[m[32m+        prepare.is_ok(),
(B[m[32m+        "rc-basic target directory should be created"
(B[m[32m+    );
(B[m 
     let execution_result: Result<(), String> = (|| {
         let binary_result =
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/project_execution_rc.rs:21:
         let binary_path = match binary_result {
             Ok(path) => path,
             Err(error) => {
[31m-                return Err(format!("rc-basic project should compile into a binary: {error}"));
(B[m[32m+                return Err(format!(
(B[m[32m+                    "rc-basic project should compile into a binary: {error}"
(B[m[32m+                ));
(B[m             }
         };
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/project_execution_rc.rs:44:
     })();
 
     let cleanup = cleanup_dir(&temp_dir);
[31m-    assert!(cleanup.is_ok(), "rc-basic target directory should be removed");
(B[m[32m+    assert!(
(B[m[32m+        cleanup.is_ok(),
(B[m[32m+        "rc-basic target directory should be removed"
(B[m[32m+    );
(B[m     let failure_message = execution_result.err().unwrap_or_default();
     assert!(
         failure_message.is_empty(),
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/project_execution_rc.rs:55:
 #[test]
 fn rc_reuse_compiles_and_runs() {
     let cwd = std::env::current_dir();
[31m-    assert!(cwd.is_ok(), "current working directory should be readable for integration tests");
(B[m[32m+    assert!(
(B[m[32m+        cwd.is_ok(),
(B[m[32m+        "current working directory should be readable for integration tests"
(B[m[32m+    );
(B[m     let Ok(cwd_path) = cwd else {
         return;
     };
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/project_execution_rc.rs:63:
     let project_dir = cwd_path.join("test-projects/rc-reuse");
     let temp_dir = project_dir.join("target");
     let prepare = prepare_dir(&temp_dir);
[31m-    assert!(prepare.is_ok(), "rc-reuse target directory should be created");
(B[m[32m+    assert!(
(B[m[32m+        prepare.is_ok(),
(B[m[32m+        "rc-reuse target directory should be created"
(B[m[32m+    );
(B[m 
     let execution_result: Result<(), String> = (|| {
         let binary_result =
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/project_execution_rc.rs:71:
         let binary_path = match binary_result {
             Ok(path) => path,
             Err(error) => {
[31m-                return Err(format!("rc-reuse project should compile into a binary: {error}"));
(B[m[32m+                return Err(format!(
(B[m[32m+                    "rc-reuse project should compile into a binary: {error}"
(B[m[32m+                ));
(B[m             }
         };
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/project_execution_rc.rs:94:
     })();
 
     let cleanup = cleanup_dir(&temp_dir);
[31m-    assert!(cleanup.is_ok(), "rc-reuse target directory should be removed");
(B[m[32m+    assert!(
(B[m[32m+        cleanup.is_ok(),
(B[m[32m+        "rc-reuse target directory should be removed"
(B[m[32m+    );
(B[m     let failure_message = execution_result.err().unwrap_or_default();
     assert!(
         failure_message.is_empty(),
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/project_execution_rc.rs:105:
 #[test]
 fn iterative_drop_compiles_and_runs() {
     let cwd = std::env::current_dir();
[31m-    assert!(cwd.is_ok(), "current working directory should be readable for integration tests");
(B[m[32m+    assert!(
(B[m[32m+        cwd.is_ok(),
(B[m[32m+        "current working directory should be readable for integration tests"
(B[m[32m+    );
(B[m     let Ok(cwd_path) = cwd else {
         return;
     };
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/project_execution_rc.rs:113:
     let project_dir = cwd_path.join("test-projects/iterative-drop");
     let temp_dir = project_dir.join("target");
     let prepare = prepare_dir(&temp_dir);
[31m-    assert!(prepare.is_ok(), "iterative-drop target directory should be created");
(B[m[32m+    assert!(
(B[m[32m+        prepare.is_ok(),
(B[m[32m+        "iterative-drop target directory should be created"
(B[m[32m+    );
(B[m 
     let execution_result: Result<(), String> = (|| {
         let binary_result =
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/project_execution_rc.rs:146:
     })();
 
     let cleanup = cleanup_dir(&temp_dir);
[31m-    assert!(cleanup.is_ok(), "iterative-drop target directory should be removed");
(B[m[32m+    assert!(
(B[m[32m+        cleanup.is_ok(),
(B[m[32m+        "iterative-drop target directory should be removed"
(B[m[32m+    );
(B[m     let failure_message = execution_result.err().unwrap_or_default();
     assert!(
         failure_message.is_empty(),
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/project_execution_rc.rs:157:
 #[test]
 fn weak_ref_compiles_and_runs() {
     let cwd = std::env::current_dir();
[31m-    assert!(cwd.is_ok(), "current working directory should be readable for integration tests");
(B[m[32m+    assert!(
(B[m[32m+        cwd.is_ok(),
(B[m[32m+        "current working directory should be readable for integration tests"
(B[m[32m+    );
(B[m     let Ok(cwd_path) = cwd else {
         return;
     };
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/project_execution_rc.rs:165:
     let project_dir = cwd_path.join("test-projects/weak-ref");
     let temp_dir = project_dir.join("target");
     let prepare = prepare_dir(&temp_dir);
[31m-    assert!(prepare.is_ok(), "weak-ref target directory should be created");
(B[m[32m+    assert!(
(B[m[32m+        prepare.is_ok(),
(B[m[32m+        "weak-ref target directory should be created"
(B[m[32m+    );
(B[m 
     let execution_result: Result<(), String> = (|| {
         let binary_result =
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/project_execution_rc.rs:173:
         let binary_path = match binary_result {
             Ok(path) => path,
             Err(error) => {
[31m-                return Err(format!("weak-ref project should compile into a binary: {error}"));
(B[m[32m+                return Err(format!(
(B[m[32m+                    "weak-ref project should compile into a binary: {error}"
(B[m[32m+                ));
(B[m             }
         };
 
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/project_execution_rc.rs:182:
             .map_err(|error| format!("weak-ref compiled binary should execute: {error}"))?;
         let stdout = String::from_utf8_lossy(&run_output.stdout);
         if stdout.trim() != "weak-ref: ok" {
[31m-            return Err(format!("weak-ref stdout should equal 'weak-ref: ok', got: '{stdout}'"));
(B[m[32m+            return Err(format!(
(B[m[32m+                "weak-ref stdout should equal 'weak-ref: ok', got: '{stdout}'"
(B[m[32m+            ));
(B[m         }
         if !run_output.status.success() {
             return Err(format!(
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/project_execution_rc.rs:194:
     })();
 
     let cleanup = cleanup_dir(&temp_dir);
[31m-    assert!(cleanup.is_ok(), "weak-ref target directory should be removed");
(B[m[32m+    assert!(
(B[m[32m+        cleanup.is_ok(),
(B[m[32m+        "weak-ref target directory should be removed"
(B[m[32m+    );
(B[m     let failure_message = execution_result.err().unwrap_or_default();
     assert!(
         failure_message.is_empty(),
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/tests.rs:4:
 
 mod bytes_stdlib;
 mod compile_failures;
[31m-mod fs_path_from;
(B[m[31m-mod fs_normalize_path;
(B[m[32m+mod fs_absolute_path_sync;
(B[m[32m+mod fs_append_file_string;
(B[m[32m+mod fs_append_log;
(B[m[32m+mod fs_copy_file;
(B[m[32m+mod fs_dir_inventory;
(B[m[32m+mod fs_directories;
(B[m mod fs_join_path_components;
[32m+mod fs_metadata;
(B[m[32m+mod fs_normalize_path;
(B[m[32m+mod fs_path_from;
(B[m mod fs_path_helpers_query;
[31m-mod fs_absolute_path_sync;
(B[m[31m-mod fs_read_text;
(B[m[32m+mod fs_predicates;
(B[m mod fs_read_bytes;
[31m-mod fs_read_lines;
(B[m mod fs_read_first_line;
[32m+mod fs_read_lines;
(B[m[32m+mod fs_read_text;
(B[m mod fs_read_text_lines;
[31m-mod fs_write_file_string;
(B[m[32m+mod fs_rename_path;
(B[m mod fs_write_file_bytes;
[31m-mod fs_append_file_string;
(B[m[32m+mod fs_write_file_string;
(B[m mod fs_write_text_atomic;
[31m-mod fs_append_log;
(B[m[31m-mod fs_predicates;
(B[m[31m-mod fs_metadata;
(B[m[31m-mod fs_copy_file;
(B[m[31m-mod fs_rename_path;
(B[m[31m-mod fs_directories;
(B[m[31m-mod fs_dir_inventory;
(B[m // The following integration test modules are planned but their files do not
 // yet exist; they are commented out so `cargo fmt` can resolve the module
 // tree. Re-enable each `mod` line when the corresponding `.rs` file is added.
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/tests.rs:31:
[31m-mod fs_path_manipulation;
(B[m mod fs_markdown_roundtrip;
[32m+mod fs_path_manipulation;
(B[m // mod fs_management;
 // mod fs_reading;
 // mod fs_writing;
Diff in /home/justi/Projects/opalescent/tests/integration_e2e/tests.rs:36:
 // mod fs_directory;
 // mod fs_permissions;
 mod fs_directory_operations;
[32m+pub mod fs_helpers;
(B[m mod fs_rerunnability;
 pub mod fs_state_guard;
[31m-pub mod fs_helpers;
(B[m mod interactive_io;
 mod project_execution;
 mod project_execution_rc;
