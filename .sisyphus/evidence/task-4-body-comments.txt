Scenario B: Comments inside function bodies

Command:
  cargo run -- fmt --output /tmp/test_body_out.op .sisyphus/tmp/test_body_comments.op

Formatted output (/tmp/test_body_out.op):
entry main = f(): void => {
    # Step 1: setup
    let x = 42
    # Step 2: finish
    return void
}
