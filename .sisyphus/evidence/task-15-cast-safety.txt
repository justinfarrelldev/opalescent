Task: 4c cast safety matching spec (compile-time + runtime integer cast checks)
Date: 2026-04-17

Files changed:
- src/codegen/expressions.rs
- src/codegen/tests.rs

What was implemented:
- Added integer-cast safety in `codegen_cast` for unsafe integer casts:
  - narrowing integer casts (in_bits > out_bits)
  - same-width signed↔unsigned casts (in_bits == out_bits && signedness differs)
- Runtime guards now branch to trap blocks that call:
  - `opal_runtime_error("cast out of range: {source} to {target}")`
  - followed by `unreachable`
- Widening casts remain unchanged (no checks when source width < target width).
- Added compile-time constant detection for integer literal cast expressions:
  - if known in-range, emit constant true guard condition
  - if known out-of-range, emit constant false guard condition
  - still uses same trap/ok branching structure for consistency.

TDD evidence:
1) RED (new tests first)
   - Added tests:
     - test_codegen_narrowing_signed_int_cast_emits_runtime_range_trap
     - test_codegen_widening_signed_int_cast_emits_no_range_trap
     - test_codegen_same_width_signed_to_unsigned_cast_emits_runtime_range_trap
     - test_codegen_same_width_unsigned_to_signed_cast_emits_runtime_range_trap
   - Initial run (`cargo test cast_emits_runtime_range_trap`) failed with 3 failing tests,
     showing no runtime trap call existed for integer narrowing/signedness-changing casts.

2) GREEN
   - Implemented checks in `codegen_cast` before `build_int_truncate`/reinterpret path.
   - Re-ran the 4 tests individually; all passed.

Verification:
- LSP diagnostics (changed files):
  - src/codegen/expressions.rs: No diagnostics found
  - src/codegen/tests.rs: No diagnostics found
- Full test suite:
  - Command: `cargo test`
  - Result: PASS
  - Summary: `980 passed; 0 failed; 5 ignored`
  - Full command output captured by tool at:
    `/home/justi/.local/share/opencode/tool-output/tool_d9acb0763001ArzZFgMmWxJfir`
