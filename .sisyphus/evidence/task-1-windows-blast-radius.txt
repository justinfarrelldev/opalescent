Task 1 Windows blast-radius map
Timestamp: 2026-05-06T06:18:01Z
Plan source: .sisyphus/plans/windows-issues.md
Issue source: WINDOWS_ISSUES.md

Acceptance command baseline
- cargo test --all-features --workspace 2>&1 | tee .sisyphus/evidence/task-1-linux-baseline.txt
- Result: PASS on Linux (workspace baseline captured separately)

Unchecked Windows issue checklist (from WINDOWS_ISSUES.md)
- #1 path_parent_directory/path_file_name/path_file_extension ignore \\ separator
- #2 Bare strdup calls in path helpers
- #3 lex_normalize_path uses POSIX-only absolute path detection
- #4 join_path_components only recognises /-rooted absolute components
- #5 absolute_path_sync stores static string literals in error fields
- #6 ANSI narrow-char file I/O blocks Unicode paths
- #7 opal_opendir uses FindFirstFileA
- #8 opal_opendir does not set errno on FindFirstFileA failure
- #9 opal_closedir does not propagate errno on FindClose failure
- #10 opal_fs.c forward declarations can conflict with portability header definitions
- #11 opal_stat always reports is_symlink = 0 on Windows
- #12 Cargo.toml still includes llvm14-0-prefer-dynamic
- #13 CI cross-msvc-from-linux job installs xwin without version pinning
- #14 Windows hot-reload DLL path lacks copy-before-load behavior
- #15 opal_runtime_init is never called for generated programs
- #16 Filesystem path buffer capped at 260 bytes
- #17 MSVC linker invocation missing bcrypt.lib
- #18 opal_rc.c absent from RUNTIME_SOURCE
- #19 Missing XWIN_CACHE / OPAL_XWIN_SYSROOT panics instead of returning an error
- #20 quote_if_needed injects literal quote characters into Command::arg values

Platform-conditional inventory
- runtime/opal_portability.h:14,20,26 contain raw platform macro gates for _WIN32 and _MSC_VER.
- runtime/opal_portability.h:340 and 369 define OPAL_HAS_DIRENT for Windows/POSIX split.
- runtime/opal_fs.c:58,64,84 branches on OPAL_HAS_DIRENT and adds Windows-only forward declarations.
- src/build_system/linker.rs:219-229 uses #[cfg(windows)] / #[cfg(not(windows))] to select link.exe vs lld-link.
- src/compiler.rs:337-345 uses #[cfg(windows)] / #[cfg(not(windows))] to select cl.exe vs clang-cl.
- src/compiler.rs:911-919 contains host-sensitive Windows-linker assertions in tests.

Key Windows API / toolchain usage inventory
- runtime/opal_portability.h:287 uses FindFirstFileA.
- runtime/opal_portability.h:310 uses FindNextFileA.
- runtime/opal_portability.h:334 uses FindClose without errno mapping.
- runtime/opal_portability.h:504-547 uses _stat64 / GetFileAttributesA in opal_stat / opal_stat_nofollow.
- runtime/opal_portability.h:605 uses _unlink.
- runtime/opal_portability.h:631-635 uses MoveFileExA with immediate GetLastError capture.
- runtime/opal_fs.c:161,643,908,1020,1165,1386,1475,1512,1585,1637,1785,1791 use fopen on filesystem paths.
- runtime/opal_fs.c:1766-1768 uses _stat64 directly in copy_file_sync cross-device detection.
- src/build_system/linker.rs:241-244 requires XWIN_CACHE / OPAL_XWIN_SYSROOT on non-Windows hosts.
- scripts/verify-wine-prereqs.sh:39-47 and scripts/msvc_link_probe.sh:35-83 also depend on XWIN_CACHE.

Required blast-radius files (present)
1) runtime/opal_fs.c
   - 64-84: Windows dir shim forward declarations under !OPAL_HAS_DIRENT.
   - 86-88: safe_strdup currently uses strdup directly.
   - 367-489: lex_normalize_path only treats path[0] == '/' as absolute and collapses to '/'.
   - 496-559: join_path_components only resets accumulator when component[0] == '/'.
   - 562-587: path_parent_directory, path_file_name, path_file_extension use strrchr(..., '/').
   - 597+: absolute_path_sync is in the issue path for error ownership and absolute-path behavior.
   - 161/643/908/1020/1165/1386/1475/1512/1585/1637/1785/1791: fopen-based file I/O blast radius.
   - 1747-1791: copy/rename flows combine opal_stat/_stat64/fopen on Windows-sensitive paths.

2) runtime/opal_portability.h
   - 261-338: Windows opal_opendir / opal_readdir / opal_closedir implementation.
   - 267-279: stack pattern buffer sized by OPAL_PATH_BUFFER_CAP.
   - 287: FindFirstFileA entry point.
   - 326-337: FindClose path currently returns -1 without errno mapping.
   - 390-397: Windows opal_realpath uses _fullpath.
   - 497-524: opal_stat uses _stat64 and hardcodes is_symlink = 0 on Windows.
   - 526-569: opal_stat_nofollow mixes GetFileAttributesA and _stat64.
   - 598-608: opal_unlink maps to _unlink.
   - 624-638: opal_replace_path uses MoveFileExA.

3) src/build_system/linker.rs
   - 95-302: LinkerCommand builder is the central Rust blast radius for Windows linking.
   - 151-208: non-MSVC paths still call quote_if_needed before Command::arg.
   - 217-250: build_msvc selects linker binary and requires XWIN_CACHE / OPAL_XWIN_SYSROOT on non-Windows hosts.
   - 254-274: msvc_shared_args currently includes /DEFAULTLIB:libcmt but not bcrypt.
   - 285-292: quote_if_needed adds literal quote characters for spaced paths.
   - 390-690: tests cover MSVC/Linux-host behavior and are direct regression targets for issues 17, 19, and 20.

4) src/compiler.rs
   - 42-58: RUNTIME_SOURCE currently includes opal_fs.c but not opal_rc.c or opal_runtime.c.
   - 330-371: compile_runtime_c_to_obj chooses cl.exe vs clang-cl and returns linker-style errors on compile failure.
   - 383-398: build_linker_command instantiates build_system::LinkerCommand.
   - 404-444: link_object_files compiles runtime C to .obj for MSVC before linking.
   - 413: compile_runtime_c_to_obj is invoked from the Windows MSVC runtime path.

Symbol map and cross-file references
- compile_runtime_c_to_obj
  - Definition: src/compiler.rs:330
  - Call site: src/compiler.rs:413
  - Why it matters: Task 2/8 Windows bootstrap path; runtime source composition and MSVC object compilation converge here.

- LinkerCommand
  - Definition: src/build_system/linker.rs:95
  - Export surface: src/build_system.rs:20
  - Compiler call site: src/compiler.rs:390
  - Internal pressure points: build()/build_msvc()/msvc_shared_args()/quote_if_needed() across src/build_system/linker.rs:151-292

- lex_normalize_path
  - Definition: runtime/opal_fs.c:367
  - Direct callers: runtime/opal_fs.c:499,557,590,2029
  - Why it matters: lexical normalization drives path root handling and cascades into absolute-path, mkdir, and path helper behavior.

- join_path_components
  - Definition: runtime/opal_fs.c:496
  - Declared in runtime/opal_runtime.h:84
  - Codegen surface: src/codegen/functions_stdlib.rs:237-247,541 and src/codegen/statements.rs:730
  - Type-system bridge: src/type_system/checker/call_resolution.rs:286,541-552 and standard symbol registration file.
  - Integration coverage already exists: tests/integration_e2e/fs_join_path_components.rs

- opal_opendir
  - Definition: runtime/opal_portability.h:261 (Windows) and 357 (POSIX)
  - Forward declaration conflict site: runtime/opal_fs.c:64-84
  - Callers in runtime/opal_fs.c:257 and 2183
  - Why it matters: directory deletion/listing behavior and errno mapping depend on it.

- opal_stat / opal_stat_nofollow
  - Definitions: runtime/opal_portability.h:497 and 526
  - Broad caller surface in runtime/opal_fs.c: 194,315,630,895,1007,1152,1366,1621,1719,1748,1757,1888,1917,1960,2082,2144,2298,2324,2353,2379
  - Why it matters: metadata, symlink detection, delete/copy/rename guards, and error classification all depend on these helpers.

Adjacent verification surfaces worth reusing later
- tests/integration_e2e/fs_join_path_components.rs already exercises canonical join behavior.
- tests/integration_e2e/windows_wine.rs already contains Windows/Wine harness entry points.
- src/build_system/linker.rs test module already contains Linux-host MSVC coverage under #[cfg(not(windows))].

Task-1 conclusion
- Linux baseline is green.
- Windows blast radius is concentrated in the C portability boundary, C filesystem helpers, MSVC linker command construction, and runtime source embedding.
- No implementation code changes were made in Task 1.
