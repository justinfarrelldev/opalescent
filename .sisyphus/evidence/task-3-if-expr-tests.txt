Task: If Expression Semantics (Phase 2 Blocker #5)
Date: 2026-04-11

TDD evidence:
1) Added parser tests first:
   - test_if_expression_parses_in_expression_position
   - test_if_expression_without_else_parses
2) Added type-system tests first:
   - test_if_expression_infers_branch_type_when_branches_match
   - test_if_expression_branch_mismatch_reports_type_error
   - test_else_less_if_expression_defaults_to_unit_type
3) Implemented AST/parser/type-checker changes to satisfy red tests.

Validation commands and outcomes:
- cargo make lint-fix 2>&1 | tee temp.log
  Result: PASS
- cargo make lint 2>&1 | tee temp.log
  Result: PASS (zero warnings/errors)
- cargo make test 2>&1 | tee temp.log
  Result: PASS (276 passed, 0 failed, 1 ignored)
- scripts/check-line-count.sh 2>&1 | tee temp.log
  Result: PASS (all files <= limits)

Behavior evidence:
- If-else expression infers unified branch type.
- Branch mismatch produces TypeMismatch with branch spans.
- Else-less if expression yields unit semantics.
- Parser accepts if in expression position and statement position.
