Task 3 StoreMode TakeOwned Review
Date: 2026-05-22
Scope: explicit RC store mode abstraction only; no Task 4 behavior switches

Summary
- `StoreMode::TakeOwned` production occurrence count: 1
- All default production store paths remain on `StoreMode::Retain`.
- The only `TakeOwned` occurrence is the placeholder match arm in `src/codegen/binding_store.rs`; no call site opts into it yet.

Exact grep command outputs
$ grep -R -n --include=*.rs "StoreMode::TakeOwned" src
src/codegen/binding_store.rs:168:        StoreMode::TakeOwned => Ok(()),

$ grep -R -n --include=*.rs "StoreMode" src/codegen
src/codegen/binding_store.rs:21:pub(crate) enum StoreMode {
src/codegen/binding_store.rs:65:        StoreMode::Retain,
src/codegen/binding_store.rs:75:    store_mode: StoreMode,
src/codegen/binding_store.rs:154:    store_mode: StoreMode,
src/codegen/binding_store.rs:167:        StoreMode::Retain => retain_new_binding_value_if_needed(codegen_context, core_type, value),
src/codegen/binding_store.rs:168:        StoreMode::TakeOwned => Ok(()),
src/codegen/functions_call/array/helpers.rs:13:use crate::codegen::binding_store::{StoreMode, store_binding_overwrite_rc_safe_with_mode};
src/codegen/functions_call/array/helpers.rs:82:        StoreMode::Retain,
src/codegen/functions_call/array/helpers.rs:92:    store_mode: StoreMode,

Occurrence-by-occurrence review
1. src/codegen/binding_store.rs:168
   Code: `StoreMode::TakeOwned => Ok(())`
   Why this is safe / placeholder-only:
   - This is the enum behavior definition inside `prepare_new_binding_value_for_store_mode`, not a lowering site that selects `TakeOwned`.
   - The match arm only states what would happen if a future proven-fresh site opts into `TakeOwned`: skip the extra retain before the overwrite.
   - Task 3 keeps all existing production entry points conservative:
     - `store_binding_overwrite_rc_safe(...)` delegates to `store_binding_overwrite_rc_safe_with_mode(..., StoreMode::Retain)`.
     - `store_array_binding(...)` delegates to `store_array_binding_with_mode(..., StoreMode::Retain)`.
   - The `grep` evidence above shows there are no `StoreMode::TakeOwned` call-site uses anywhere under `src/`, so this arm is currently unreachable from production lowering flows.
   - Old-value release behavior is unchanged; runtime semantics are unchanged; this task only made ownership mode explicit for later Task 4 reviewable call-site switches.

Retain-default proof points
- `src/codegen/binding_store.rs:65` forces existing binding overwrite callers through `StoreMode::Retain`.
- `src/codegen/functions_call/array/helpers.rs:82` forces existing array binding store callers through `StoreMode::Retain`.
- No call site in `src/` passes `StoreMode::TakeOwned` today.

Conclusion
Task 3 meets the abstraction-only requirement: `StoreMode::{Retain, TakeOwned}` exists explicitly, `Retain` remains the conservative default, and `TakeOwned` is definition/plumbing only with zero production call-site adoption so far.
