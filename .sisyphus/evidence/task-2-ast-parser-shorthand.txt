Task 2 evidence — optional statement guard success binding
Date: 2026-05-07

Summary of implemented changes:
- Updated AST statement guard success binding to optional:
  - src/ast.rs: Stmt::Guard.success_binding changed from String to Option<String>.
- Updated statement guard parser to allow omitted `into` binding:
  - src/parser/statements.rs::parse_guard_statement now accepts:
    - `guard expr into ok else err => ...` => success_binding = Some("ok")
    - `guard expr else err => ...` => success_binding = None
  - Legacy explicit discard preserved:
    - `guard expr into _ else err => ...` => success_binding = Some("_")
- Expression guard parsing intentionally unchanged (still requires `into`).
- Updated parser tests for Option semantics:
  - guard_shorthand_without_into_parses_as_statement asserts success_binding.is_none().
  - Added guard_into_underscore_still_parses_as_explicit_binding asserting Some("_").
  - Updated existing success-binding assertion to success_binding.as_deref() == Some("n").
- Compile-fixed Stmt::Guard consumers:
  - src/type_system/checker/statements.rs: switched guard success binding paths to Option<&str> and conditional symbol registration.
  - src/codegen/statements.rs: switched guard success binding to Option<&str>, conditionally binds success value in env only when present.
  - src/formatter/printer.rs: prints `guard expr else err =>` when success binding omitted, otherwise preserves `into` form.
  - src/formatter/naming.rs: checks success binding naming only when binding exists.

LSP diagnostics on modified files:
- src/ast.rs: no diagnostics
- src/parser/statements.rs: no diagnostics
- src/parser/tests.rs: no diagnostics
- src/type_system/checker/statements.rs: no diagnostics
- src/codegen/statements.rs: no diagnostics
- src/formatter/printer.rs: no diagnostics
- src/formatter/naming.rs: no diagnostics

Required targeted tests run (all passed):
1) cargo test --lib guard_shorthand_without_into_parses_as_statement -- --nocapture
   result: ok
2) cargo test --lib guard_into_underscore_still_parses_as_explicit_binding -- --nocapture
   result: ok
3) cargo test --lib guard_expression_shorthand_still_requires_into -- --nocapture
   result: ok
4) cargo test --lib test_guard_success_binding_with_invalid_token -- --nocapture
   result: ok
